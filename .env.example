# Anthropic API Key (optional — only needed for tools that call Anthropic API directly)
# Translation pipeline uses Claude Code's native LLM layer (no API key needed)
# Get yours at: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-...
