---
phase: 51-logging-cli-standardization
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - tools/discovery/backfill_gaps.py
  - tools/discovery/orchestrator.py
  - tools/discovery/recommender.py
  - tools/discovery/format_filters.py
  - tools/discovery/competition.py
  - tools/discovery/keywords.py
  - tools/discovery/diagnostics.py
  - tools/discovery/vidiq_workflow.py
  - tools/discovery/autocomplete.py
  - tools/discovery/intent_mapper.py
  - tools/discovery/database.py
  - tools/discovery/demand.py
  - tools/discovery/metadata_checker.py
  - tools/discovery/opportunity.py
  - tools/discovery/trends.py
  - tools/intel/refresh.py
  - tools/intel/query.py
  - tools/production/parser.py
  - tools/production/split_screen_guide.py
  - tools/production/editguide.py
  - tools/production/metadata.py
  - tools/translation/cli.py
  - tools/translation/verification.py
  - tools/translation/smoke_test.py
  - tools/translation/formatter.py
  - tools/script_checkers/cli.py
  - tools/script_checkers/voice/pattern_extractor.py
  - tools/script_checkers/voice/corpus_builder.py
  - tools/script_checkers/voice/pattern_applier.py
  - tools/script_checkers/install_srt.py
  - tools/document_discovery/cli.py
  - tools/notebooklm_bridge.py
  - tools/citation_extractor.py
autonomous: true
requirements:
  - LOG-02
  - CLI-02

must_haves:
  truths:
    - "No diagnostic print() calls remain in discovery/, intel/, production/, translation/, script_checkers/, document_discovery/"
    - "Error conditions in all packages exit with code 1 and print to stderr"
    - "Running any tool with --quiet produces no output to stdout or stderr unless error"
  artifacts:
    - path: "tools/discovery/orchestrator.py"
      provides: "Discovery orchestrator with logging (47 prints converted)"
      contains: "get_logger"
    - path: "tools/intel/refresh.py"
      provides: "Intel refresh with logging (18 phase-marker prints converted)"
      contains: "get_logger"
  key_links:
    - from: "tools/discovery/*.py"
      to: "tools/logging_config.py"
      via: "from tools.logging_config import get_logger"
      pattern: "get_logger\\(__name__\\)"
    - from: "tools/intel/refresh.py"
      to: "tools/logging_config.py"
      via: "from tools.logging_config import get_logger"
      pattern: "get_logger\\(__name__\\)"
---

<objective>
Convert all diagnostic print() calls to logging in discovery/, intel/, production/, translation/, script_checkers/, document_discovery/, and root-level tool files. Standardize error exits across all packages.

Purpose: Completes LOG-02 coverage for all non-youtube_analytics packages (~356 print() calls across 33 files). After this plan, zero diagnostic print() calls remain anywhere in tools/.

Output: All tools/ modules use logger for diagnostics; print() only for intentional output; error exits standardized everywhere.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/51-logging-cli-standardization/51-CONTEXT.md
@.planning/phases/51-logging-cli-standardization/51-RESEARCH.md
@.planning/phases/51-logging-cli-standardization/51-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — tools/logging_config.py -->
from tools.logging_config import get_logger
logger = get_logger(__name__)

# Same conversion rules as Plan 02:
# KEEP as print() — intentional output (results, tables, JSON, reports)
# CONVERT to logger — diagnostic (progress, warnings, errors, debug)
# Use lazy formatting: logger.info("Found %d items", count)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert print() to logging in discovery/ package</name>
  <files>
    tools/discovery/backfill_gaps.py,
    tools/discovery/orchestrator.py,
    tools/discovery/recommender.py,
    tools/discovery/format_filters.py,
    tools/discovery/competition.py,
    tools/discovery/keywords.py,
    tools/discovery/diagnostics.py,
    tools/discovery/vidiq_workflow.py,
    tools/discovery/autocomplete.py,
    tools/discovery/intent_mapper.py,
    tools/discovery/database.py,
    tools/discovery/demand.py,
    tools/discovery/metadata_checker.py,
    tools/discovery/opportunity.py,
    tools/discovery/trends.py
  </files>
  <action>
For each file, add at the top (after existing imports):

```python
from tools.logging_config import get_logger
logger = get_logger(__name__)
```

Then convert diagnostic print() calls. **File-specific guidance:**

**backfill_gaps.py (65 prints):** Heavy diagnostic mix. Progress messages ("Processing keyword...", "Backfilling...") → logger.info. Debug details → logger.debug. Summary results at end → keep as print().

**orchestrator.py (47 prints):** Mix of report output (opportunity scores, rankings) and progress. Keep formatted report sections as print(). Convert "Running analysis...", "Fetching..." to logger.info.

**recommender.py (29 prints):** Report output (topic recommendations, scores) → keep. Progress → logger.info.

**format_filters.py (28 prints):** Report output (filter results, scores) → keep. Progress → logger.info.

**competition.py (28 prints):** Report output (competition analysis) → keep. Progress → logger.info.

**keywords.py (22 prints):** Database operation results → keep. Progress → logger.info.

**diagnostics.py (22 prints):** Diagnostic report output → keep. Internal progress → logger.

**vidiq_workflow.py (21 prints):** Workflow step output → keep if user-facing steps. Internal progress → logger.

**autocomplete.py (21 prints):** Many use `file=sys.stderr` already — convert those to logger directly. Rate-limit warnings → logger.warning. Progress → logger.info.

**intent_mapper.py (20 prints):** Classification results → keep. Progress → logger.

**database.py (23 prints):** Migration/setup messages → logger.info. Query results → keep.

**demand.py (13 prints):** Report output → keep. Status → logger.

**metadata_checker.py (3 prints), opportunity.py (2 prints), trends.py (0 prints):** Small conversions.

For files with `print(..., file=sys.stderr)` already (autocomplete.py, etc.), those convert directly to the matching logger level.

Also standardize error exits (CLI-02): In any __main__ block, ensure error paths use `print(..., file=sys.stderr)` + `sys.exit(1)`.
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && python -c "
import subprocess, sys
modules = [
    'tools.discovery.orchestrator',
    'tools.discovery.backfill_gaps',
    'tools.discovery.autocomplete',
    'tools.discovery.keywords',
]
for m in modules:
    r = subprocess.run([sys.executable, '-c', f'import {m}; print(\"OK: {m}\")'],
                       capture_output=True, text=True, cwd='G:/History vs Hype')
    if r.returncode != 0:
        print(f'FAIL: {m}: {r.stderr[:200]}')
    else:
        print(r.stdout.strip())
"</automated>
  </verify>
  <done>All 15 discovery/ files have diagnostic prints converted to logging; intentional output remains as print(); error exits standardized; all imports succeed</done>
</task>

<task type="auto">
  <name>Task 2: Convert print() to logging in intel/, production/, translation/, script_checkers/, document_discovery/, and root tool files</name>
  <files>
    tools/intel/refresh.py,
    tools/intel/query.py,
    tools/production/parser.py,
    tools/production/split_screen_guide.py,
    tools/production/editguide.py,
    tools/production/metadata.py,
    tools/translation/cli.py,
    tools/translation/verification.py,
    tools/translation/smoke_test.py,
    tools/translation/formatter.py,
    tools/script_checkers/cli.py,
    tools/script_checkers/voice/pattern_extractor.py,
    tools/script_checkers/voice/corpus_builder.py,
    tools/script_checkers/voice/pattern_applier.py,
    tools/script_checkers/install_srt.py,
    tools/document_discovery/cli.py,
    tools/notebooklm_bridge.py,
    tools/citation_extractor.py
  </files>
  <action>
Same pattern — add get_logger, convert diagnostic prints. **File-specific guidance:**

**intel/refresh.py (18 prints):** Has `_print_phase(n, description)` helper pattern for "[1/10] Checking..." markers. Convert ALL of these to logger.info. Remove the _print_phase helper if it only wraps print(). This file is NOT a CLI entry point — it's called from query.py. Do NOT add argparse.

**intel/query.py (1 print):** Minimal. Keep intentional output.

**production/parser.py (53 prints):** Mostly formatted parse output (entities, sections). Keep result tables. Convert "Parsing script..." progress to logger.info.

**production/split_screen_guide.py (10 prints):** Report output → keep. Progress → logger.

**production/editguide.py (4 prints), production/metadata.py (4 prints):** Small conversions.

**translation/cli.py (29 prints):** Mix of status messages ("Translating...", "Cross-checking...") and formatted results. Status → logger.info. Results → keep.

**translation/verification.py (2 prints), translation/smoke_test.py (11 prints):** smoke_test is a test utility — convert status prints to logger, keep results. verification.py — minimal.

**translation/formatter.py (0 direct prints but check):** May have print calls added in recent phases.

**script_checkers/cli.py (24 prints):** Report output (check results, scores) → keep. Progress → logger.

**script_checkers/voice/pattern_extractor.py (7 prints), corpus_builder.py (3 prints), pattern_applier.py (1 print):** Small conversions.

**script_checkers/install_srt.py (3 prints):** Setup messages → logger.info.

**document_discovery/cli.py (6 prints):** Report output → keep. Status → logger.

**notebooklm_bridge.py (23 prints):** Source list output → keep. Progress → logger.info.

**citation_extractor.py (10 prints):** Extraction results → keep. Progress → logger.

Standardize error exits in all CLI entry points: stderr + exit(1).

**Final sweep verification:** After converting all files, run a grep to confirm zero diagnostic prints remain. The success criteria requires `grep -r "print(" tools/` to return ONLY intentional output prints (those inside report/table formatting contexts).
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && python -c "
import re, os
# Count remaining diagnostic print() across ALL of tools/ (excluding history-clip-tool and test files)
suspicious = 0
for root, dirs, files in os.walk('tools'):
    if 'history-clip-tool' in root or '__pycache__' in root:
        continue
    for f in files:
        if not f.endswith('.py') or f.startswith('test_'):
            continue
        path = os.path.join(root, f)
        with open(path, encoding='utf-8') as fh:
            for i, line in enumerate(fh, 1):
                if re.search(r'\bprint\s*\(', line):
                    stripped = line.strip()
                    lower = stripped.lower()
                    # Flag prints that look diagnostic (should have been converted)
                    if any(kw in lower for kw in ['processing', 'fetching', 'loading', 'analyzing',
                                                    'debug:', 'warning:', 'error:', 'checking',
                                                    'running', 'starting', 'connecting']):
                        print(f'SUSPECT: {path}:{i}: {stripped[:80]}')
                        suspicious += 1
print(f'Total suspected unconverted diagnostic prints: {suspicious}')
print('PASS' if suspicious == 0 else f'REVIEW NEEDED: {suspicious} suspects')
"</automated>
  </verify>
  <done>All non-youtube_analytics packages have diagnostic prints converted to logging; zero diagnostic print() calls remain across entire tools/ codebase; error exits standardized; all imports succeed</done>
</task>

</tasks>

<verification>
1. `grep -rn "get_logger" tools/ --include="*.py" | grep -v history-clip-tool | grep -v __pycache__ | grep -v test_ | wc -l` — should be ~55+ (every module with diagnostic prints)
2. `python -m tools.discovery.orchestrator --help` — works with --verbose/--quiet
3. `python -m tools.intel.query --help` — works with --verbose/--quiet
4. `python -m tools.translation.cli --help` — works with --verbose/--quiet
5. Final diagnostic print() sweep returns zero suspects across all tools/
</verification>

<success_criteria>
- Every tools/ module (outside history-clip-tool and test files) uses logger for diagnostics
- All intentional output (tables, JSON, reports, scores) remains as print()
- All CLI error paths use stderr + exit(1)
- All modules import successfully
- Combined with Plan 02, LOG-02 is fully satisfied across all packages
</success_criteria>

<output>
After completion, create `.planning/phases/51-logging-cli-standardization/51-03-SUMMARY.md`
</output>
