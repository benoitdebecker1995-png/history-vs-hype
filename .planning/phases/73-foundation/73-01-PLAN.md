---
phase: 73-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/AGENT-ORCHESTRATION.md
  - CLAUDE.md
  - .claude/USER-PREFERENCES.md
autonomous: true
requirements: [AGENT-01]
tags: [foundation, agent-orchestration, rules-doc, v9.0]

must_haves:
  truths:
    - ".claude/AGENT-ORCHESTRATION.md exists and is under 400 lines"
    - "Doc contains 5 locked top-level sections: Spawning Pattern, Return Contract, Reference Loading Tiers, Rate-Limit Resilience, Extend Don't Add"
    - "Doc contains a Worked Examples placeholder section (empty) for Phase 75a to fill"
    - "Return contract block is identifiable by the marker <!-- RETURN-CONTRACT-V1 --> so Phase 75a's copy-paste is mechanical"
    - "CLAUDE.md Critical Reminders section references AGENT-ORCHESTRATION.md (item 15)"
    - ".claude/USER-PREFERENCES.md contains a Main Context = Orchestrator Only section pointing at AGENT-ORCHESTRATION.md"
  artifacts:
    - path: ".claude/AGENT-ORCHESTRATION.md"
      provides: "The v9.0 agent-orchestration rules doc cited by Phases 74-79"
      contains:
        - "## Spawning Pattern"
        - "## Return Contract"
        - "## Reference Loading Tiers"
        - "## Rate-Limit Resilience"
        - "## Extend, Don't Add"
        - "## Worked Examples"
        - "<!-- RETURN-CONTRACT-V1 -->"
    - path: "CLAUDE.md"
      provides: "Pointer from always-loaded project instructions to the orchestration doc"
      contains: "AGENT-ORCHESTRATION.md"
    - path: ".claude/USER-PREFERENCES.md"
      provides: "Main Context = Orchestrator Only section restating the core rule"
      contains: "Main Context = Orchestrator Only"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/AGENT-ORCHESTRATION.md"
      via: "Critical Reminders item 15 referencing the filename"
      pattern: "AGENT-ORCHESTRATION\\.md"
    - from: ".claude/USER-PREFERENCES.md"
      to: ".claude/AGENT-ORCHESTRATION.md"
      via: "pointer line in the new Main Context = Orchestrator Only section"
      pattern: "AGENT-ORCHESTRATION\\.md"
    - from: ".claude/AGENT-ORCHESTRATION.md"
      to: "Phase 75a worked-example insertion"
      via: "empty '## Worked Examples' section serving as a named anchor"
      pattern: "## Worked Examples"
---

<objective>
Write `.claude/AGENT-ORCHESTRATION.md` — the v9.0 rules doc every subsequent phase cites — and wire the minimal integration touches in CLAUDE.md and USER-PREFERENCES.md so the rules are discoverable from always-loaded instructions.

Purpose: v9.0's thesis ("rules that keep it clean") collapses without a concrete, cite-able rules doc. This phase produces the load-bearing reference. Rushed or vague output = milestone ships as cleanup that re-accumulates within months (same failure mode as v1.0 Phase 05).

Output: 1 new doc + 2 surgical edits to existing always-loaded instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-foundation/73-CONTEXT.md
@CLAUDE.md
@.claude/USER-PREFERENCES.md
</context>

<scope_guardrails>
This is a PURE WRITING phase. Do NOT:
- Refactor any agent in `.claude/agents/` (9 agents exist — inventory only; Phase 75a applies the contract to them)
- Refactor any command in `.claude/commands/` (Phase 75a-d owns command refactors)
- Write concrete worked examples (deferred to Phase 75a — leave the `## Worked Examples` section empty under a placeholder comment)
- Write per-agent token-measurement patterns (deferred to Phase 79)
- Write reference-load accounting format (deferred to Phase 79)

The doc is a SPEC, not an implementation. Phase 75a is the first implementation.
</scope_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Write .claude/AGENT-ORCHESTRATION.md — the v9.0 rules doc</name>

  <files>.claude/AGENT-ORCHESTRATION.md</files>

  <read_first>
    - .planning/phases/73-foundation/73-CONTEXT.md (the 5 locked sections + return-contract required elements + 400-line cap + Worked Examples placeholder rule)
    - .planning/ROADMAP.md (Phase 73 section lines 244-262 — success criteria; also Phase 75a lines 285-306 and Phase 79 lines 430-449 so you know how this doc is cited downstream)
    - .planning/REQUIREMENTS.md (AGENT-01 at line 18; also the Out of Scope table at lines 76-86 to avoid overreach)
    - CLAUDE.md (so your cross-references match existing always-loaded tone and structure)
  </read_first>

  <action>
Create `.claude/AGENT-ORCHESTRATION.md` as a new file. The doc is the authoritative v9.0 rules spec — every later phase cites it.

**Structural requirements (non-negotiable):**

1. The doc MUST use these EXACT top-level headings in this order (each heading is a named anchor cited by later phases):

   ```
   # Agent Orchestration Rules (v9.0)

   > **Status:** Load-bearing spec for v9.0. Cited by Phases 74-79.
   > **Length discipline:** Hard cap 400 lines. This doc is itself the first test of "extend, don't add" — if it grows past 400 lines, it has failed its own rule. Prefer examples and terse statements over explanation.

   ## Why This Doc Exists

   [2-4 sentence preamble: main context = orchestrator; heavy reads + reasoning delegated; rules codified so the lean state survives past v9.0; cite the v1.0 Phase 05 failure mode from ROADMAP line 200 as the thing this doc prevents.]

   ## Spawning Pattern

   [When to spawn / How to invoke / What goes in the prompt. See detailed content requirements below.]

   ## Return Contract

   [The copy-pasteable block. See detailed content requirements below.]

   ## Reference Loading Tiers

   [Tier 0 / Tier 1 / Tier 2 / Tier 3. See detailed content requirements below.]

   ## Rate-Limit Resilience

   [Sequential-over-parallel fallback rule. See detailed content requirements below.]

   ## Extend, Don't Add

   [The default rule + the 400-line self-test. See detailed content requirements below.]

   ## Worked Examples

   <!-- PLACEHOLDER: filled by Phase 75a (/research pilot). Do not write examples in Phase 73. -->
   <!-- Phase 75a success criterion #7 (ROADMAP line 298): "Pattern documented in .claude/AGENT-ORCHESTRATION.md as a concrete worked example so 75b/c/d can cite 'apply the 75a pattern'". -->
   <!-- Phase 75a APPENDS here — does not rewrite earlier sections. -->

   ## Cross-References

   [Link back to CLAUDE.md, USER-PREFERENCES.md, ROADMAP Phase 79 (enforcement via /status --surface).]
   ```

2. **`## Spawning Pattern` section content requirements:**
   - State the three triggers verbatim (AGENT-01 in REQUIREMENTS.md line 18 + AGENT-02 line 19 phrasing):
     - Read >500 lines
     - Reasoning >3 steps
     - Parallel independent work (2+ tasks with no shared state)
   - State how to invoke: use the Task tool with an explicit `subagent_type`; main context does NOT read the files the sub-agent will read
   - State what goes in the spawn prompt: (a) `<read_first>` block with exact paths, (b) the single goal, (c) the Return Contract pasted verbatim (point readers to the marker — see next section)
   - State the anti-pattern: "Main context opens the file 'just to check' before spawning" — this defeats the point

3. **`## Return Contract` section content requirements — the single most-copied artifact in v9.0. Get it right.**
   - Open the section with one sentence explaining that the fenced block below is pasted verbatim into every sub-agent prompt
   - Wrap the contract block in the marker comments `<!-- RETURN-CONTRACT-V1 -->` (open) and `<!-- /RETURN-CONTRACT-V1 -->` (close) so Phase 75a's copy-paste is mechanical (a grep for the marker yields the exact range)
   - Inside the markers, use a fenced markdown block containing a contract with these four REQUIRED elements (CONTEXT.md lines 33-38). Keep wording tight; suggested shape:

     ```
     === SUB-AGENT RETURN CONTRACT v1 ===

     1. SUMMARY (≤200 words). Structured as:
        - What you did (1-2 sentences)
        - Where the output lives (absolute path)
        - What's next (1 sentence — the orchestrator's next move)

     2. WRITE FULL OUTPUT TO DISK. Path convention:
        `.planning/phases/{phase}/agent-outputs/{agent}-{task}-{timestamp}.md`
        (or a project-appropriate path stated in the spawn prompt).
        Do NOT return the full output inline.

     3. NO RAW DUMPS. Do not paste raw quotes, transcripts, or any excerpt
        >10 lines into the return. If the caller needs a specific quote,
        cite the file + line range; the orchestrator reads on demand.

     4. HANDOFF LINE. Final line of the return MUST be exactly:
        `OUTPUT: <absolute-path>`
        The orchestrator parses this line to pick up the artifact.

     === END CONTRACT ===
     ```
   - Below the fenced block, add a one-line note: "Phase 75a-d and Phase 77 paste this block verbatim into their agent prompts. If this contract changes, bump to `RETURN-CONTRACT-V2` and update callers — do not silently edit."

4. **`## Reference Loading Tiers` section content requirements:**
   - Define the four tiers (CONTEXT.md line 28):
     - **Tier 0 — Always loaded:** CLAUDE.md, MEMORY.md. Loaded into every session unconditionally.
     - **Tier 1 — Phase-critical:** loaded by the orchestrator when the current phase/command cites them in frontmatter.
     - **Tier 2 — Task-specific:** loaded ONLY by the sub-agent that needs them, named in its `<read_first>` block. Main context never reads these.
     - **Tier 3 — On-demand:** loaded only if explicitly requested by the user or cited by a prior artifact mid-run.
   - State the core principle in one sentence: "Every additional ref above Tier 0 requires a named caller — no orphan refs."
   - Cross-reference Phase 74 (REF-05 — INDEX.md regeneration with 'last used by' annotations) and Phase 79 (TOK-04 — reference-load accounting) without depending on them.

5. **`## Rate-Limit Resilience` section content requirements (ROADMAP 75a success criterion #6 + AGENT-07):**
   - State the detection surface: HTTP 429, error strings matching `rate`, `rate-exceeded`, `rate limit`, `too many requests` (case-insensitive).
   - State the rule: on detection, the orchestrator serializes pending spawns (sequential, one at a time) and retries the failed spawn — it does NOT fail the command.
   - State the default backoff: 30s, 60s, 120s (three retries max) — orchestrator surfaces an AskUserQuestion checkpoint if all three fail.
   - State that this is a SPEC; the first implementation is Phase 75a. Phase 75a's worked example will make it concrete.

6. **`## Extend, Don't Add` section content requirements:**
   - State the default rule in one sentence: "Before creating any new reference, command, or agent, check whether an existing one can absorb the change."
   - State the acceptance test: "New surface area requires a named justification in the phase's CONTEXT.md or ROADMAP entry — not an implicit choice."
   - State the self-test: "This doc is capped at 400 lines. If it bloats past that cap, it has failed its own rule — split or cut, don't grow."
   - Cross-reference Phase 79 TOK-03 (monthly `/status --surface` check) as the enforcement mechanism — "rule here, measurement there."

7. **`## Cross-References` section:**
   - Link to CLAUDE.md Critical Reminders (item 15 — updated in Task 2 of this plan)
   - Link to USER-PREFERENCES.md "Main Context = Orchestrator Only" section (added in Task 2)
   - Link to ROADMAP Phase 79 (monthly surface-area check)
   - Link to REQUIREMENTS.md AGENT-01 line 18

**Tone / style:**
- Terse, imperative, rule-shaped. No hedging, no "consider", no "generally".
- No emojis.
- Use fenced code blocks for any path examples.
- Each section: prefer 3-8 lines of rules over 3 paragraphs of explanation.

**Length discipline (self-enforcing):**
- Target 250-350 lines. Hard cap 400. If you hit 380 while writing, cut examples — do not cut sections.
- Running `wc -l .claude/AGENT-ORCHESTRATION.md` after writing MUST return ≤400.

**What NOT to write (per CONTEXT.md deferred list + scope guardrails):**
- Do NOT write concrete worked examples in the Worked Examples section — leave the HTML-comment placeholder there. Phase 75a fills it.
- Do NOT write per-agent token-measurement patterns (Phase 79).
- Do NOT write reference-load accounting format (Phase 79).
- Do NOT inventory or modify the 9 agents in `.claude/agents/` (Phase 75a).

**Per D (Decisions, CONTEXT.md lines 20-51):**
- The 5 locked section names are non-negotiable.
- The 4 return-contract elements are non-negotiable.
- The 400-line cap is non-negotiable.
- The `## Worked Examples` placeholder is non-negotiable.
- The `<!-- RETURN-CONTRACT-V1 -->` marker is non-negotiable (deep_work_rules acceptance criterion).
  </action>

  <verify>
    <automated>test -f .claude/AGENT-ORCHESTRATION.md && grep -q "^## Spawning Pattern$" .claude/AGENT-ORCHESTRATION.md && grep -q "^## Return Contract$" .claude/AGENT-ORCHESTRATION.md && grep -q "^## Reference Loading Tiers$" .claude/AGENT-ORCHESTRATION.md && grep -q "^## Rate-Limit Resilience$" .claude/AGENT-ORCHESTRATION.md && grep -q "^## Extend, Don't Add$" .claude/AGENT-ORCHESTRATION.md && grep -q "^## Worked Examples$" .claude/AGENT-ORCHESTRATION.md && grep -q "<!-- RETURN-CONTRACT-V1 -->" .claude/AGENT-ORCHESTRATION.md && [ $(wc -l < .claude/AGENT-ORCHESTRATION.md) -le 400 ] && grep -q "429" .claude/AGENT-ORCHESTRATION.md && grep -q "sequential" .claude/AGENT-ORCHESTRATION.md && grep -q "Tier 0" .claude/AGENT-ORCHESTRATION.md && grep -q "Tier 3" .claude/AGENT-ORCHESTRATION.md && awk '/^## Worked Examples$/,/^## /' .claude/AGENT-ORCHESTRATION.md | grep -q "PLACEHOLDER" && ! awk '/^## Worked Examples$/,/^## /' .claude/AGENT-ORCHESTRATION.md | grep -qiE "^### Example|^- Example:|^Example [0-9]"</automated>
  </verify>

  <acceptance_criteria>
    - File `.claude/AGENT-ORCHESTRATION.md` exists
    - File contains exact heading `## Spawning Pattern` on its own line
    - File contains exact heading `## Return Contract` on its own line
    - File contains exact heading `## Reference Loading Tiers` on its own line
    - File contains exact heading `## Rate-Limit Resilience` on its own line
    - File contains exact heading `## Extend, Don't Add` on its own line
    - File contains exact heading `## Worked Examples` on its own line
    - File contains the marker `<!-- RETURN-CONTRACT-V1 -->` (at least one occurrence)
    - File line count ≤ 400 (`wc -l` check)
    - The Worked Examples section contains NO concrete example — only a placeholder HTML comment (grep for "PLACEHOLDER" inside the section passes; grep for "Example:" or numbered example headings in that section fails)
    - Return Contract section references all 4 required elements: `≤200 word` summary, `write` to disk / `WRITE FULL OUTPUT TO DISK`, `no raw` / `NO RAW DUMPS`, `OUTPUT:` handoff line
    - Rate-Limit Resilience section contains the strings `429` and `sequential`
    - Reference Loading Tiers section contains `Tier 0`, `Tier 1`, `Tier 2`, `Tier 3`
  </acceptance_criteria>

  <done>
    `.claude/AGENT-ORCHESTRATION.md` exists, ≤400 lines, contains all 6 locked section headings plus the Cross-References and Why This Doc Exists sections, return contract wrapped in the `RETURN-CONTRACT-V1` marker, Worked Examples section left empty for Phase 75a to fill. Every success criterion from ROADMAP Phase 73 lines 250-255 except integration touches (handled in Task 2) is satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CLAUDE.md and USER-PREFERENCES.md to the new doc</name>

  <files>CLAUDE.md, .claude/USER-PREFERENCES.md</files>

  <read_first>
    - CLAUDE.md (current Critical Reminders section lines 133-149 — append as item 15)
    - .claude/USER-PREFERENCES.md (current structure — decide insertion point; recommended: after the top-of-file "Research Quality Standards" section OR before "Communication Style" section so it sits near the top)
    - .planning/phases/73-foundation/73-CONTEXT.md (locked integration-touch wording at lines 40-41)
    - .claude/AGENT-ORCHESTRATION.md (created in Task 1 — must exist before this task runs)
  </read_first>

  <action>
Make two surgical edits. Both reference `.claude/AGENT-ORCHESTRATION.md` which was created in Task 1.

**Edit 1 — CLAUDE.md Critical Reminders section:**

Append a new item 15 to the numbered list in the `## Critical Reminders` section (currently ends at item 14 — "Text overlay on thumbnails"). Insert this EXACT line as item 15, before the `---` separator on line 150:

```
15. **AGENT ORCHESTRATION** — Read `.claude/AGENT-ORCHESTRATION.md` before spawning sub-agents — return contract, tiers, rate-limit rule
```

Do NOT change any other items, any other section, or the file's overall structure. The edit is purely an append to the numbered list.

Per D (CONTEXT.md line 40): locked wording is "Read `.claude/AGENT-ORCHESTRATION.md` before spawning sub-agents — return contract, tiers, rate-limit rule". Use the locked wording verbatim.

**Edit 2 — .claude/USER-PREFERENCES.md "Main Context = Orchestrator Only" section:**

Insert a new top-level section into `.claude/USER-PREFERENCES.md`. Recommended insertion point: immediately after the closing `---` of the "Research Quality Standards (CRITICAL)" section (between current lines 23 and 25 — i.e., after the `---` on line 23 and before `## Communication Style` on line 25). This places the rule high in the file where it's discoverable.

Section content (use this wording exactly — satisfies CONTEXT.md lines 40-41 and the specifics note at lines 77-78):

```
## Main Context = Orchestrator Only

**The rule (v9.0):** Main context is an orchestrator. Heavy reads and multi-step reasoning are delegated to sub-agents. If the work involves reading >500 lines, reasoning across >3 steps, or running independent tasks in parallel — spawn a sub-agent.

**Why it matters for session flow:**
- Main context stays terse, the conversation stays conversational
- Token cost scales with delegation, not with work
- AskUserQuestion checkpoints land BEFORE expensive actions, not after

**Full spec:** `.claude/AGENT-ORCHESTRATION.md` (return contract, reference tiers, rate-limit fallback, "extend, don't add" default)

**Codified in:** Phase 73 (v9.0 Foundation). Enforced monthly via `/status --surface` (Phase 79).

---
```

Do NOT modify any other section of USER-PREFERENCES.md.

**Per D (CONTEXT.md line 41 — locked wording):**
"Add a new section 'Main Context = Orchestrator Only' that restates the core rule and points to AGENT-ORCHESTRATION.md" — both achieved above.

**Order:** Edit 1 and Edit 2 are independent. Execute in any order — just both.
  </action>

  <verify>
    <automated>grep -q "AGENT-ORCHESTRATION\.md" CLAUDE.md && grep -Pzo "(?s)## Critical Reminders.*?AGENT-ORCHESTRATION\.md" CLAUDE.md >/dev/null && grep -q "^## Main Context = Orchestrator Only$" .claude/USER-PREFERENCES.md && grep -q "AGENT-ORCHESTRATION\.md" .claude/USER-PREFERENCES.md</automated>
  </verify>

  <acceptance_criteria>
    - `CLAUDE.md` contains the string `AGENT-ORCHESTRATION.md` (at least one occurrence)
    - The `AGENT-ORCHESTRATION.md` reference in CLAUDE.md appears AFTER the `## Critical Reminders` heading and BEFORE the next `---` separator (i.e., it lives inside that section — verifiable with `grep -Pzo "(?s)## Critical Reminders.*?AGENT-ORCHESTRATION"`)
    - `.claude/USER-PREFERENCES.md` contains the exact heading `## Main Context = Orchestrator Only` on its own line
    - `.claude/USER-PREFERENCES.md` contains the string `AGENT-ORCHESTRATION.md` (at least one occurrence — the pointer from the new section)
    - No other sections of CLAUDE.md or USER-PREFERENCES.md are modified (diff scope = +1 numbered-list item in CLAUDE.md and +1 new section in USER-PREFERENCES.md)
  </acceptance_criteria>

  <done>
    CLAUDE.md Critical Reminders section has a new item 15 pointing at AGENT-ORCHESTRATION.md. USER-PREFERENCES.md has a new "Main Context = Orchestrator Only" section with the locked wording and a pointer to AGENT-ORCHESTRATION.md. Both edits surgical — no unrelated changes. ROADMAP Phase 73 success criteria #3 and the CONTEXT.md locked integration touches are satisfied.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run end-to-end phase verification:

```
# 1. Doc exists and under cap
test -f .claude/AGENT-ORCHESTRATION.md
[ $(wc -l < .claude/AGENT-ORCHESTRATION.md) -le 400 ]

# 2. All 5 locked sections + Worked Examples placeholder present
for h in "Spawning Pattern" "Return Contract" "Reference Loading Tiers" "Rate-Limit Resilience" "Extend, Don't Add" "Worked Examples"; do
  grep -q "^## $h$" .claude/AGENT-ORCHESTRATION.md || echo "MISSING: $h"
done

# 3. Return contract marker present (Phase 75a copy-paste anchor)
grep -c "<!-- RETURN-CONTRACT-V1 -->" .claude/AGENT-ORCHESTRATION.md

# 4. Worked Examples section is empty (placeholder only — Phase 75a fills it)
# Acceptable: the section contains HTML comments but no example content.

# 5. CLAUDE.md links to the doc inside Critical Reminders
grep -Pzo "(?s)## Critical Reminders.*?AGENT-ORCHESTRATION\.md" CLAUDE.md

# 6. USER-PREFERENCES.md has the new section and a pointer
grep -q "^## Main Context = Orchestrator Only$" .claude/USER-PREFERENCES.md
grep -q "AGENT-ORCHESTRATION\.md" .claude/USER-PREFERENCES.md
```

All six checks must pass.

**Sanity read:** Re-read the full `.claude/AGENT-ORCHESTRATION.md` end-to-end. Confirm a future session could copy the `RETURN-CONTRACT-V1`-marked block verbatim into an agent prompt with zero edits — that is the Phase 75a use case.
</verification>

<success_criteria>
Maps to ROADMAP Phase 73 success criteria (lines 250-255):

1. ✅ `.claude/AGENT-ORCHESTRATION.md` exists and contains all 5 required subjects: spawning pattern, return contract (≤200-word summary, write-to-disk, no raw quotes), reference loading tiers, sequential-over-parallel rate-limit rule, "extend, don't add" default
2. ✅ Return contract block is copy-pasteable — wrapped in `<!-- RETURN-CONTRACT-V1 -->` markers so Phase 75a's extraction is mechanical
3. ✅ `CLAUDE.md` Critical Reminders section references the new doc (item 15)
4. ✅ Every later phase (74-79) can cite a specific section of this doc — all 5 locked section names exist as named anchors
5. ✅ Doc length ≤400 lines (self-enforcing example of "extend, don't add")

Phase 73 ships when all 5 roadmap criteria pass AND the USER-PREFERENCES.md integration (ROADMAP line 259) is in place.
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/73-foundation/73-01-SUMMARY.md` documenting:
- Final line count of `.claude/AGENT-ORCHESTRATION.md`
- Confirmation the 5 locked sections and Worked Examples placeholder exist
- Confirmation the `RETURN-CONTRACT-V1` marker is in place
- Confirmation CLAUDE.md item 15 and USER-PREFERENCES.md new section are wired
- Any deviations from the locked spec (expected: none)
- Handoff note for Phase 74: "AGENT-ORCHESTRATION.md's `Extend, Don't Add` section is the rule Phase 74 cites when merging scriptwriting trio and hook duo into ≤70% of original length — consolidation by synthesis, not concatenation."
</output>
