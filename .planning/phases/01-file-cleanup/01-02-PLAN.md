---
phase: 01-file-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - transcripts/Kraut - The Origins of Russian Authoritarianism.en.vtt (DELETE)
  - research/_archive/Cambodia-Thailand_NotebookLM-Research-Compilation (1).md (DELETE)
  - research/_archive/Cambodia-Thailand_NotebookLM-Research-Compilation (2).md (DELETE)
  - research/_archive/History vs Hype.docx (DELETE, keep newest)
  - transcripts/south-china-sea-map-duplicate.srt (DELETE)
  - transcripts/SPANISH-subtitles-2.srt (DELETE)
  - transcripts/DUPLICATE-alt-version-*.srt (DELETE 3 files)
  - transcripts/essequibo-venezuela-guyana-ALT-VERSION.srt (review)
  - transcripts/thailand-cambodia-border-ALT-VERSION.srt (review)
autonomous: true

must_haves:
  truths:
    - "No explicit duplicate files exist in transcripts/"
    - "No numbered duplicate copies exist in research/_archive/"
    - "Each transcript file is unique (no -duplicate, -ALT-VERSION copies without reason)"
  artifacts:
    - path: "transcripts/"
      provides: "Clean transcript folder without duplicates"
      constraint: "No files with -duplicate or (1)/(2) in name"
    - path: "research/_archive/"
      provides: "Archive with unique files only"
      constraint: "No numbered copies of same file"
  key_links:
    - from: "video-projects/"
      to: "research files"
      via: "should reference single source, not duplicates"
      pattern: "Research-Compilation\\.md$"
---

<objective>
Consolidate duplicate files into single sources of truth

Purpose: Eliminate confusion about which version is canonical. Research identified 8+ explicit duplicates and several ALT-VERSION files that need review.

Output: Each piece of content exists in exactly one place with clear naming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-file-cleanup/01-RESEARCH.md

Key finding from RESEARCH.md: "Duplicate Files Analysis" section identifies confirmed duplicates and partial duplicates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete explicit duplicates in transcripts/</name>
  <files>
    - transcripts/Kraut - The Origins of Russian Authoritarianism.en.vtt
    - transcripts/south-china-sea-map-duplicate.srt
    - transcripts/SPANISH-subtitles-2.srt
    - transcripts/DUPLICATE-alt-version-*.srt (3 files)
  </files>
  <action>
    These are CONFIRMED duplicates based on naming:

    1. Delete Kraut transcript duplicate:
       - KEEP: `transcripts/Kraut/The Origins of Russian Authoritarianism.en.vtt` (organized version)
       - DELETE: `transcripts/Kraut - The Origins of Russian Authoritarianism.en.vtt` (root duplicate)

    2. Delete explicitly-named duplicates:
       - `rm transcripts/south-china-sea-map-duplicate.srt`
       - `rm transcripts/SPANISH-subtitles-2.srt` (keep SPANISH-subtitles-1.srt)
       - `rm transcripts/DUPLICATE-alt-version-*.srt` (all 3)

    Before deleting ALT-VERSION files, verify they're true duplicates:
    3. Compare ALT-VERSION files:
       - `diff transcripts/essequibo-venezuela-guyana.srt transcripts/essequibo-venezuela-guyana-ALT-VERSION.srt`
       - If identical or ALT is older: DELETE ALT-VERSION
       - If different and ALT is better: KEEP ALT, delete original, rename ALT to remove suffix

       - `diff transcripts/thailand-cambodia-border.srt transcripts/thailand-cambodia-border-ALT-VERSION.srt`
       - Same logic as above
  </action>
  <verify>
    - `ls transcripts/*duplicate*` returns no files
    - `ls transcripts/*ALT-VERSION*` returns no files (either deleted or renamed)
    - `ls transcripts/DUPLICATE*` returns no files
    - `ls transcripts/Kraut\ -\ *.vtt` returns no files (root duplicate gone)
  </verify>
  <done>All explicit transcript duplicates removed. Each transcript exists once with canonical name.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate research archive duplicates</name>
  <files>
    - research/_archive/Cambodia-Thailand_NotebookLM-Research-Compilation.md (if exists)
    - research/_archive/Cambodia-Thailand_NotebookLM-Research-Compilation (1).md
    - research/_archive/Cambodia-Thailand_NotebookLM-Research-Compilation (2).md
    - research/_archive/History vs Hype.docx
    - research/_archive/History_vs_Hype.docx
  </files>
  <action>
    1. Cambodia-Thailand research duplicates:
       - List all versions: `ls research/_archive/*Cambodia*`
       - Compare file sizes and dates
       - Determine which is most complete (likely original without number)
       - KEEP the most complete version
       - DELETE the (1) and (2) copies

    2. History vs Hype .docx duplicates:
       - Compare: `ls -la research/_archive/History*Hype*.docx`
       - KEEP newest/largest (likely History_vs_Hype.docx based on naming)
       - DELETE older version

    3. Check for any other numbered duplicates in archive:
       - `ls research/_archive/ | grep -E '\([0-9]+\)'`
       - Delete any (1), (2), etc. copies after verifying original exists

    4. If research/_archive/ is empty or only has obsolete files after cleanup:
       - Consider deleting entire folder if all content moved elsewhere
       - Or keep as legitimate archive with clear contents
  </action>
  <verify>
    - `ls research/_archive/ | grep -E '\([0-9]+\)'` returns nothing (no numbered copies)
    - `ls research/_archive/*Cambodia*` returns at most 1 file
    - `ls research/_archive/*History*Hype*` returns at most 1 file
  </verify>
  <done>Research archive contains only unique files. No numbered copies or obvious duplicates.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Count files in transcripts/: `ls transcripts/*.srt transcripts/*.vtt 2>/dev/null | wc -l` (should be lower)
2. Count files in research/_archive/: `ls research/_archive/ | wc -l` (should be lower)
3. Search for any remaining obvious duplicates: `find . -name "*duplicate*" -o -name "*(1)*" -o -name "*-ALT-*"`
</verification>

<success_criteria>
- No files with "duplicate" in name exist
- No files with "(1)" or "(2)" numbering exist
- No ALT-VERSION files remain (either deleted or properly renamed)
- Each piece of content exists in exactly one location
</success_criteria>

<output>
After completion, create `.planning/phases/01-file-cleanup/01-02-SUMMARY.md`
</output>
