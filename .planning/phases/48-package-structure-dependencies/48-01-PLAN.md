---
phase: 48-package-structure-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/youtube_analytics/ (entire directory, renamed from youtube-analytics/)
  - tools/script_checkers/ (entire directory, renamed from script-checkers/)
  - tools/__init__.py
  - tools/youtube_analytics/__init__.py
  - tools/script_checkers/__init__.py
  - tools/script_checkers/tests/__init__.py
  - .claude/commands/analyze.md
  - .claude/commands/next.md
  - .claude/commands/patterns.md
  - .claude/commands/prep.md
  - .claude/commands/publish.md
  - .claude/commands/script.md
  - .claude/REFERENCE/STYLE-GUIDE.md
  - CLAUDE.md
autonomous: true
requirements:
  - PKG-01
  - PKG-03

must_haves:
  truths:
    - "`tools/youtube_analytics/` exists as a directory (hyphen gone)"
    - "`tools/script_checkers/` exists as a directory (hyphen gone)"
    - "`tools/__init__.py` exists (empty)"
    - "`tools/youtube_analytics/__init__.py` exists (empty)"
    - "`tools/script_checkers/__init__.py` exists (empty)"
    - "`tools/script_checkers/tests/__init__.py` exists (empty)"
    - "No file in the repo contains the string `youtube-analytics` as a path segment (except dead files _competitor_fetch.py, _csv_backfill.py)"
    - "No file in the repo contains the string `script-checkers` as a path segment"
  artifacts:
    - path: "tools/__init__.py"
      provides: "Marks tools/ as a Python package"
    - path: "tools/youtube_analytics/__init__.py"
      provides: "Marks youtube_analytics/ as a Python package"
    - path: "tools/script_checkers/__init__.py"
      provides: "Marks script_checkers/ as a Python package"
    - path: "tools/script_checkers/tests/__init__.py"
      provides: "Makes tests/ discoverable by pytest"
  key_links:
    - from: "tools/__init__.py"
      to: "tools/youtube_analytics/__init__.py"
      via: "directory hierarchy"
      pattern: "tools/youtube_analytics"
    - from: ".claude/commands/*.md"
      to: "tools/youtube_analytics/"
      via: "path references updated"
      pattern: "youtube_analytics"
---

<objective>
Rename hyphenated tool directories to Python-legal names and add the four missing __init__.py files, making all tool packages importable as `tools.youtube_analytics` and `tools.script_checkers`. Sweep every reference across the codebase in one atomic pass.

Purpose: Python cannot import modules from directories with hyphens. This is the foundation that Plan 02 (import rewrites) and Plan 03 (pyproject.toml) depend on. Must complete first.
Output: Two renamed directories, four new __init__.py files, zero remaining references to old hyphenated names.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-package-structure-dependencies/48-CONTEXT.md
@.planning/audits/48-package-structure.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories and create __init__.py files</name>
  <files>
    tools/youtube_analytics/ (directory, renamed from youtube-analytics/)
    tools/script_checkers/ (directory, renamed from script-checkers/)
    tools/__init__.py
    tools/youtube_analytics/__init__.py
    tools/script_checkers/__init__.py
    tools/script_checkers/tests/__init__.py
  </files>
  <action>
    Rename both hyphenated directories in a single pass using git mv so git tracks the renames:

    ```bash
    cd "G:/History vs Hype"
    git mv "tools/youtube-analytics" "tools/youtube_analytics"
    git mv "tools/script-checkers" "tools/script_checkers"
    ```

    Then create four empty __init__.py files:
    - `tools/__init__.py` — empty (marks tools/ as a package)
    - `tools/youtube_analytics/__init__.py` — empty
    - `tools/script_checkers/__init__.py` — empty
    - `tools/script_checkers/tests/__init__.py` — empty

    All four files are intentionally empty per user decision: "All __init__.py files are empty (just mark directories as packages). No re-exports."

    Verify the renames worked:
    ```bash
    ls tools/ | grep youtube   # should show youtube_analytics
    ls tools/ | grep script    # should show script_checkers
    ```
  </action>
  <verify>
    <automated>
      cd "G:/History vs Hype" && python -c "import sys; sys.path.insert(0, '.'); from tools.youtube_analytics import backfill; print('youtube_analytics importable')" 2>&1 | head -5
      cd "G:/History vs Hype" && python -c "import sys; sys.path.insert(0, '.'); from tools.script_checkers import cli; print('script_checkers importable')" 2>&1 | head -5
    </automated>
    <manual>Confirm `tools/youtube_analytics/` and `tools/script_checkers/` directories exist. Confirm `tools/__init__.py` exists and is empty.</manual>
  </verify>
  <done>Both directories renamed. Four __init__.py files created. Python can import `tools.youtube_analytics` and `tools.script_checkers` from repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep all references to old hyphenated names</name>
  <files>
    .claude/commands/analyze.md
    .claude/commands/next.md
    .claude/commands/patterns.md
    .claude/commands/prep.md
    .claude/commands/publish.md
    .claude/commands/script.md
    .claude/REFERENCE/STYLE-GUIDE.md
    CLAUDE.md
    tools/intel/competitor_tracker.py
    tools/intel/algo_scraper.py (comment only)
    tools/youtube_analytics/ (all internal .py files referencing old dir name)
    tools/script_checkers/ (all internal .py files referencing old dir name)
    tools/discovery/diagnostics.py
    tools/discovery/recommender.py
  </files>
  <action>
    Sweep every file in the repo for references to the old names `youtube-analytics` and `script-checkers` and update them.

    **Step 1 — Confirm what needs changing (grep first):**
    ```bash
    cd "G:/History vs Hype"
    grep -r "youtube-analytics\|script-checkers" --include="*.md" --include="*.py" --include="*.txt" -l | grep -v "__pycache__"
    ```

    **Step 2 — Update Python files:**

    Files containing `/ 'youtube-analytics'` path strings in sys.path hacks (these get converted in Plan 02 to proper imports, but the path string itself must change to `youtube_analytics` now):
    - `tools/discovery/diagnostics.py` line 35: change `'youtube-analytics'` → `'youtube_analytics'`
    - `tools/discovery/recommender.py` line 37: change `'youtube-analytics'` → `'youtube_analytics'`
    - `tools/intel/competitor_tracker.py` line 237: change `"youtube-analytics"` → `"youtube_analytics"`

    Comment updates (no functional change):
    - `tools/intel/algo_scraper.py`: update comment `tools/youtube-analytics` → `tools/youtube_analytics`
    - `tools/intel/competitor_tracker.py`: update docstring comments

    **Step 3 — Update command files** (`.claude/commands/`):
    In `analyze.md`, `next.md`, `patterns.md`, `prep.md`, `publish.md`, `script.md`:
    - Replace any path like `tools/youtube-analytics/` with `tools/youtube_analytics/`
    - Replace any path like `tools/script-checkers/` with `tools/script_checkers/`
    - Replace any invocation like `python tools/youtube-analytics/X.py` with `python -m tools.youtube_analytics.X`
    - Replace any invocation like `python tools/script-checkers/X.py` with `python -m tools.script_checkers.X`

    **Step 4 — Update STYLE-GUIDE.md and CLAUDE.md:**
    - `.claude/REFERENCE/STYLE-GUIDE.md`: update any path references
    - `CLAUDE.md`: update any path references

    **Step 5 — Handle dead files** (leave their sys.path lines as-is per CONTEXT.md):
    - `tools/youtube_analytics/_competitor_fetch.py` — skip (removed in Phase 49)
    - `tools/youtube_analytics/_csv_backfill.py` — skip (removed in Phase 49)

    **Step 6 — Final verification grep:**
    ```bash
    grep -r "youtube-analytics\|script-checkers" --include="*.md" --include="*.py" -l | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill"
    ```
    This should return zero results.
  </action>
  <verify>
    <automated>
      cd "G:/History vs Hype" && grep -r "youtube-analytics\|script-checkers" --include="*.py" --include="*.md" -l 2>/dev/null | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill" | wc -l
    </automated>
    <manual>Count should be 0 (or only the two dead files if grep finds them). All command files reference youtube_analytics with underscore.</manual>
  </verify>
  <done>Zero references to hyphenated names remain outside the two dead files. All command files, Python imports, comments, and docs use youtube_analytics / script_checkers with underscores.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls "G:/History vs Hype/tools/" | grep analytics` → shows `youtube_analytics` (with underscore)
2. `ls "G:/History vs Hype/tools/" | grep script` → shows `script_checkers` (with underscore)
3. `python -c "import sys; sys.path.insert(0, 'G:/History vs Hype'); from tools import youtube_analytics"` succeeds
4. Zero grep hits for `youtube-analytics` or `script-checkers` in *.py and *.md files (excluding dead files)
</verification>

<success_criteria>
- `tools/youtube_analytics/` directory exists with all original files intact
- `tools/script_checkers/` directory exists with all original files intact
- `tools/__init__.py`, `tools/youtube_analytics/__init__.py`, `tools/script_checkers/__init__.py`, `tools/script_checkers/tests/__init__.py` all exist and are empty
- Zero remaining references to `youtube-analytics` or `script-checkers` as path segments (excluding the two dead files being removed in Phase 49)
</success_criteria>

<output>
After completion, create `.planning/phases/48-package-structure-dependencies/48-01-SUMMARY.md`
</output>
