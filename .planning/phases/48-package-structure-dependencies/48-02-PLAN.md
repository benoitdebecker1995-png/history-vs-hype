---
phase: 48-package-structure-dependencies
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - tools/youtube_analytics/analyze.py
  - tools/youtube_analytics/backfill.py
  - tools/youtube_analytics/benchmarks.py
  - tools/youtube_analytics/feedback.py
  - tools/youtube_analytics/feedback_parser.py
  - tools/youtube_analytics/feedback_queries.py
  - tools/youtube_analytics/pattern_extractor.py
  - tools/youtube_analytics/pattern_synthesizer_v2.py
  - tools/youtube_analytics/performance.py
  - tools/youtube_analytics/performance_report.py
  - tools/youtube_analytics/playbook_synthesizer.py
  - tools/youtube_analytics/retention_mapper.py
  - tools/youtube_analytics/retention_scorer.py
  - tools/youtube_analytics/topic_strategy.py
  - tools/youtube_analytics/variants.py
  - tools/discovery/diagnostics.py
  - tools/discovery/recommender.py
  - tools/discovery/backfill_gaps.py
  - tools/discovery/orchestrator.py
  - tools/document_discovery/cli.py
  - tools/production/parser.py
  - tools/translation/cli.py
  - tools/translation/smoke_test.py
  - tools/translation/structure_detector.py
  - tools/intel/competitor_tracker.py
  - tools/script_checkers/checkers/pacing.py
  - tools/script_checkers/tests/test_pacing.py
autonomous: true
requirements:
  - PKG-02

must_haves:
  truths:
    - "No sys.path.insert or sys.path.append calls exist in any tools/ file except the two dead files"
    - "`from tools.discovery import orchestrator` runs from repo root without sys.path manipulation"
    - "`from tools.intel import kb_store` runs from repo root without sys.path manipulation"
    - "discovery/diagnostics.py and discovery/recommender.py use lazy (function-level) imports for youtube_analytics modules"
    - "Self-imports (files that added their own directory to sys.path) now use relative imports"
  artifacts:
    - path: "tools/youtube_analytics/backfill.py"
      provides: "Clean imports: relative for same-package, absolute tools.* for cross-package"
    - path: "tools/discovery/diagnostics.py"
      provides: "Lazy import of channel_averages at function level, no sys.path hack"
    - path: "tools/discovery/recommender.py"
      provides: "Lazy imports of pattern_extractor at function level, no sys.path hack"
    - path: "tools/intel/competitor_tracker.py"
      provides: "Uses from tools.youtube_analytics.auth import ... inside function"
  key_links:
    - from: "tools/youtube_analytics/backfill.py"
      to: "tools/discovery/database.py"
      via: "from tools.discovery.database import KeywordDB"
      pattern: "from tools\\.discovery"
    - from: "tools/discovery/diagnostics.py"
      to: "tools/youtube_analytics/channel_averages.py"
      via: "lazy import inside function body"
      pattern: "def.*:\\s+from tools\\.youtube_analytics"
    - from: "tools/script_checkers/checkers/pacing.py"
      to: "tools/discovery/database.py"
      via: "from tools.discovery.database import KeywordDB"
      pattern: "from tools\\.discovery"
---

<objective>
Replace all 37 sys.path.insert/append hacks with proper absolute and relative imports. After Plan 01 established the package structure, this plan wires the actual imports so tools can find each other through the Python package hierarchy instead of runtime path manipulation.

Purpose: Eliminates the primary technical debt of the codebase — fragile runtime path manipulation that breaks as soon as working directory changes. Proper imports work from any working directory once the package is installed.
Output: 27 files updated with clean imports. Zero sys.path hacks remaining (except dead files being removed in Phase 49).
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-package-structure-dependencies/48-CONTEXT.md
@.planning/audits/48-package-structure.md
@.planning/phases/48-package-structure-dependencies/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sys.path hacks in youtube_analytics/ with proper imports</name>
  <files>
    tools/youtube_analytics/analyze.py
    tools/youtube_analytics/backfill.py
    tools/youtube_analytics/benchmarks.py
    tools/youtube_analytics/feedback.py
    tools/youtube_analytics/feedback_parser.py
    tools/youtube_analytics/feedback_queries.py
    tools/youtube_analytics/pattern_extractor.py
    tools/youtube_analytics/pattern_synthesizer_v2.py
    tools/youtube_analytics/performance.py
    tools/youtube_analytics/performance_report.py
    tools/youtube_analytics/playbook_synthesizer.py
    tools/youtube_analytics/retention_mapper.py
    tools/youtube_analytics/retention_scorer.py
    tools/youtube_analytics/topic_strategy.py
    tools/youtube_analytics/variants.py
  </files>
  <action>
    For each file in `tools/youtube_analytics/`, remove sys.path manipulation and replace with proper imports. Pattern rules per CONTEXT.md decisions:

    **Rule A — Cross-package imports** (youtube_analytics → discovery or production):
    Remove the `sys.path.insert(...)` block and replace with:
    - `from tools.discovery.database import KeywordDB` (or whichever specific symbol)
    - `from tools.discovery import database` (if importing the module)
    - `from tools.production import parser` (for production/ imports)
    - `from tools.production.parser import ScriptParser` (specific symbol)

    **Rule B — Self-imports** (file adding its own package to sys.path):
    - `backfill.py` adding `youtube_analytics/` to path → remove the sys.path line, use relative import: `from .metrics import get_video_metrics` (or whichever symbol it was importing)
    - `pattern_synthesizer_v2.py` adding self → same treatment with relative import

    **Rule C — Keep try/except ImportError wrappers** around optional deps (feedparser, imagehash, etc.) — only remove the sys.path line inside the try block, keep the try/except/feature flag pattern intact per CONTEXT.md.

    **File-by-file changes** (from audit table):

    `analyze.py` (lines ~55, ~87):
    - Remove sys.path for discovery/ → `from tools.discovery import database` (or specific symbols)
    - Remove sys.path for production/ → `from tools.production import parser`

    `backfill.py` (lines ~46, ~55):
    - Remove sys.path for discovery/ → `from tools.discovery.database import KeywordDB`
    - Remove sys.path for self → replace `from database import X` with `from .database import X` if such exists, else remove self-path and use relative

    `benchmarks.py` (line ~27):
    - Remove sys.path for discovery/ → `from tools.discovery.database import KeywordDB`

    `feedback.py` (lines ~27-28):
    - Remove sys.path for both youtube_analytics/ (self) and discovery/
    - Self-path: convert `from database import X` → `from .database import X` (relative)
    - discovery/ path: `from tools.discovery.database import KeywordDB`

    `feedback_queries.py`, `feedback_parser.py`, `pattern_extractor.py`, `performance.py`, `performance_report.py`, `playbook_synthesizer.py`, `topic_strategy.py`, `variants.py` (all import from discovery/):
    - Pattern: remove sys.path.insert for discovery/, replace `from database import X` with `from tools.discovery.database import X`

    `retention_mapper.py` (imports production/):
    - Remove sys.path for production/ → `from tools.production.parser import ScriptParser` (or the actual symbol used)

    `retention_scorer.py` (lines ~41, ~651 — imports discovery/ and production/):
    - Remove both sys.path lines, replace with `from tools.discovery.database import ...` and `from tools.production.parser import ...`

    `pattern_synthesizer_v2.py` (self-import):
    - Remove sys.path for self, convert bare `from pattern_extractor import X` → `from .pattern_extractor import X`

    **Verification after each file:** Read the modified file and confirm no `sys.path` remains (except dead files).
  </action>
  <verify>
    <automated>
      cd "G:/History vs Hype" && grep -r "sys.path" tools/youtube_analytics/ --include="*.py" | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill"
    </automated>
    <manual>Output should be empty. Then test: `python -c "import sys; sys.path.insert(0, 'G:/History vs Hype'); from tools.youtube_analytics import backfill"` should succeed (or fail only on missing API credentials, not import errors).</manual>
  </verify>
  <done>All youtube_analytics/ files use `from tools.discovery.*` and `from tools.production.*` for cross-package imports. Self-imports use relative imports. Zero sys.path hacks remain in this directory (excluding dead files).</done>
</task>

<task type="auto">
  <name>Task 2: Replace sys.path hacks in discovery/, translation/, document_discovery/, production/, intel/, script_checkers/</name>
  <files>
    tools/discovery/diagnostics.py
    tools/discovery/recommender.py
    tools/discovery/backfill_gaps.py
    tools/discovery/orchestrator.py
    tools/document_discovery/cli.py
    tools/production/parser.py
    tools/translation/cli.py
    tools/translation/smoke_test.py
    tools/translation/structure_detector.py
    tools/intel/competitor_tracker.py
    tools/script_checkers/checkers/pacing.py
    tools/script_checkers/tests/test_pacing.py
  </files>
  <action>
    Fix remaining sys.path hacks across all other tool packages.

    **discovery/backfill_gaps.py** (self-import):
    - Remove sys.path for discovery/ (self), convert bare `from database import X` → `from .database import X`

    **discovery/orchestrator.py** (self-import):
    - Remove sys.path for discovery/ (self), convert internal bare imports → relative: `from .database import X`, `from .demand import X`, etc.

    **discovery/diagnostics.py** (cross: imports youtube_analytics) — USE LAZY IMPORT per CONTEXT.md:
    The current pattern is:
    ```python
    try:
        sys.path.insert(0, str(Path(__file__).parent.parent / 'youtube_analytics'))
        from channel_averages import get_channel_averages
        from metrics import get_video_metrics
    except ImportError:
        get_channel_averages = None
        get_video_metrics = None
    ```
    Replace with a lazy import function — move the import inside the function(s) that actually use `get_channel_averages` / `get_video_metrics`:
    ```python
    def _get_channel_averages():
        try:
            from tools.youtube_analytics.channel_averages import get_channel_averages
            return get_channel_averages
        except ImportError:
            return None
    ```
    Or inject as a callable where used. The key: NO module-level sys.path hack, import happens at call time. Keep the graceful None fallback.

    **discovery/recommender.py** (cross: imports youtube_analytics and tools root) — USE LAZY IMPORT per CONTEXT.md:
    Current module-level sys.path hacks at lines 37 and 53. Replace:
    - `from database import KeywordDB` → `from .database import KeywordDB` (self, discovery package)
    - `from pattern_extractor import extract_winning_patterns` → lazy import inside function body:
      ```python
      def _load_pattern_extractor():
          try:
              from tools.youtube_analytics.pattern_extractor import extract_winning_patterns
              return extract_winning_patterns
          except ImportError:
              return None
      ```
    - `from tools.intel.topic_scorer import score_topic` — this is already an absolute import, just remove the sys.path.insert before it, the import itself is already correct.

    **translation/cli.py** (self-import):
    - Remove sys.path for translation/ (self), convert internal imports → relative: `from .translator import X`

    **translation/smoke_test.py** (self-import):
    - Remove sys.path for translation/ (self), convert → relative imports

    **translation/structure_detector.py** (cross: imports document_discovery/):
    - Remove sys.path for document_discovery/ → `from tools.document_discovery.X import Y`

    **document_discovery/cli.py** (self-import):
    - Remove sys.path for document_discovery/ (self) → relative imports

    **production/parser.py** (line ~391, adds project root):
    - Remove the sys.path.insert for project root. After pyproject.toml + `pip install -e .` (Plan 03), the project root is in PYTHONPATH automatically. For now, remove the hack — if tools/production/parser.py needs to import something from tools.*, use `from tools.X import Y` directly.

    **intel/competitor_tracker.py** (lines ~237-239, lazy runtime auth import):
    - This is already inside a function (lazy import pattern). Convert the path hack to:
      ```python
      from tools.youtube_analytics.auth import get_authenticated_service
      ```
      inside the function where it's used. Remove the `_auth_dir` path manipulation block.

    **script_checkers/checkers/pacing.py** (cross: imports discovery/):
    - Remove sys.path for tools/ root → `from tools.discovery.database import KeywordDB`

    **script_checkers/tests/test_pacing.py** (cross: imports script_checkers/):
    - Remove sys.path hack → `from tools.script_checkers.checkers import pacing` (or specific class)

    **Final verification:**
    ```bash
    grep -r "sys.path" "G:/History vs Hype/tools/" --include="*.py" | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill"
    ```
    Should return zero results.
  </action>
  <verify>
    <automated>
      cd "G:/History vs Hype" && grep -rn "sys.path.insert\|sys.path.append" tools/ --include="*.py" 2>/dev/null | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill"
    </automated>
    <manual>Zero results expected. Then run: `python -c "import sys; sys.path.insert(0, 'G:/History vs Hype'); from tools.discovery import orchestrator; print('ok')"` — should print 'ok' with no ImportError.</manual>
  </verify>
  <done>All sys.path hacks removed from discovery/, translation/, document_discovery/, production/, intel/, and script_checkers/. discovery/diagnostics.py and discovery/recommender.py use lazy function-level imports for the circular youtube_analytics dependency. Zero sys.path.insert/append calls remain in live code.</done>
</task>

</tasks>

<verification>
End-to-end smoke tests from repo root (no sys.path manipulation):
```bash
cd "G:/History vs Hype"
python -c "from tools.discovery import orchestrator; print('discovery.orchestrator OK')"
python -c "from tools.intel import kb_store; print('intel.kb_store OK')"
python -c "from tools.youtube_analytics import backfill; print('youtube_analytics.backfill OK')"
python -c "from tools.script_checkers.checkers import pacing; print('script_checkers.pacing OK')"
```
All four should print their OK message (or fail only on missing external dependencies like google-api, not on import path errors).

Final grep confirms no sys.path hacks remain:
```bash
grep -r "sys.path.insert\|sys.path.append" tools/ --include="*.py" | grep -v "__pycache__" | grep -v "_competitor_fetch\|_csv_backfill"
```
</verification>

<success_criteria>
- Zero sys.path.insert/append calls in any live tool file (excluding the two dead files)
- `from tools.discovery import orchestrator` works from repo root
- `from tools.intel import kb_store` works from repo root
- discovery/diagnostics.py and recommender.py use lazy imports (inside function bodies) for youtube_analytics modules
- All same-package imports use relative syntax (`from .module import X`)
- All cross-package imports use absolute `tools.*` syntax
</success_criteria>

<output>
After completion, create `.planning/phases/48-package-structure-dependencies/48-02-SUMMARY.md`
</output>
