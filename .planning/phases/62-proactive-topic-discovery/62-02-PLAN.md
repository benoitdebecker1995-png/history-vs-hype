---
phase: 62-proactive-topic-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - .claude/commands/discover.md
  - tools/discovery/discovery_scanner.py
autonomous: false
requirements:
  - DISC-04

must_haves:
  truths:
    - "User can run `/discover --scan` and get a ranked discovery feed"
    - "discover.md documents the --scan flag with usage examples"
    - "DISCOVERY-FEED.md output is readable and actionable"
  artifacts:
    - path: ".claude/commands/discover.md"
      provides: "--scan flag documentation with usage, example output, and integration notes"
      contains: "--scan"
    - path: "channel-data/DISCOVERY-FEED.md"
      provides: "Generated discovery feed report (created during verification)"
  key_links:
    - from: ".claude/commands/discover.md"
      to: "tools/discovery/discovery_scanner.py"
      via: "--scan flag references python -m tools.discovery.discovery_scanner"
      pattern: "discovery_scanner"
---

<objective>
Wire `/discover --scan` into the existing discover.md slash command and verify the end-to-end pipeline produces a useful DISCOVERY-FEED.md report.

Purpose: The scanner module exists from Plan 01 but needs to be accessible via the standard command interface and verified by the user.
Output: Updated discover.md with `--scan` documentation + human-verified report quality
</objective>

<execution_context>
@C:/Users/Benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-proactive-topic-discovery/62-CONTEXT.md
@.planning/phases/62-proactive-topic-discovery/62-01-SUMMARY.md

<interfaces>
From tools/discovery/discovery_scanner.py (created in Plan 01):
```python
class DiscoveryScanner:
    def scan(self, limit: int = 10) -> dict:
        """Run full discovery pipeline. Returns {'opportunities': [...], 'feed_path': str, 'signal_quality': dict}"""

CHANNEL_SEEDS: list[str]  # ~15 seed keywords from channel DNA categories
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --scan flag to discover.md + wire CLI integration</name>
  <files>.claude/commands/discover.md, tools/discovery/discovery_scanner.py</files>
  <action>
    1. Read .claude/commands/discover.md (existing file — 827 lines). Add a new `## PROACTIVE DISCOVERY SCAN (--scan)` section AFTER the flags table and BEFORE the FULL KEYWORD RESEARCH section. Add `--scan` to the flags table.

    The new section should document:
    - Purpose: Proactive scanning for high-demand topics matching channel strengths
    - Usage: `/discover --scan` (runs full pipeline), `/discover --scan --limit 20` (more results)
    - What it does: Runs autocomplete mining (15 seeds), competitor gap detection (10 channels), Google Trends pulse, scores with extended Belize formula, deduplicates, writes DISCOVERY-FEED.md
    - Execution command: `python -m tools.discovery.discovery_scanner [--limit N] [--json] [--verbose]`
    - Example output: Brief sample of DISCOVERY-FEED.md table
    - Integration: Separate from TOPIC-PIPELINE.md. User promotes opportunities manually. `/next` reads DISCOVERY-FEED.md if available.
    - Runtime: ~90-120 seconds (autocomplete rate limiting)

    2. In discovery_scanner.py, verify the __main__ block handles --limit correctly and outputs a brief summary to stdout after writing the feed file (e.g., "Discovery feed written to channel-data/DISCOVERY-FEED.md with 10 opportunities. Top: [topic] (score: 82)").

    Add `| --scan | Proactive topic discovery scan | /discover --scan |` to the flags table in discover.md.
  </action>
  <verify>
    <automated>cd "D:\History vs Hype" && python -m tools.discovery.discovery_scanner --help && grep -c "scan" .claude/commands/discover.md</automated>
  </verify>
  <done>discover.md contains --scan section with usage documentation. `python -m tools.discovery.discovery_scanner --help` shows --limit, --json, --verbose flags.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify discovery feed output quality</name>
  <files>channel-data/DISCOVERY-FEED.md</files>
  <action>
    Run the full discovery scan and verify the output report is useful and accurate.

    What was built: Complete proactive topic discovery pipeline — DiscoveryScanner orchestrates autocomplete mining, competitor gap detection, and Google Trends pulse into a ranked DISCOVERY-FEED.md report scored by the extended Belize formula (demand + map angle + news hook + no competitor + conversion potential).

    Steps to verify:
    1. Run `/discover --scan` (or `python -m tools.discovery.discovery_scanner`)
    2. Wait ~90-120 seconds for autocomplete rate limiting
    3. Check channel-data/DISCOVERY-FEED.md was created
    4. Verify report contains: header with scan timestamp, top 10 table, per-opportunity details, signal quality table, channel suggestions
    5. Verify dedup: no topics already in _IN_PRODUCTION/ should appear
    6. Verify scoring: ideological topics should rank higher than pure territorial
    7. Run tests: `python -m pytest tests/test_discovery_scanner.py -x -q`
  </action>
  <verify>
    <automated>cd "D:\History vs Hype" && python -m pytest tests/test_discovery_scanner.py -x -q</automated>
  </verify>
  <done>User confirms DISCOVERY-FEED.md is readable, actionable, and correctly ranked. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `/discover --scan` runs without errors and produces channel-data/DISCOVERY-FEED.md
- discover.md contains --scan documentation
- All tests still pass: `python -m pytest tests/test_discovery_scanner.py -x -q`
- Full test suite clean: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- User can run `/discover --scan` and get actionable topic recommendations
- DISCOVERY-FEED.md is readable, ranked, and deduplicated
- Report includes signal quality transparency (which sources worked, which didn't)
</success_criteria>

<output>
After completion, create `.planning/phases/62-proactive-topic-discovery/62-02-SUMMARY.md`
</output>
