---
phase: 64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption/64-INSTALL-LOG.md
autonomous: false
requirements:
  - EVAL-ADOPT-01
  - EVAL-ADOPT-02

must_haves:
  truths:
    - "Firecrawl MCP is installed and can scrape a JS-rendered page to clean markdown"
    - "Context7 MCP is installed and can resolve a library's live documentation"
    - "Both tools appear in `claude mcp list` output"
  artifacts:
    - path: ".planning/phases/64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption/64-INSTALL-LOG.md"
      provides: "Installation commands, outputs, and smoke test results for ADOPT-tier tools"
      contains: "Firecrawl"
  key_links:
    - from: "claude mcp list"
      to: "firecrawl"
      via: "MCP registration"
      pattern: "firecrawl"
    - from: "claude mcp list"
      to: "context7"
      via: "MCP registration"
      pattern: "context7"
---

<objective>
Install and verify the two ADOPT-verdict MCP servers (Firecrawl and Context7) on this Windows machine.

Purpose: These two tools have no overlap with existing tooling and provide immediate value -- Firecrawl for JS-rendered primary source scraping during research, Context7 for live library documentation during development.

Output: Both MCP servers registered and functional, installation log documenting commands and results.
</objective>

<execution_context>
@C:/Users/Benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption/64-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Firecrawl MCP and Context7 MCP</name>
  <files>.planning/phases/64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption/64-INSTALL-LOG.md</files>
  <action>
Install both ADOPT-verdict MCP servers:

**Firecrawl MCP:**
1. User must first get a free API key from https://firecrawl.dev (500 one-time credits). Check if FIRECRAWL_API_KEY is already set in environment. If not, create a checkpoint:human-action pause.
2. Install: `claude mcp add firecrawl -e FIRECRAWL_API_KEY=$KEY -- npx -y firecrawl-mcp`
3. If MSYS2 path issues occur, use Windows workaround: `claude mcp add firecrawl -- cmd /c "set FIRECRAWL_API_KEY=$KEY && npx -y firecrawl-mcp"`
4. Verify: `claude mcp list` shows firecrawl entry
5. Smoke test: Use the firecrawl scrape tool to fetch https://en.wikipedia.org/wiki/Partition_of_India and confirm markdown output is returned

**Context7 MCP:**
1. Install: `claude mcp add --scope user context7 -- npx -y @upstash/context7-mcp`
2. No API key needed for basic usage (rate-limited without key, but sufficient for evaluation)
3. Verify: `claude mcp list` shows context7 entry
4. Smoke test: Use Context7 to resolve library documentation for "sqlite3" or "pytest" and confirm docs are returned

**Install Log:**
Create 64-INSTALL-LOG.md documenting:
- Exact commands run (copy-paste)
- Output/errors from each step
- Smoke test results (pass/fail with details)
- Any Windows-specific workarounds needed
- Final `claude mcp list` output showing both servers
  </action>
  <verify>
    <automated>claude mcp list 2>&1 | grep -i "firecrawl\|context7"</automated>
  </verify>
  <done>Both Firecrawl and Context7 appear in `claude mcp list`. Smoke tests documented in 64-INSTALL-LOG.md with pass/fail for each tool.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ADOPT-tier MCP servers work in live session</name>
  <action>Human verifies that both installed MCP servers function correctly in a live Claude Code session.</action>
  <what-built>Installed Firecrawl MCP (JS-rendered web scraping) and Context7 MCP (live library docs) as Claude Code MCP servers. Both should now be available as tools in Claude Code sessions.</what-built>
  <how-to-verify>
    1. Run `claude mcp list` and confirm both firecrawl and context7 appear
    2. Start a new Claude Code session and try: "Use firecrawl to scrape https://en.wikipedia.org/wiki/Treaty_of_Tordesillas" -- should return clean markdown
    3. Try: "Use context7 to look up the pytest assert documentation" -- should return current docs
    4. Check that no background processes are consuming unexpected CPU/RAM (Task Manager)
  </how-to-verify>
  <verify>User confirms both tools work in live session</verify>
  <done>User approved both Firecrawl and Context7 as functional</done>
  <resume-signal>Type "approved" if both tools work, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `claude mcp list` shows both firecrawl and context7
- 64-INSTALL-LOG.md exists with documented smoke test results
- No unexpected background processes spawned
</verification>

<success_criteria>
Firecrawl MCP scrapes a real URL to markdown. Context7 MCP resolves live library docs. Both registered in Claude Code. Installation fully documented.
</success_criteria>

<output>
After completion, create `.planning/phases/64-evaluate-youtube-mcp-servers-and-packaging-plugins-for-adoption/64-01-SUMMARY.md`
</output>
