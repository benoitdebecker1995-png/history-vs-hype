---
phase: 36-retention-science
plan: 03
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - .claude/agents/script-writer-v2.md
  - .claude/commands/script.md
  - .claude/commands/analyze.md
  - tools/youtube-analytics/analyze.py
autonomous: true

must_haves:
  truths:
    - "Script-writer-v2 references Part 9 retention rules during script generation"
    - "Retention warnings flag risky script sections during /script generation"
    - "Part 9 playbook updates automatically after /analyze --script completes"
    - "User sees retention score warnings in /script output before filming"
  artifacts:
    - path: ".claude/agents/script-writer-v2.md"
      provides: "Rule 15 referencing Part 9 retention playbook during generation"
      contains: "RULE 15"
    - path: ".claude/commands/script.md"
      provides: "Retention scoring integration in /script workflow"
      contains: "retention_scorer"
    - path: ".claude/commands/analyze.md"
      provides: "Playbook update documentation"
      contains: "playbook_synthesizer"
    - path: "tools/youtube-analytics/analyze.py"
      provides: "Auto-update playbook trigger after analysis"
      contains: "playbook_synthesizer"
  key_links:
    - from: ".claude/agents/script-writer-v2.md"
      to: ".claude/REFERENCE/STYLE-GUIDE.md"
      via: "Rule 15 instructs agent to read Part 9 before writing"
      pattern: "Part 9"
    - from: ".claude/commands/script.md"
      to: "tools/youtube-analytics/retention_scorer.py"
      via: "Score sections after generation, surface warnings"
      pattern: "retention_scorer"
    - from: "tools/youtube-analytics/analyze.py"
      to: "tools/youtube-analytics/playbook_synthesizer.py"
      via: "Auto-update Part 9 after analysis completes"
      pattern: "playbook_synthesizer"
---

<objective>
Wire playbook_synthesizer.py and retention_scorer.py into the script-writer-v2 agent, /script command, and /analyze command -- completing the feedback loop where published video data improves future script generation.

Purpose: Closes the loop between RET-01 (agent reads Part 9), RET-02/RET-03 (warnings surface during generation), and auto-update (Part 9 stays current).
Output: Updated agent, command docs, and analyze.py integration
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-retention-science/36-01-SUMMARY.md
@.planning/phases/36-retention-science/36-02-SUMMARY.md
@.claude/agents/script-writer-v2.md
@.claude/commands/script.md
@.claude/commands/analyze.md
@tools/youtube-analytics/analyze.py
@tools/youtube-analytics/playbook_synthesizer.py
@tools/youtube-analytics/retention_scorer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rule 15 to script-writer-v2.md for Part 9 retention awareness</name>
  <files>.claude/agents/script-writer-v2.md</files>
  <action>
Read .claude/agents/script-writer-v2.md. Add RULE 15 after RULE 14 (Voice Pattern Application), before the REASONING FRAMEWORK section.

**RULE 15: RETENTION PLAYBOOK APPLICATION (Added YYYY-MM-DD)**

Content for Rule 15:

```
### RULE 15: RETENTION PLAYBOOK APPLICATION

**Before writing, read STYLE-GUIDE.md Part 9 for data-driven retention rules.**

Part 9 is auto-generated from published video retention data. It contains:
- Opening retention rules per topic type (Part 9.1)
- Section pacing guidelines with length thresholds (Part 9.2)
- Modern relevance proximity rules (Part 9.3)
- Voice pattern effectiveness ranking (Part 9.4)
- Topic-type baselines (Part 9.5)

**Application during writing:**

A. **Before starting:** Check Part 9.5 for topic-type baseline. Note avg section length and confidence level.

B. **Each section:** Keep section word count within topic-type avg + 1 std dev (from Part 9.2). If section runs long, add pattern interrupt or split.

C. **Modern relevance:** Ensure no gap exceeds the topic-type threshold from Part 9.3. If writing a long historical sequence, insert "which is why today..." bridge before exceeding threshold.

D. **Evidence density:** Maintain at least 1 evidence marker per 200 words (quote, "according to", page reference). Part 9.4 shows which evidence patterns correlate with highest retention.

E. **After completing draft:** Note which Part 9 rules were applied in VOICE PATTERNS APPLIED section:
```
## RETENTION RULES APPLIED
- Topic baseline: [topic_type] (Part 9.5)
- Max section length: [X words] (Part 9.2)
- Modern relevance gap limit: [X words] (Part 9.3)
- Sections flagged: [any sections that exceeded thresholds]
```

**If Part 9 shows "insufficient data":** Fall back to default thresholds (150 words/section, 100-word modern relevance gap) and note "Part 9 insufficient data - using defaults" in metadata.

**See:** `.claude/REFERENCE/STYLE-GUIDE.md` Part 9 for current retention playbook.
```

Also update the Tier 1 MANDATORY reference table at the top of the file to mention Part 9:
- Add note to the STYLE-GUIDE.md row: "Parts 1-7 style + Part 6 voice patterns + **Part 9 retention playbook**"
  </action>
  <verify>
Grep script-writer-v2.md for "RULE 15" -- must exist.
Grep script-writer-v2.md for "Part 9" -- must appear in Rule 15 and reference table.
Verify RULE 14 still exists and is unchanged.
Verify REASONING FRAMEWORK section still follows after Rule 15.
  </verify>
  <done>
script-writer-v2.md contains Rule 15: RETENTION PLAYBOOK APPLICATION that instructs the agent to read Part 9 before writing, apply topic baselines during section writing, and note which retention rules were followed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /script and /analyze commands with retention scoring and playbook update</name>
  <files>.claude/commands/script.md, .claude/commands/analyze.md, tools/youtube-analytics/analyze.py</files>
  <action>
**A. Update .claude/commands/script.md:**

Read the existing script.md. Find the "PRE-SCRIPT INTELLIGENCE" section (added in Phase 35-03). Add a new subsection for retention scoring AFTER the pre-script intelligence section:

```
### RETENTION SCORING (Post-Generation)

After script generation is complete, run retention scoring on the output:

1. Parse the generated script with ScriptParser
2. Import and call `score_all_sections(sections, topic_type)` from retention_scorer.py
3. Call `format_retention_warnings(scored_sections)` to get formatted warnings
4. Display warnings to user BEFORE finalizing script

**Output format:**
```
## RETENTION RISK ASSESSMENT

| Section | Risk | Score | Top Warning |
|---------|------|-------|-------------|
| Introduction | LOW | 0.82 | - |
| The Treaty of 1859 | HIGH | 0.38 | Section 280 words exceeds territorial avg 150 |
| Modern Consequences | LOW | 0.91 | - |

**HIGH RISK sections should be revised before filming.**
```

**Implementation for Claude:**
```python
import sys
sys.path.insert(0, 'tools/youtube-analytics')
try:
    from retention_scorer import score_all_sections, format_retention_warnings
    SCORER_AVAILABLE = True
except ImportError:
    SCORER_AVAILABLE = False

# After script generation:
if SCORER_AVAILABLE:
    from tools.production.parser import ScriptParser
    parser = ScriptParser()
    sections = parser.parse_file(script_path)
    scored = score_all_sections(sections, topic_type)
    warnings = format_retention_warnings(scored)
    # Display warnings to user
```

If retention_scorer not available, skip silently (graceful degradation).
```

Also update the "Workflow Steps" subsection to add Step 4: "Run retention scoring on completed script and display risk assessment."

**B. Update .claude/commands/analyze.md:**

Read the existing analyze.md. Add documentation for the playbook auto-update feature:

After the "SECTION-LEVEL RETENTION DIAGNOSTICS" section, add:

```
### PLAYBOOK UPDATE (Auto-Update Part 9)

After each video analysis with the --script flag, the retention playbook (STYLE-GUIDE.md Part 9) is automatically updated:

```bash
# Manual update (standalone)
python tools/youtube-analytics/playbook_synthesizer.py --update
```

This re-synthesizes Part 9 from all available retention data, incorporating patterns from the newly analyzed video.

**Automatic trigger:** analyze.py automatically updates Part 9 after `/analyze VIDEO_ID --script PATH` completes section diagnostics. No separate flag needed.
```

**C. Update tools/youtube-analytics/analyze.py:**

Read the existing analyze.py. Add playbook update integration:

1. Add import at the top (with try/except pattern matching DIAGNOSTICS_AVAILABLE):
```python
try:
    from playbook_synthesizer import synthesize_part9, write_part9_to_style_guide
    PLAYBOOK_AVAILABLE = True
except ImportError:
    PLAYBOOK_AVAILABLE = False
```

2. In the main CLI flow (find `if __name__ == '__main__'`), after the analysis is complete and saved:
   - If PLAYBOOK_AVAILABLE and section diagnostics were generated (--script flag was used):
     - Print: "Updating retention playbook (STYLE-GUIDE.md Part 9)..."
     - Call: `result = write_part9_to_style_guide(synthesize_part9())`
     - Print: "Part 9 updated." or error message
   - If PLAYBOOK_AVAILABLE but no --script flag: skip silently

This ensures Part 9 auto-updates after each `/analyze VIDEO_ID --script PATH` run, satisfying the "updates automatically as new videos publish" requirement.
  </action>
  <verify>
Grep script.md for "RETENTION SCORING" and "retention_scorer" -- both must exist.
Grep analyze.md for "PLAYBOOK UPDATE" and "playbook_synthesizer" -- both must exist.
Grep analyze.py for "PLAYBOOK_AVAILABLE" and "playbook_synthesizer" -- both must exist.
Run: `python tools/youtube-analytics/analyze.py --help` -- must not crash (playbook import is optional).
  </verify>
  <done>
- script.md documents retention scoring as Step 4 in workflow with implementation code for Claude
- analyze.md documents playbook update command and automatic trigger after --script
- analyze.py imports playbook_synthesizer with feature flag and triggers Part 9 update after section diagnostics
- All changes are backward-compatible (feature flags, optional imports, graceful degradation)
  </done>
</task>

</tasks>

<verification>
1. script-writer-v2.md has Rule 15 referencing Part 9
2. /script command documents retention scoring post-generation
3. /analyze command documents playbook update
4. analyze.py triggers playbook update after --script analysis
5. All existing functionality unchanged (no breaking changes)
6. Feature flags ensure graceful degradation if modules not available
</verification>

<success_criteria>
- Script-writer-v2 agent reads and applies Part 9 retention rules (Rule 15)
- Retention warnings surface during /script generation workflow
- Part 9 auto-updates when /analyze runs with --script flag
- All integration uses feature flags for backward compatibility
- Complete feedback loop: analyze video -> update playbook -> write better scripts -> analyze next video
</success_criteria>

<output>
After completion, create `.planning/phases/36-retention-science/36-03-SUMMARY.md`
</output>
