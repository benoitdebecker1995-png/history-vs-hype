---
phase: 72-prep-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/prep.md
autonomous: true
requirements:
  - FACT-01
  - FACT-02

must_haves:
  truths:
    - "Running /prep on a project without 03-FACT-CHECK-VERIFICATION.md blocks with a clear message"
    - "Running /prep on a project with a non-APPROVED verdict blocks showing the verdict and outstanding items"
    - "Running /prep on a project with a placeholder file (no verdict line) blocks with 'no verdict' message"
    - "Running /prep on an APPROVED project proceeds normally"
    - "APPROVED with required fixes passes but shows a warning with fix count"
    - "Gate applies to all /prep modes: --edit-guide, --assets, --full, --split-screen, and interactive"
  artifacts:
    - path: ".claude/commands/prep.md"
      provides: "Fact-Check Verification Gate section"
      contains: "FACT-CHECK VERIFICATION GATE"
  key_links:
    - from: ".claude/commands/prep.md"
      to: "03-FACT-CHECK-VERIFICATION.md"
      via: "Glob lookup and verdict line scanning"
      pattern: "Verdict.*APPROVED"
---

<objective>
Add a Fact-Check Verification Gate to `/prep` that blocks filming preparation when `03-FACT-CHECK-VERIFICATION.md` is missing or its verdict is not APPROVED. Display verdict and outstanding revision items when blocking.

Purpose: Prevent users from entering filming prep on videos that haven't passed the fact-check quality gate. This closes a silent-failure gap where unverified scripts could reach filming.

Output: Updated `.claude/commands/prep.md` with mandatory gate section.
</objective>

<execution_context>
@C:/Users/Benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-prep-gate/72-CONTEXT.md
@.planning/phases/71-script-entry-gates/71-01-SUMMARY.md

@.claude/commands/prep.md
@.claude/commands/script.md (lines 147-206 — Research Verification Gate pattern to replicate)
</context>

<interfaces>
<!-- Phase 71 established the gate pattern in script.md lines 147-206. Key structure: -->

Gate section format:
```
## [Gate Name] (MANDATORY - Runs Before [X])

**Applies to:** [modes]

**Step 1:** Locate file via Glob
**Step 2:** Parse/scan content
**Step 3:** Apply gate logic — BLOCK or PASS with bordered message
```

Block message format:
```
--- GATE NAME: BLOCKED ---
Project: [project-name]
[Relevant counts/details]

Cannot proceed: [reason]
Fix: [action], then re-run /prep.
---
```

Pass message format:
```
--- Gate Name: PASSED ---
[Summary stats]
---
```

Real fact-check file format (from Haiti project):
- Verdict line: `**Overall Verdict:** [emoji] **APPROVED FOR FILMING** (with N required fix)`
- Executive summary table with Category/Count/Status columns
- `REQUIRED FIX` sections marked with red circle emoji
- Simplification flags marked with yellow circle emoji
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Fact-Check Verification Gate section to prep.md</name>
  <files>.claude/commands/prep.md</files>
  <action>
Add a new section titled "## Fact-Check Verification Gate (MANDATORY — Runs Before All Prep Output)" to `.claude/commands/prep.md`. Insert it AFTER the "Feedback Insights" section (after line 101) and BEFORE the "EDIT GUIDE" section (line 104). This positions the gate after all context-loading sections but before any output generation — matching the pattern from Phase 71.

**Applies to:** ALL modes — `--edit-guide`, `--assets`, `--full`, `--split-screen`, and interactive (no mode specified). Unlike Phase 71's research gate which skips revision modes, this gate has no skips because all prep modes are pre-filming and require verified facts.

**Step 1: Locate the fact-check file.**

Glob for `video-projects/**/[project]/03-FACT-CHECK-VERIFICATION.md`. If the file is not found, BLOCK — display the blocked message below and STOP. Do NOT proceed (unlike Phase 71 which warns on missing file, a missing fact-check file means the verification step was skipped entirely).

```
--- FACT-CHECK VERIFICATION GATE: BLOCKED ---
Project: [project-name]
Status: No fact-check file found

Cannot proceed: 03-FACT-CHECK-VERIFICATION.md does not exist.
Run /verify first to generate the fact-check report, then re-run /prep.
---
```

**Step 2: Scan for verdict line.**

Search the file for lines containing "Verdict:" (case-insensitive). Check if the word "APPROVED" appears anywhere on that line.

- If NO line contains "Verdict:" at all — treat as placeholder/incomplete file. BLOCK:

```
--- FACT-CHECK VERIFICATION GATE: BLOCKED ---
Project: [project-name]
Status: Fact-check file exists but contains no verdict

Cannot proceed: 03-FACT-CHECK-VERIFICATION.md has no verdict line.
Complete the fact-check with /verify, then re-run /prep.
---
```

- If a verdict line exists but does NOT contain "APPROVED" — BLOCK. Extract the actual verdict text for display. Also scan the file for severity markers to build an issue summary:
  - Count lines/sections containing red circle emoji or "REQUIRED FIX" = required fixes
  - Count lines/sections containing yellow circle emoji or "SIMPLIFICATION" = simplification flags  
  - Count lines/sections containing warning emoji or "NEEDS CLARIFICATION" or "Needs clarification" = clarification items

```
--- FACT-CHECK VERIFICATION GATE: BLOCKED ---
Project: [project-name]
Verdict: [extracted verdict text from the verdict line]
Required fixes: N
Clarification needed: N
Simplification flags: N

Cannot proceed: Fact-check verdict is not APPROVED.
Address the issues in 03-FACT-CHECK-VERIFICATION.md, re-run /verify, then re-run /prep.
---
```

After the bordered block, list each outstanding item with a one-line description. Format:
```
Outstanding items:
- [RED CIRCLE] [item title]: [one-line description from the file]
- [WARNING] [item title]: [one-line description]
```

**Step 3: Handle APPROVED verdict.**

If the verdict line contains "APPROVED":

First, check if any required fix sections still exist in the file (lines with red circle emoji or "REQUIRED FIX"). If yes, PASS but with a WARNING:

```
--- Fact-Check Verification Gate: PASSED (with warnings) ---
Verdict: [extracted verdict text]
Note: N required fix(es) still listed — verify these are resolved before filming.
---
```

If APPROVED with no outstanding required fixes, clean PASS. Extract summary stats from the executive summary table if present (total claims, verified count):

```
--- Fact-Check Verification Gate: PASSED ---
Verdict: [extracted verdict text]
Claims: X verified of Y total
---
```

Then proceed normally to the prep output generation.
  </action>
  <verify>
    <automated>cd "D:/History vs Hype" && grep -c "FACT-CHECK VERIFICATION GATE" .claude/commands/prep.md</automated>
  </verify>
  <done>
- prep.md contains "Fact-Check Verification Gate" section between Feedback Insights and EDIT GUIDE sections
- Section applies to ALL modes (no skip list)
- Missing file = BLOCK with message directing to /verify
- No verdict line = BLOCK with "no verdict" message
- Non-APPROVED verdict = BLOCK showing verdict + severity counts + item list
- APPROVED with required fixes = PASS with warning
- Clean APPROVED = PASS with summary stats
- Gate pattern matches Phase 71's bordered message format
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Prerequisites section to reflect mandatory gate</name>
  <files>.claude/commands/prep.md</files>
  <action>
Update the existing "Prerequisites" list in the "Integration with Production Workflow" section (around line 523-527) to make the fact-check gate explicit. The current list says:
```
- SCRIPT.md exists (for --assets)
- SRT file exists (for --edit-guide with runtime comparison)
- Fact-check APPROVED (for --full)
```

Change it to:
```
- Fact-check APPROVED in 03-FACT-CHECK-VERIFICATION.md (ALL modes — enforced by gate)
- SCRIPT.md exists (for --assets)
- SRT file exists (for --edit-guide with runtime comparison)
```

This moves fact-check from a soft prerequisite for `--full` only to a hard gate for all modes, matching the new gate behavior.
  </action>
  <verify>
    <automated>cd "D:/History vs Hype" && grep -A3 "Prerequisites" .claude/commands/prep.md | grep -c "ALL modes"</automated>
  </verify>
  <done>Prerequisites section lists fact-check APPROVED as first item applying to ALL modes, not just --full.</done>
</task>

</tasks>

<verification>
1. `grep -c "FACT-CHECK VERIFICATION GATE" .claude/commands/prep.md` returns at least 1
2. `grep -c "BLOCKED" .claude/commands/prep.md` returns at least 3 (three block scenarios: missing file, no verdict, non-approved verdict)
3. `grep -c "PASSED" .claude/commands/prep.md` returns at least 2 (pass with warning, clean pass)
4. The gate section appears AFTER "Feedback Insights" and BEFORE "EDIT GUIDE"
5. No modes are exempted from the gate
</verification>

<success_criteria>
- The `/prep` command file contains a Fact-Check Verification Gate that blocks on missing file, missing verdict, and non-APPROVED verdict
- APPROVED verdicts pass through (with warning if required fixes exist)
- Block messages show the actual verdict, severity counts, and outstanding item list
- Gate applies to ALL /prep modes with no exceptions
- Pattern matches Phase 71's gate format (bordered messages, clear BLOCKED/PASSED labels)
</success_criteria>

<output>
After completion, create `.planning/phases/72-prep-gate/72-01-SUMMARY.md`
</output>
