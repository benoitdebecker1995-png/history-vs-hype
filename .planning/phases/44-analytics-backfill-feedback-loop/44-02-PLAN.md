---
phase: 44-analytics-backfill-feedback-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - .claude/commands/prep.md
  - .claude/commands/publish.md
  - .claude/commands/research.md
  - .claude/commands/analyze.md
  - tools/youtube-analytics/analyze.py
autonomous: true
requirements:
  - ANLYT-02

must_haves:
  truths:
    - "During /prep execution, a brief channel performance advisory appears if channel-insights.md exists"
    - "During /publish execution, a brief channel performance advisory appears if channel-insights.md exists"
    - "During /research --new execution, a brief channel performance advisory appears if channel-insights.md exists"
    - "If channel-insights.md does not exist, commands proceed silently without errors"
    - "After /analyze --save completes, channel-insights.md is auto-regenerated with updated data"
  artifacts:
    - path: ".claude/commands/prep.md"
      provides: "Channel insights loading section"
      contains: "Channel Insights"
    - path: ".claude/commands/publish.md"
      provides: "Channel insights loading section"
      contains: "Channel Insights"
    - path: ".claude/commands/research.md"
      provides: "Channel insights loading section"
      contains: "Channel Insights"
    - path: ".claude/commands/analyze.md"
      provides: "Backfill flag and auto-regeneration hook"
      contains: "backfill"
  key_links:
    - from: ".claude/commands/prep.md"
      to: "channel-data/channel-insights.md"
      via: "File read as agent context"
      pattern: "channel-insights.md"
    - from: ".claude/commands/publish.md"
      to: "channel-data/channel-insights.md"
      via: "File read as agent context"
      pattern: "channel-insights.md"
    - from: ".claude/commands/research.md"
      to: "channel-data/channel-insights.md"
      via: "File read as agent context"
      pattern: "channel-insights.md"
    - from: "tools/youtube-analytics/analyze.py"
      to: "tools/youtube-analytics/backfill.py"
      via: "generate_channel_insights_report() call after save_analysis()"
      pattern: "generate_channel_insights_report"
---

<objective>
Wire channel performance insights into all production commands and add auto-regeneration after /analyze.

Purpose: The backfill module (Plan 01) populates the DB and generates channel-insights.md. This plan ensures that insights actually surface during video production workflows — /script already has pre-script intelligence, but /prep, /publish, and /research --new do not. Also adds --backfill flag to /analyze and auto-regenerates insights after each analysis save.

Output: Updated command files with insight loading sections, updated analyze.py with auto-regeneration
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-analytics-backfill-feedback-loop/44-01-SUMMARY.md
@.claude/commands/prep.md
@.claude/commands/publish.md
@.claude/commands/research.md
@.claude/commands/analyze.md
@tools/youtube-analytics/analyze.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add channel insights loading to /prep, /publish, /research commands</name>
  <files>.claude/commands/prep.md, .claude/commands/publish.md, .claude/commands/research.md</files>
  <action>
Add a "Channel Insights Context" section near the top of each command file (after the usage/flags section, before the main workflow). Use the SAME pattern in all three files for consistency.

**Section to add to each command:**

```markdown
## Channel Insights Context (Auto-run)

Before generating output, check for own-channel performance context:

1. Read `channel-data/channel-insights.md` if it exists
2. Use as **internal context** for decisions — do NOT dump full file to user
3. Display a brief 2-3 line advisory block:

```
--- Channel Performance Context ---
[Extract 2-3 most relevant lines from channel-insights.md for this workflow]
Example: Top format: territorial (avg 1,950 views). Best retention: 42.0%.
Low signal: ~15 videos — experiment freely.
---
```

4. If file does not exist, skip silently — NEVER block generation on missing analytics
5. Insights are advisory — guide experimentation, never dictate choices

**For /prep:** Focus on format/length insights (what video lengths retain best, what B-roll types correlate with engagement)
**For /publish:** Focus on title patterns and topic performance (what titles/topics get highest CTR/views)
**For /research --new:** Focus on topic opportunity insights (what topic types perform well, what's underexplored)
```

**Key principles from locked decisions:**
- Surface ALL insight types (retention, topic, format)
- Advisory, not prescriptive — "has worked well historically" not "you must"
- Brief display (2-3 lines), full file as silent context
- Graceful degradation: skip if file missing, never block

For /prep: insert the section after the Flags table, before the first workflow section.
For /publish: insert the section after the Flags table, before the first workflow section.
For /research: insert the section after the Flags table, before the first workflow section.
  </action>
  <verify>
Read each updated command file and confirm:
1. "Channel Insights Context" section exists
2. References channel-data/channel-insights.md
3. Includes "skip silently" graceful degradation
4. Contains advisory language (not prescriptive)
5. Each command has workflow-specific focus (prep=format, publish=titles, research=topics)
  </verify>
  <done>
All three commands (/prep, /publish, /research) load channel-insights.md as context and display a brief advisory. Missing file is handled gracefully. Insights are advisory, not prescriptive.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --backfill flag to /analyze and auto-regenerate insights after save</name>
  <files>.claude/commands/analyze.md, tools/youtube-analytics/analyze.py</files>
  <action>
**Part A: Update .claude/commands/analyze.md**

1. Add `--backfill` flag to the usage section and flags table:
   - `--backfill`: Run full analytics backfill (imports JSON + markdown + reclassifies + generates insights)
   - Example: `/analyze --backfill`

2. Add a workflow section for `--backfill`:
```markdown
## BACKFILL ANALYTICS (`--backfill`)

Run the full analytics backfill pipeline to populate the DB from all existing channel data.

```python
import sys
sys.path.insert(0, '.')
from pathlib import Path
from tools.youtube_analytics.backfill import run_backfill

project_root = Path('.')
result = run_backfill(project_root)

print(f"JSON import: {result['imported_json']} videos")
print(f"Markdown import: {result['imported_md']} analyses")
print(f"Reclassified: {result['reclassified']} topics")
print(f"Insights saved to: {result.get('insights_path', 'N/A')}")
```

This is safe to re-run anytime (idempotent upsert design).
```

3. Add a note in the existing save/analysis workflow: after save_analysis() completes, auto-regenerate channel-insights.md:
```markdown
### Auto-Regenerate Channel Insights

After saving any analysis, regenerate the channel insights report to keep it current:

```python
from tools.youtube_analytics.backfill import generate_channel_insights_report
result = generate_channel_insights_report(Path('.'))
if 'error' not in result:
    print(f"Channel insights updated: {result['saved_to']}")
```
```

**Part B: Update tools/youtube-analytics/analyze.py**

In the `save_analysis()` function, after the existing save logic completes successfully, add a call to auto-regenerate channel insights:

```python
# Auto-regenerate channel insights after saving analysis
try:
    from backfill import generate_channel_insights_report
    insights_result = generate_channel_insights_report(project_root)
    if 'error' not in insights_result:
        print(f"  Channel insights updated: {insights_result['saved_to']}")
except ImportError:
    pass  # backfill.py not yet available — graceful degradation
except Exception as e:
    print(f"  Warning: Could not regenerate insights: {e}")
```

This import uses the relative import pattern since both files are in tools/youtube-analytics/. Use try/except for graceful degradation.

**IMPORTANT:** Do NOT change any other behavior in analyze.py. This is a minimal addition — one try/except block at the end of save_analysis().
  </action>
  <verify>
1. Read analyze.md and confirm --backfill flag is documented with workflow section
2. Read analyze.md and confirm auto-regeneration note exists
3. Read analyze.py and confirm save_analysis() has the insights regeneration hook
4. Run `python tools/youtube-analytics/analyze.py --help` to confirm no syntax errors
  </verify>
  <done>
/analyze --backfill documented and wired. save_analysis() auto-regenerates channel-insights.md after each analysis save. Graceful degradation if backfill.py not available.
  </done>
</task>

</tasks>

<verification>
1. Read /prep command — contains "Channel Insights Context" section
2. Read /publish command — contains "Channel Insights Context" section
3. Read /research command — contains "Channel Insights Context" section
4. Read /analyze command — contains --backfill flag and auto-regeneration
5. analyze.py save_analysis() includes insights regeneration hook
6. No command blocks on missing channel-insights.md
</verification>

<success_criteria>
- Channel-specific insights surface during /prep, /publish, /research --new (ANLYT-02)
- /analyze --backfill provides CLI entry point for the backfill module
- channel-insights.md auto-regenerates after each /analyze --save
- All integration is graceful — no command breaks if analytics data is missing
</success_criteria>

<output>
After completion, create `.planning/phases/44-analytics-backfill-feedback-loop/44-02-SUMMARY.md`
</output>
