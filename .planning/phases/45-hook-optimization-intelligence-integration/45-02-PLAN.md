---
phase: 45-hook-optimization-intelligence-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/script.md
  - .claude/commands/prep.md
  - .claude/commands/publish.md
autonomous: true
requirements:
  - INTEL-05

must_haves:
  truths:
    - "YouTube intelligence insights auto-surface during /script generation without manual lookup"
    - "YouTube intelligence insights auto-surface during /prep generation without manual lookup"
    - "YouTube intelligence insights auto-surface during /publish generation without manual lookup"
    - "Advisory is brief (2-3 lines) and workflow-specific (hook tips for /script, format tips for /prep, title patterns for /publish)"
    - "Commands skip silently if youtube-intelligence.md is missing"
    - "Staleness (>30 days) is noted but does not block generation"
  artifacts:
    - path: ".claude/commands/script.md"
      provides: "YouTube intelligence advisory in /script"
      contains: "YouTube Intelligence Context"
    - path: ".claude/commands/prep.md"
      provides: "YouTube intelligence advisory in /prep"
      contains: "YouTube Intelligence Context"
    - path: ".claude/commands/publish.md"
      provides: "YouTube intelligence advisory in /publish"
      contains: "YouTube Intelligence Context"
  key_links:
    - from: ".claude/commands/script.md"
      to: "channel-data/youtube-intelligence.md"
      via: "Advisory reads KB and extracts hook/algorithm insights"
      pattern: "youtube-intelligence"
    - from: ".claude/commands/prep.md"
      to: "channel-data/youtube-intelligence.md"
      via: "Advisory reads KB and extracts format/length insights"
      pattern: "youtube-intelligence"
    - from: ".claude/commands/publish.md"
      to: "channel-data/youtube-intelligence.md"
      via: "Advisory reads KB and extracts title/CTR insights"
      pattern: "youtube-intelligence"
---

<objective>
Add YouTube Intelligence advisory blocks to /script, /prep, and /publish commands. Each command auto-surfaces relevant intelligence insights at command start, following the same proven pattern as Phase 44's channel insights advisory (read file, extract relevant lines, display brief advisory, skip silently if missing).

Purpose: Intelligence gathered by the YouTube Intelligence Engine should surface automatically during production workflows without requiring manual /intel lookups. Each command gets workflow-specific insights.
Output: Updated script.md, prep.md, publish.md with YouTube Intelligence Context sections
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/commands/script.md
@.claude/commands/prep.md
@.claude/commands/publish.md
@channel-data/youtube-intelligence.md
@.planning/phases/45-hook-optimization-intelligence-integration/45-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add YouTube Intelligence advisory to /script command</name>
  <files>.claude/commands/script.md</files>
  <action>
Add a new section titled "## YouTube Intelligence Context (Auto-run)" to the /script command. Place it AFTER the existing "## YouTube Intelligence Check (Auto-run Before Every Script)" section and BEFORE the "## Before Writing" section.

**The section follows the EXACT same pattern as the existing "## Channel Insights Context (Auto-run)" in prep.md and publish.md.** This is the Phase 44 proven advisory pattern.

**Section content:**

```markdown
## YouTube Intelligence Context (Auto-run)

Before generating output, check for YouTube algorithm and niche intelligence:

1. Read `channel-data/youtube-intelligence.md` if it exists
2. Use as **internal context** for hook and structure decisions — do NOT dump full file to user
3. Display a brief 2-3 line advisory block:

```
--- YouTube Intelligence Context ---
[Extract 2-3 most relevant lines from youtube-intelligence.md for this workflow]
Example: Algorithm priority: viewer satisfaction (very_high weight). Niche trend: 20-30min docs dominating.
Hook pattern from outliers: "legal fiction exposed" frame drove 4x median views.
---
```

4. If file does not exist, skip silently — NEVER block generation on missing intelligence
5. If last refresh date is >30 days old, add note: "(Intel last refreshed [date] — consider running /intel --refresh)"
6. Intelligence is advisory — inform hook and structure decisions, never dictate

**For /script:** Focus on:
- **Algorithm signals:** What satisfaction signals matter most right now (AVD, CTR, satisfaction surveys)
- **Hook patterns:** What hook types are working in outlier videos (from Outlier Analysis section)
- **Niche format trends:** What video lengths and formats are performing (from Niche Patterns section)
- **Competitor gaps:** What topics competitors are NOT covering (differentiation opportunities)
```

Also update the existing "## YouTube Intelligence Check (Auto-run Before Every Script)" section. After the Step 2 code block that loads the KB, add a paragraph:

```markdown
**Advisory Display:** After loading the KB, display a brief 2-3 line advisory summarizing the most relevant intelligence for this script. Focus on algorithm priorities, niche patterns, and any outlier hook patterns. See "YouTube Intelligence Context (Auto-run)" section below for format.
```

This connects the existing staleness/refresh mechanism with the new advisory display.
  </action>
  <verify>
Read script.md. Verify "YouTube Intelligence Context (Auto-run)" section exists. Verify it mentions youtube-intelligence.md. Verify it specifies workflow-specific focus (hook patterns, algorithm signals). Verify staleness note instruction (>30 days). Verify "skip silently" instruction.
  </verify>
  <done>/script command has YouTube Intelligence Context advisory section with hook-focused intelligence surfacing, staleness detection, and graceful skip on missing data.</done>
</task>

<task type="auto">
  <name>Task 2: Add YouTube Intelligence advisory to /prep and /publish commands</name>
  <files>.claude/commands/prep.md, .claude/commands/publish.md</files>
  <action>
Add "## YouTube Intelligence Context (Auto-run)" sections to both /prep and /publish commands. Place each section AFTER the existing "## Channel Insights Context (Auto-run)" section and BEFORE the next workflow section.

**Use the same structure for both, but with workflow-specific focus lines.**

**For /prep (prep.md):**

Place after "## Channel Insights Context (Auto-run)" and before "## Feedback Insights (Automatic)".

```markdown
## YouTube Intelligence Context (Auto-run)

Before generating output, check for YouTube algorithm and niche intelligence:

1. Read `channel-data/youtube-intelligence.md` if it exists
2. Use as **internal context** for production decisions — do NOT dump full file to user
3. Display a brief 2-3 line advisory block:

```
--- YouTube Intelligence Context ---
[Extract 2-3 most relevant lines from youtube-intelligence.md for this workflow]
Example: Niche trend: 20-30min docs with evidence B-roll outperforming shorter formats.
Competitor gap: No channels covering [topic angle] — differentiation opportunity.
---
```

4. If file does not exist, skip silently — NEVER block generation on missing intelligence
5. If last refresh date is >30 days old, add note: "(Intel last refreshed [date] — consider running /intel --refresh)"
6. Intelligence is advisory — inform production decisions, never dictate

**For /prep:** Focus on:
- **Format/length insights:** What video lengths and formats are performing in the niche
- **B-roll patterns:** What visual styles correlate with outlier performance (evidence on screen, maps, documents)
- **Competitor production patterns:** What production approaches competitors are using
```

**For /publish (publish.md):**

Place after "## Channel Insights Context (Auto-run)" and before "## PRE-PUBLISH QUALITY GATES".

```markdown
## YouTube Intelligence Context (Auto-run)

Before generating output, check for YouTube algorithm and niche intelligence:

1. Read `channel-data/youtube-intelligence.md` if it exists
2. Use as **internal context** for metadata decisions — do NOT dump full file to user
3. Display a brief 2-3 line advisory block:

```
--- YouTube Intelligence Context ---
[Extract 2-3 most relevant lines from youtube-intelligence.md for this workflow]
Example: Title pattern from outliers: specific mechanism titles ("How X deleted Y") > vague framing.
Algorithm: CTR weight is "high" — thumbnail/title alignment critical.
---
```

4. If file does not exist, skip silently — NEVER block generation on missing intelligence
5. If last refresh date is >30 days old, add note: "(Intel last refreshed [date] — consider running /intel --refresh)"
6. Intelligence is advisory — inform title and metadata decisions, never dictate

**For /publish:** Focus on:
- **Title patterns:** What title formulas are working in outlier videos (mechanism titles, question titles, etc.)
- **CTR signals:** What the algorithm currently weights for CTR and how it affects discovery
- **Competitor title trends:** What title patterns competitors are using (avoid/differentiate)
- **Niche topic clusters:** What topics are trending in the niche (tag relevance)
```

**Important implementation notes for both files:**
- Keep the existing "Channel Insights Context" section unchanged — YouTube Intelligence Context is a SEPARATE advisory
- The two advisories should be displayed as separate blocks (channel insights = own-channel data, YouTube intelligence = algorithm + niche + competitor data)
- Both are brief (2-3 lines each) so combined overhead is still minimal
  </action>
  <verify>
Read prep.md. Verify "YouTube Intelligence Context (Auto-run)" section exists after "Channel Insights Context". Verify workflow-specific focus (format/length, B-roll patterns).
Read publish.md. Verify "YouTube Intelligence Context (Auto-run)" section exists after "Channel Insights Context". Verify workflow-specific focus (title patterns, CTR signals).
Both files: Verify staleness note instruction. Verify "skip silently" instruction. Verify existing sections unchanged.
  </verify>
  <done>/prep and /publish commands both have YouTube Intelligence Context advisory sections with workflow-specific intelligence surfacing, separate from existing channel insights, with staleness detection and graceful skip on missing data.</done>
</task>

</tasks>

<verification>
1. `grep "YouTube Intelligence Context" .claude/commands/script.md` returns match
2. `grep "YouTube Intelligence Context" .claude/commands/prep.md` returns match
3. `grep "YouTube Intelligence Context" .claude/commands/publish.md` returns match
4. `grep "youtube-intelligence.md" .claude/commands/script.md` returns match(es)
5. `grep "youtube-intelligence.md" .claude/commands/prep.md` returns match
6. `grep "youtube-intelligence.md" .claude/commands/publish.md` returns match
7. `grep "skip silently" .claude/commands/script.md` returns match in intelligence context section
8. `grep "skip silently" .claude/commands/prep.md` returns match in both context sections
9. `grep "skip silently" .claude/commands/publish.md` returns match in both context sections
10. `grep "30 days" .claude/commands/script.md` returns match for staleness check
11. Existing "Channel Insights Context" sections in prep.md and publish.md are unchanged
12. Each command has workflow-specific focus (hook for script, format for prep, title for publish)
</verification>

<success_criteria>
- All three commands (/script, /prep, /publish) surface YouTube intelligence automatically
- Each advisory is workflow-specific (not generic copy-paste)
- Advisory format matches Phase 44's channel insights pattern (brief, advisory, skip if missing)
- Staleness >30 days is noted without blocking
- Missing file does not block any command
- Existing channel insights advisories are preserved and separate
</success_criteria>

<output>
After completion, create `.planning/phases/45-hook-optimization-intelligence-integration/45-02-SUMMARY.md`
</output>
