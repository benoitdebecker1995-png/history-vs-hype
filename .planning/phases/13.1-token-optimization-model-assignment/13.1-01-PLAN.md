---
phase: 13.1-token-optimization-model-assignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/status.md
  - .claude/commands/help.md
  - .claude/commands/fix.md
  - .claude/commands/sources.md
  - .claude/commands/prep.md
  - .claude/commands/discover.md
  - .claude/commands/verify.md
  - .claude/commands/publish.md
  - .claude/commands/engage.md
  - .claude/commands/analyze.md
  - .claude/commands/patterns.md
  - .claude/commands/research.md
  - .claude/commands/script.md
autonomous: true

must_haves:
  truths:
    - "Each skill file has model: field in YAML frontmatter"
    - "Haiku assigned to simple tasks (status, help, fix, sources, prep, discover)"
    - "Sonnet assigned to standard tasks (verify, publish, engage, analyze, patterns, research)"
    - "Opus assigned to script command (complex creative task)"
  artifacts:
    - path: ".claude/commands/status.md"
      provides: "Project status routing"
      contains: "model: haiku"
    - path: ".claude/commands/help.md"
      provides: "Command menu display"
      contains: "model: haiku"
    - path: ".claude/commands/fix.md"
      provides: "Subtitle correction"
      contains: "model: haiku"
    - path: ".claude/commands/sources.md"
      provides: "Source recommendations"
      contains: "model: haiku"
    - path: ".claude/commands/prep.md"
      provides: "Filming preparation"
      contains: "model: haiku"
    - path: ".claude/commands/discover.md"
      provides: "Keyword research workflow"
      contains: "model: haiku"
    - path: ".claude/commands/verify.md"
      provides: "Fact-checking workflow"
      contains: "model: sonnet"
    - path: ".claude/commands/publish.md"
      provides: "YouTube metadata workflow"
      contains: "model: sonnet"
    - path: ".claude/commands/engage.md"
      provides: "Comment response workflow"
      contains: "model: sonnet"
    - path: ".claude/commands/analyze.md"
      provides: "Post-publish analysis"
      contains: "model: sonnet"
    - path: ".claude/commands/patterns.md"
      provides: "Pattern recognition"
      contains: "model: sonnet"
    - path: ".claude/commands/research.md"
      provides: "Research workflow"
      contains: "model: sonnet"
    - path: ".claude/commands/script.md"
      provides: "Script generation"
      contains: "model: opus"
  key_links: []
---

<objective>
Add model assignments to all skill files (slash commands)

Purpose: Enable token optimization by specifying which model should execute each skill. Simple tasks get Haiku (faster/cheaper), standard tasks get Sonnet (balanced), complex creative tasks get Opus (highest quality).

Output: All 13 skill files updated with `model:` field in YAML frontmatter
</objective>

<execution_context>
@C:\Users\Benoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Benoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Haiku model to simple skills</name>
  <files>
    .claude/commands/status.md
    .claude/commands/help.md
    .claude/commands/fix.md
    .claude/commands/sources.md
    .claude/commands/prep.md
    .claude/commands/discover.md
  </files>
  <action>
For each file, update the YAML frontmatter to add `model: haiku` field.

Current pattern (example from status.md):
```yaml
---
description: Smart project status and next action suggestion
---
```

Target pattern:
```yaml
---
description: Smart project status and next action suggestion
model: haiku
---
```

Add `model: haiku` as the last field before the closing `---`.

Why Haiku for these:
- /status: File scanning, simple logic, no creativity needed
- /help: Static menu display, no reasoning
- /fix: Pattern replacement in SRT files, mechanical task
- /sources: Template-based recommendations, no synthesis
- /prep: Template-based edit guides, no creativity
- /discover: Keyword workflow orchestration, mostly templated steps
  </action>
  <verify>
For each file, run: `head -5 .claude/commands/{file}.md` and confirm `model: haiku` appears in frontmatter.
  </verify>
  <done>All 6 simple skill files contain `model: haiku` in YAML frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Add Sonnet model to standard skills</name>
  <files>
    .claude/commands/verify.md
    .claude/commands/publish.md
    .claude/commands/engage.md
    .claude/commands/analyze.md
    .claude/commands/patterns.md
    .claude/commands/research.md
  </files>
  <action>
For each file, update the YAML frontmatter to add `model: sonnet` field.

Add `model: sonnet` as the last field before the closing `---`.

Why Sonnet for these:
- /verify: Fact-checking requires reasoning, source evaluation
- /publish: Metadata generation requires some creativity
- /engage: Comment responses need nuance, empathy
- /analyze: Analytics interpretation requires pattern recognition
- /patterns: Cross-video pattern analysis needs synthesis
- /research: Research synthesis requires balanced reasoning
  </action>
  <verify>
For each file, run: `head -5 .claude/commands/{file}.md` and confirm `model: sonnet` appears in frontmatter.
  </verify>
  <done>All 6 standard skill files contain `model: sonnet` in YAML frontmatter</done>
</task>

<task type="auto">
  <name>Task 3: Add Opus model to script skill</name>
  <files>
    .claude/commands/script.md
  </files>
  <action>
Update YAML frontmatter to add `model: opus` field.

Why Opus for /script:
- Script generation is the most creative task
- Requires retention engineering, style matching
- Quality directly impacts video performance
- Worth the higher token cost for best output
  </action>
  <verify>
Run: `head -5 .claude/commands/script.md` and confirm `model: opus` appears in frontmatter.
  </verify>
  <done>Script skill file contains `model: opus` in YAML frontmatter</done>
</task>

</tasks>

<verification>
Run verification for all 13 skill files:

```bash
for file in status help fix sources prep discover verify publish engage analyze patterns research script; do
  echo "=== $file ==="
  head -5 .claude/commands/$file.md
done
```

Confirm:
- 6 files show `model: haiku`
- 6 files show `model: sonnet`
- 1 file shows `model: opus`
</verification>

<success_criteria>
1. All 13 skill files have `model:` field in YAML frontmatter
2. Model assignments match the optimization strategy:
   - Haiku: status, help, fix, sources, prep, discover
   - Sonnet: verify, publish, engage, analyze, patterns, research
   - Opus: script
3. YAML syntax valid (no parsing errors)
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-token-optimization-model-assignment/13.1-01-SUMMARY.md`
</output>
