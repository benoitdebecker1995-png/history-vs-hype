---
phase: 35-actionable-analytics
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified:
  - tools/youtube-analytics/analyze.py
  - .claude/commands/analyze.md
  - .claude/commands/script.md
autonomous: true

must_haves:
  truths:
    - "/analyze VIDEO_ID with a script path produces section-level retention diagnostics with voice pattern recommendations"
    - "/script command displays pre-script intelligence block before generating script"
    - "Pre-script insights include topic performance summary, retention lessons, and suggested patterns"
    - "Section diagnostics appear in POST-PUBLISH-ANALYSIS output when both retention data and script are available"
  artifacts:
    - path: "tools/youtube-analytics/analyze.py"
      provides: "Extended analyze with section-level retention diagnostics"
      contains: "retention_mapper"
    - path: ".claude/commands/analyze.md"
      provides: "Updated /analyze command with --script flag documentation"
      contains: "--script"
    - path: ".claude/commands/script.md"
      provides: "Updated /script command with pre-script intelligence surfacing"
      contains: "pre_script_insights"
  key_links:
    - from: "tools/youtube-analytics/analyze.py"
      to: "tools/youtube-analytics/retention_mapper.py"
      via: "Import for section mapping"
      pattern: "retention_mapper"
    - from: "tools/youtube-analytics/analyze.py"
      to: "tools/youtube-analytics/section_diagnostics.py"
      via: "Import for drop diagnosis"
      pattern: "section_diagnostics"
    - from: ".claude/commands/script.md"
      to: "tools/youtube-analytics/feedback_queries.py"
      via: "Reference to get_pre_script_insights"
      pattern: "get_pre_script_insights|pre.script"
---

<objective>
Wire the retention mapper, section diagnostics, and pre-script insights into the /analyze and /script commands — completing the "actionable" part of Actionable Analytics.

Purpose: Plans 35-01 and 35-02 built the intelligence modules (retention mapping, diagnostics, topic strategy, pre-script insights). This plan connects them to the user-facing commands so insights actually surface during production workflows. ACTN-01/02 need /analyze integration. ACTN-04 needs /script integration.

Output: Extended analyze.py with section diagnostics, updated /analyze and /script command documentation.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-actionable-analytics/35-RESEARCH.md
@tools/youtube-analytics/analyze.py
@.claude/commands/analyze.md
@.claude/commands/script.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend analyze.py with section-level retention diagnostics</name>
  <files>tools/youtube-analytics/analyze.py</files>
  <action>
Add section-level retention analysis to `analyze.py`:

**New function: `generate_section_diagnostics(video_id, script_path)`**
- Import retention_mapper and section_diagnostics with try/except and DIAGNOSTICS_AVAILABLE flag
- Import ScriptParser from production/parser.py
- Call existing `get_retention_data(video_id)` to get retention curve
- Call `find_drop_off_points(retention_data)` to get drops
- Parse script with ScriptParser to get sections
- Call `map_retention_to_sections(drops, sections)` to map drops to sections
- Call `diagnose_all_drops(mapped_drops, sections)` to get diagnostics
- Return `{'status': 'success', 'mapped_drops': [...], 'diagnostics': [...], 'summary_table': str}` or `{'error': msg}`
- Falls back gracefully if retention_mapper/section_diagnostics not importable

**Integration into existing analyze workflow:**
- Add optional `--script PATH` argument to analyze CLI (argparse)
- When --script provided AND retention data available: call generate_section_diagnostics
- Append section diagnostics to the POST-PUBLISH-ANALYSIS output as new section:
  ```markdown
  ## Section-Level Retention Analysis

  ### Retention Drop Map
  {formatted_mapped_drops_table from retention_mapper}

  ### Diagnostics
  {formatted_diagnostics_markdown from section_diagnostics}
  ```
- When --script NOT provided: skip section diagnostics (existing behavior unchanged)

**Error handling:**
- Missing script file: print warning, skip section diagnostics
- No retention data available: print warning, skip section diagnostics
- Import errors for new modules: set DIAGNOSTICS_AVAILABLE=False, skip silently
- Error dict pattern for generate_section_diagnostics return

**Preserve all existing analyze.py functionality.** Only add new code, don't modify existing functions.
  </action>
  <verify>
Run: `python tools/youtube-analytics/analyze.py --help` -- shows --script argument.
Run: `python -c "import sys; sys.path.insert(0, 'tools/youtube-analytics'); from analyze import generate_section_diagnostics; print('OK')"` -- function exists.
Verify existing analyze behavior unchanged (no --script = no section diagnostics).
  </verify>
  <done>
analyze.py extended with generate_section_diagnostics function and --script CLI argument. Section diagnostics appear in POST-PUBLISH-ANALYSIS when both retention data and script path provided. Existing behavior unchanged when --script omitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /analyze command documentation</name>
  <files>.claude/commands/analyze.md</files>
  <action>
Update `.claude/commands/analyze.md` to document the new section-level diagnostics capability:

**Changes:**
1. Add `--script PATH` to the flags table:
   ```
   | `--script` | Path to script file for section-level retention analysis | `/analyze VIDEO_ID --script path/to/script.md` |
   ```

2. Add new section after the existing retention analysis section:
   ```markdown
   ## SECTION-LEVEL RETENTION DIAGNOSTICS (`--script`)

   When a script file is provided, the system maps retention drops to specific script sections and provides actionable fix recommendations.

   ### How It Works
   1. Fetches retention curve from YouTube Analytics API
   2. Parses script into H2 sections with word counts
   3. Maps retention drop points to sections using word-count-based timing (150 WPM)
   4. Diagnoses root causes for each drop
   5. Recommends specific voice patterns from STYLE-GUIDE.md Part 6

   ### Output
   - **Retention Drop Map:** Table showing which sections lost viewers, with magnitude and severity
   - **Section Diagnostics:** Root cause analysis with specific pattern recommendations
   - Each recommendation references exact STYLE-GUIDE.md Part 6 patterns

   ### Requirements
   - Video must have retention data (published and >48 hours old)
   - Script file must be a markdown file with H2 section headings

   ### Example
   ```bash
   /analyze VIDEO_ID --script video-projects/_ARCHIVED/1-belize-2025/02-SCRIPT-DRAFT.md
   ```
   ```

3. Update the workflow section to show when to use --script.

**Preserve all existing content.**
  </action>
  <verify>
Read `.claude/commands/analyze.md` and confirm: (1) --script in flags table, (2) SECTION-LEVEL RETENTION DIAGNOSTICS section exists, (3) existing content unchanged.
  </verify>
  <done>
/analyze command updated with --script flag documentation and section-level retention diagnostics section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update /script command with pre-script intelligence surfacing</name>
  <files>.claude/commands/script.md</files>
  <action>
Update `.claude/commands/script.md` to add pre-script intelligence surfacing (ACTN-04):

**Changes:**
1. Add a new section near the top of the command workflow (after "## SCRIPT GENERATION" but before the actual generation steps):
   ```markdown
   ## PRE-SCRIPT INTELLIGENCE

   Before generating a script, the system automatically surfaces relevant past performance insights.

   ### Automatic Display
   When topic type is known (territorial, ideological, fact-check, general), the system displays:
   - **Topic Performance:** How this topic type has performed historically (retention, conversion)
   - **Retention Lessons:** What caused viewer drop-offs in similar past videos
   - **Suggested Patterns:** Which STYLE-GUIDE.md Part 6 voice patterns work best for this topic type

   ### How to Use
   The pre-script intelligence block appears automatically before script generation begins.
   Use these insights to inform structure decisions:
   - If past territorial videos lost viewers during treaty text → show treaty on screen instead
   - If causal chains correlated with high retention → prioritize "which meant that" transitions
   - If topic type has low sample size → insights flagged as "low confidence"

   ### Technical Details
   Insights come from:
   - `tools/youtube-analytics/feedback_queries.py` → `get_pre_script_insights(topic_type)`
   - `tools/youtube-analytics/topic_strategy.py` → `generate_topic_strategy()`
   - Past POST-PUBLISH-ANALYSIS data stored in keywords.db

   ### Requirements
   - At least 1 past video of the same topic type must exist in the feedback database
   - Run `python tools/youtube-analytics/feedback.py backfill` to populate feedback data
   ```

2. In the script generation workflow section, add a step at the beginning:
   ```
   1. **Surface pre-script intelligence** (automatic)
      - Determine topic type from user input or project metadata
      - Call get_pre_script_insights(topic_type) to load past lessons
      - Display intelligence block to user before proceeding
   ```

3. Add a note that this references the tools from Phase 35 (feedback_queries.py, topic_strategy.py).

**Preserve all existing /script functionality.** Only add the new pre-script intelligence sections.
  </action>
  <verify>
Read `.claude/commands/script.md` and confirm: (1) PRE-SCRIPT INTELLIGENCE section exists, (2) workflow updated with pre-script step, (3) references feedback_queries and topic_strategy, (4) existing content unchanged.
  </verify>
  <done>
/script command updated with PRE-SCRIPT INTELLIGENCE section. Insights surface automatically before script generation begins. References get_pre_script_insights and topic_strategy tools.
  </done>
</task>

</tasks>

<verification>
1. `python tools/youtube-analytics/analyze.py --help` shows --script argument
2. `.claude/commands/analyze.md` contains --script documentation and SECTION-LEVEL RETENTION DIAGNOSTICS section
3. `.claude/commands/script.md` contains PRE-SCRIPT INTELLIGENCE section with automatic display instructions
4. analyze.py generate_section_diagnostics function imports retention_mapper and section_diagnostics
5. Existing analyze.py and /script functionality unchanged
</verification>

<success_criteria>
- analyze.py extended with section diagnostics (not replacing existing behavior)
- /analyze documented with --script flag
- /script documented with pre-script intelligence surfacing
- Integration uses optional imports with graceful fallback
- No breaking changes to existing commands
</success_criteria>

<output>
After completion, create `.planning/phases/35-actionable-analytics/35-03-SUMMARY.md`
</output>
