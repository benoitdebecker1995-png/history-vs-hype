---
phase: 09-post-publish-analysis
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - tools/youtube-analytics/analyze.py
  - .claude/commands/analyze.md
autonomous: true

must_haves:
  truths:
    - "Running /analyze VIDEO_ID produces complete analysis"
    - "Analysis is saved to video's project folder automatically"
    - "Analysis can also be saved to central location if project folder not found"
    - "User sees clear output with next steps"
  artifacts:
    - path: ".claude/commands/analyze.md"
      provides: "Slash command for post-publish analysis"
      min_lines: 40
    - path: "tools/youtube-analytics/analyze.py"
      provides: "Project folder discovery and file saving"
      exports: ["find_project_folder", "save_analysis"]
  key_links:
    - from: ".claude/commands/analyze.md"
      to: "tools/youtube-analytics/analyze.py"
      via: "calls run_analysis"
      pattern: "python analyze.py"
    - from: "tools/youtube-analytics/analyze.py"
      to: "video-projects/_IN_PRODUCTION/*"
      via: "find_project_folder search"
      pattern: "glob.*video-projects"
---

<objective>
Create the /analyze slash command and add project folder discovery + file saving to complete the post-publish analysis workflow.

Purpose: This is the user-facing interface. When user runs `/analyze VIDEO_ID`, Claude executes the analysis and saves results to the appropriate video project folder. This closes the loop from ANALYSIS-01 (command trigger) and ANALYSIS-06 (linked to project folder).

Output: Working /analyze command + automatic file saving to project folder
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-post-publish-analysis/09-CONTEXT.md

# Existing commands for reference
@.claude/commands/research.md
@.claude/commands/status.md

# Plan 02 creates analyze.py orchestrator
# Reference 09-RESEARCH.md Pattern 6 for project folder discovery
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project folder discovery and file saving to analyze.py</name>
  <files>tools/youtube-analytics/analyze.py</files>
  <action>
Enhance analyze.py with project folder discovery and automatic file saving.

**Function: find_project_folder(video_id=None, video_title=None)**
Find the video project folder matching video ID or title.

Search strategy (in order):
1. Search for video ID string in any .md file within video-projects/_IN_PRODUCTION/*/
2. Search for video ID in video-projects/_ARCHIVED/*/
3. If video_title provided, match title words against folder names
4. Return None if no match found

Implementation:
```python
import glob
import os

def find_project_folder(video_id=None, video_title=None):
    """
    Find project folder matching video ID or title.

    Returns: Path to project folder, or None
    """
    search_paths = [
        'video-projects/_IN_PRODUCTION/*/',
        'video-projects/_ARCHIVED/*/',
        'video-projects/_READY_TO_FILM/*/'
    ]

    # Strategy 1: Search for video ID in files
    if video_id:
        for pattern in search_paths:
            for folder in glob.glob(pattern):
                for filepath in glob.glob(f"{folder}*.md"):
                    try:
                        with open(filepath, 'r', encoding='utf-8') as f:
                            if video_id in f.read():
                                return folder.rstrip('/')
                    except:
                        continue

    # Strategy 2: Match title slug to folder name
    if video_title:
        import re
        slug_words = re.sub(r'[^a-z0-9]+', ' ', video_title.lower()).split()
        significant_words = [w for w in slug_words if len(w) > 3]

        for pattern in search_paths:
            for folder in glob.glob(pattern):
                folder_name = os.path.basename(folder.rstrip('/'))
                if any(word in folder_name.lower() for word in significant_words):
                    return folder.rstrip('/')

    return None
```

**Function: save_analysis(analysis, output_path=None)**
Save analysis to file.

Parameters:
- analysis: Complete analysis dict from run_analysis()
- output_path: Optional explicit path. If None, auto-discover project folder.

Behavior:
1. If output_path provided, save there
2. If not, try find_project_folder(video_id, title)
3. If project folder found, save as `{folder}/POST-PUBLISH-ANALYSIS.md`
4. If not found, save to `channel-data/analyses/POST-PUBLISH-ANALYSIS-{video_id}.md`
5. Create directory if needed

Return: `{'saved_to': path, 'project_folder_found': bool}`

**Update run_analysis():**
- Add optional parameter: `run_analysis(video_id_or_url, manual_ctr=None, save=False, output_path=None)`
- If save=True, call save_analysis() after generating analysis
- Return analysis dict with 'saved_to' field added

**Update CLI:**
- Add `--save` flag: `python analyze.py VIDEO_ID --save`
- Add `--output PATH` flag: `python analyze.py VIDEO_ID --save --output ./custom/path.md`
- Default (no --save): just print to stdout
- With --save: print to stdout AND save to file, show saved path

**File content format:**
Save as Markdown using format_analysis_markdown() output with header:
```markdown
# Post-Publish Analysis: {title}

**Video ID:** {video_id}
**Analyzed:** {timestamp}
**Saved to:** {path}

---

{full analysis markdown content}
```
  </action>
  <verify>
Run: `cd tools/youtube-analytics && python analyze.py wCFReiCGiks --save`
Expected: Analysis printed AND file saved (shows path). Check file exists.
Run: `python analyze.py wCFReiCGiks --save --output ./test-analysis.md`
Expected: Saves to specified path ./test-analysis.md
Cleanup: `rm ./test-analysis.md`
Run: `python -c "from analyze import find_project_folder; print(find_project_folder('wCFReiCGiks'))"`
Expected: Either a project folder path or None (confirms function exists)
  </verify>
  <done>
analyze.py has find_project_folder(), save_analysis(), --save and --output CLI flags, saves analysis to project folder or fallback location
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /analyze slash command</name>
  <files>.claude/commands/analyze.md</files>
  <action>
Create the /analyze slash command that triggers complete post-publish analysis.

**File: .claude/commands/analyze.md**

Structure (follow existing command patterns in .claude/commands/):

```markdown
---
description: Run complete post-publish analysis on any video
---

# /analyze - Post-Publish Video Analysis

Run comprehensive performance analysis for a published video with automated lessons.

## Usage

```
/analyze VIDEO_ID_OR_URL [--ctr VALUE]
```

**Arguments:**
- `VIDEO_ID_OR_URL`: YouTube video ID or full URL (required)
- `--ctr VALUE`: Manually provide CTR percentage from YouTube Studio (optional)

## What It Does

1. **Fetches performance data** via YouTube Analytics API:
   - Views, watch time, likes, comments, shares
   - Subscriber gain/loss
   - Retention curve with drop-off detection
   - CTR (if available via API, otherwise prompts for manual input)

2. **Calculates benchmarks** against channel average:
   - Compares this video vs. last 10 videos
   - Shows above/below/at average for each metric

3. **Analyzes comments** (Questions, Objections, Requests):
   - Fetches top 100 comments by relevance
   - Categorizes into actionable buckets

4. **Generates automated lessons**:
   - Observations about what the data shows
   - Actionable takeaways for future videos

5. **Saves analysis** to video's project folder:
   - Attempts to find matching project in `video-projects/`
   - Falls back to `channel-data/analyses/` if not found

## Example

```
/analyze wCFReiCGiks
/analyze https://youtu.be/wCFReiCGiks
/analyze wCFReiCGiks --ctr 4.2
```

## Output

Creates `POST-PUBLISH-ANALYSIS.md` containing:
- Quick summary (above/below benchmarks)
- Performance metrics table with comparisons
- Retention analysis with ASCII curve visualization
- All significant drop-off points with timestamps
- Categorized comments (full list under each category)
- Lessons: observations and actionable takeaways

## Requirements

- YouTube Analytics API configured (Phase 7)
- OAuth token valid (`tools/youtube-analytics/credentials/token.json`)

## Note on CTR

CTR may not be available via YouTube Analytics API (known limitation). If unavailable:
- Analysis shows "Check YouTube Studio manually"
- Use `--ctr VALUE` to manually provide the CTR percentage
- CTR can be found in: YouTube Studio > Analytics > Reach tab
```

**Execution behavior:**
When user runs `/analyze VIDEO_ID`, Claude should:
1. Navigate to tools/youtube-analytics/
2. Run: `python analyze.py VIDEO_ID --save --markdown`
3. Display the markdown output to user
4. Confirm where file was saved
5. If CTR unavailable, suggest: "Add CTR with: /analyze VIDEO_ID --ctr VALUE"
  </action>
  <verify>
Check file exists: `ls -la .claude/commands/analyze.md`
Check format: File has frontmatter with description, clear usage section, examples
Manual test: Run `/analyze wCFReiCGiks` in Claude Code and verify it triggers analysis
  </verify>
  <done>
/analyze command exists, documents full workflow, shows usage examples, explains CTR fallback
  </done>
</task>

<task type="auto">
  <name>Task 3: Create analyses directory and verify full workflow</name>
  <files>channel-data/analyses/.gitkeep</files>
  <action>
Ensure fallback save location exists and verify complete workflow.

**Create fallback directory:**
```bash
mkdir -p channel-data/analyses
touch channel-data/analyses/.gitkeep
```

This ensures analyses can be saved even when no project folder match is found.

**Full workflow verification:**
Run a complete analysis cycle to verify all components work together:

1. Test with video that has project folder:
   - Find a video ID that exists in video-projects/ (search for any .md files with video IDs)
   - Run analysis, verify it saves to that project folder

2. Test with video that has NO project folder:
   - Use a video ID with no matching project (e.g., a random public video)
   - Run analysis, verify it saves to channel-data/analyses/

3. Test manual CTR input:
   - Run with --ctr flag, verify CTR appears in output as "manual"

4. Test error handling:
   - Run with invalid video ID, verify graceful error message

**Update .gitignore if needed:**
- Ensure channel-data/analyses/*.md files are NOT gitignored (analysis files should be tracked)
- Check existing .gitignore patterns
  </action>
  <verify>
Directory exists: `ls -la channel-data/analyses/`
Full workflow: `cd tools/youtube-analytics && python analyze.py wCFReiCGiks --save --markdown`
Expected: Analysis displays, file saved (either to project folder or channel-data/analyses/)
Invalid ID test: `python analyze.py INVALID123 --save`
Expected: Graceful error handling, no crash
  </verify>
  <done>
Fallback directory exists, full workflow tested end-to-end, error handling verified, /analyze command ready for use
  </done>
</task>

</tasks>

<verification>
1. `/analyze VIDEO_ID` in Claude Code triggers analysis workflow
2. Analysis saved to correct location:
   - Project folder if video ID found in project files
   - channel-data/analyses/ if no project match
3. Manual CTR works: `/analyze VIDEO_ID --ctr 4.2`
4. Error handling graceful for invalid video IDs
5. Full markdown report includes all sections (summary, metrics, retention, comments, lessons)
</verification>

<success_criteria>
- ANALYSIS-01 complete: `/analyze` command triggers full analysis
- ANALYSIS-06 complete: Analysis linked to video project folder (or fallback)
- All 6 ANALYSIS requirements satisfied
- Phase 9 complete and ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/09-post-publish-analysis/09-03-SUMMARY.md`
</output>
