---
phase: 41-verification-production-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/verify.md
  - tools/translation/verification.py
autonomous: true
requirements: [VERF-01, VERF-02, VERF-03]

must_haves:
  truths:
    - "User can run /verify --translation to verify translated documents"
    - "System checks translation completeness (cross-check done, annotations present)"
    - "System compares translation against scholarly descriptions when provided"
    - "Verification produces tiered verdict (GREEN/YELLOW/RED) for filming decisions"
  artifacts:
    - path: "tools/translation/verification.py"
      provides: "Translation verification engine with completeness checks and scholarly comparison"
      min_lines: 200
      exports: ["TranslationVerifier", "verify_translation"]
    - path: ".claude/commands/verify.md"
      provides: "/verify --translation mode documentation and workflow"
      contains: "--translation"
  key_links:
    - from: ".claude/commands/verify.md"
      to: "tools/translation/verification.py"
      via: "slash command invokes verification module"
      pattern: "python.*verification\\.py"
    - from: "tools/translation/verification.py"
      to: "tools/translation/cross_checker.py"
      via: "reads cross-check results from Phase 40"
      pattern: "CrossChecker|cross_check"
    - from: "tools/translation/verification.py"
      to: "tools/translation/legal_annotator.py"
      via: "reads annotation results from Phase 40"
      pattern: "LegalAnnotator|annotate"
---

<objective>
Integrate translation verification into /verify command, allowing users to audit translation quality before filming.

Purpose: Translation verification catches discrepancies, missing annotations, and scholarly misalignments before production investment.
Output: /verify --translation mode with GREEN/YELLOW/RED verdicts and TRANSLATION-VERIFICATION.md reports.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/commands/verify.md
@tools/translation/cross_checker.py
@tools/translation/legal_annotator.py
@tools/translation/surprise_detector.py
@.planning/phases/41-verification-production-integration/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create translation verification module</name>
  <files>tools/translation/verification.py</files>
  <action>
Create TranslationVerifier class with two verification modes:

**Mode 1: Audit existing output (default)**
- Read translation output file (from Phase 40 formatter)
- Check completeness:
  - Cross-check section exists (not [PENDING])
  - Legal annotation section exists with at least 1 term annotated
  - Surprise section exists (if --skip-surprise not used during translation)
  - No [NEEDS CROSS-CHECK] placeholders remain
- Parse cross-check discrepancies and count HIGH/MEDIUM/LOW severity flags
- Parse annotation coverage (% of legal terms with definitions)

**Mode 2: Re-run fresh (--fresh flag)**
- Re-run CrossChecker against original + translation
- Re-run LegalAnnotator on original text
- Re-run SurpriseDetector if narrative provided
- Compare fresh results against existing output (if exists)

**Scholarly comparison (both modes):**
- If --scholarly-summary FILE provided: parse user's scholarly description and compare against translation clauses
  - Extract key provisions from scholarly summary
  - Check if translation contains corresponding clauses
  - Flag omissions or contradictions
- If no file provided: use Claude API to compare translation against training knowledge
  - Prompt: "Based on your knowledge of [document name], what are the key provisions scholars typically cite?"
  - Compare against translation output
  - Flag if major scholarly interpretations missing

**Verdict calculation:**
- GREEN: 0 HIGH discrepancies, 0 annotation gaps, scholarly alignment confirmed
- YELLOW: 1-2 MEDIUM discrepancies, minor annotation gaps (<10% coverage), or scholarly summary has 1-2 minor omissions
- RED: Any HIGH discrepancies, >10% annotation gap, or scholarly summary contradicts translation

**Report generation:**
- Always write full report to TRANSLATION-VERIFICATION.md in project folder
- Print condensed summary to terminal (verdict + top 3 issues)

Return dict with 'verdict', 'report_path', 'issues', 'summary'.
  </action>
  <verify>
python tools/translation/verification.py --help shows usage
python -c "from tools.translation.verification import TranslationVerifier; v = TranslationVerifier(); print('Import successful')"
  </verify>
  <done>TranslationVerifier class exists with verify_translation() method accepting translation_file, mode='audit'|'fresh', scholarly_file=None. Returns dict with verdict (GREEN/YELLOW/RED).</done>
</task>

<task type="auto">
  <name>Integrate /verify --translation mode</name>
  <files>.claude/commands/verify.md</files>
  <action>
Add new section after "NOTEBOOKLM CITATION EXTRACTION" and before "SIMPLIFICATION DETECTION ONLY":

## TRANSLATION VERIFICATION (`--translation`)

Verify translated documents before filming to catch discrepancies and missing annotations.

### Usage

```
/verify --translation [project]           # Audit existing translation output
/verify --translation [project] --fresh   # Re-run cross-check and annotation
/verify --translation [project] --scholarly-summary FILE  # Compare against scholarly description
```

### Modes

**Audit mode (default):** Reads existing translation output and checks completeness.
- Cross-check results present
- Legal annotations exist
- Surprise detection complete (if enabled)
- No pending placeholders

**Fresh mode (--fresh):** Re-runs cross-checker, annotator, and surprise detector from scratch. Use when:
- Translation was updated after initial cross-check
- Different DeepL/Claude versions want to be tested
- Narrative baseline changed for surprise detection

**Scholarly comparison:** Optional verification against academic descriptions of the document.
- `--scholarly-summary FILE`: User provides file with scholarly description (e.g., "Article 3 establishes X, Article 5 prohibits Y")
- System compares translation output against scholarly baseline
- Flags omissions or contradictions

### Process

1. Locate translation output file in project folder (formatted output from Phase 40)
2. Run TranslationVerifier in specified mode
3. Generate TRANSLATION-VERIFICATION.md with full findings
4. Print condensed summary to terminal

### Output Format

Terminal summary:
```
TRANSLATION VERIFICATION: [Document Name]
VERDICT: GREEN / YELLOW / RED

Top issues:
1. [Issue description]
2. [Issue description]
3. [Issue description]

Full report: video-projects/[project]/TRANSLATION-VERIFICATION.md
```

Full report sections:
- **Verdict:** GREEN/YELLOW/RED with reasoning
- **Completeness Check:** Cross-check status, annotation coverage, surprise detection status
- **Discrepancy Analysis:** HIGH/MEDIUM/LOW severity issues from cross-check
- **Annotation Coverage:** % of legal terms with definitions, missing terms list
- **Scholarly Comparison:** (if --scholarly-summary used) Alignment check, omissions, contradictions
- **Recommendation:** Proceed to filming / Revise translation / Major issues - retranslate

### Verdict Interpretation

| Verdict | Meaning | Next Step |
|---------|---------|-----------|
| GREEN | No significant issues | Proceed to script generation |
| YELLOW | Minor discrepancies or gaps | Review flagged sections, decide if acceptable |
| RED | Significant problems | Revise translation before filming |

### Output Location

`video-projects/[project]/TRANSLATION-VERIFICATION.md`

### After Completion

**When verification returns GREEN:**
> "Translation verified! No significant issues found.
> Next steps:
> 1. `/script --document-mode` - Generate document-structured script
> 2. `/prep --split-screen` - Create split-screen edit guide
>
> Ready to write script? Run `/script --document-mode`"

**When verification returns YELLOW:**
> "Translation verification found minor issues (see TRANSLATION-VERIFICATION.md).
> Review flagged sections and decide if acceptable for filming.
> To proceed: `/script --document-mode`"

**When verification returns RED:**
> "Translation verification found significant problems.
> See TRANSLATION-VERIFICATION.md for details.
> Revise translation before proceeding to script generation."
  </action>
  <verify>
Grep for "--translation" in .claude/commands/verify.md returns matches
Grep for "TRANSLATION VERIFICATION" in .claude/commands/verify.md returns section heading
Grep for "TranslationVerifier" in .claude/commands/verify.md returns implementation reference
  </verify>
  <done>/verify command documentation includes --translation mode with audit/fresh modes, scholarly comparison option, verdict interpretation table, and post-verification workflow guidance.</done>
</task>

</tasks>

<verification>
- [ ] User can run /verify --translation on project with Phase 40 translation output
- [ ] Audit mode checks completeness without re-running tools
- [ ] Fresh mode re-runs cross-checker and annotator
- [ ] Scholarly comparison works when user provides summary file
- [ ] Verdict calculation produces GREEN/YELLOW/RED based on defined thresholds
- [ ] TRANSLATION-VERIFICATION.md written to project folder
- [ ] Terminal prints condensed summary
- [ ] Documentation in verify.md explains all modes and flags
</verification>

<success_criteria>
1. TranslationVerifier class accepts translation file path and returns verdict dict
2. /verify --translation mode documented with usage examples
3. Audit mode completes in <5 seconds (reads existing output, no API calls)
4. Fresh mode re-runs cross-check and annotation (uses Claude API)
5. Scholarly comparison flags omissions when user provides reference summary
6. GREEN verdict means zero HIGH discrepancies and >90% annotation coverage
7. YELLOW verdict means 1-2 MEDIUM issues or 80-90% annotation coverage
8. RED verdict means any HIGH discrepancies or <80% annotation coverage
</success_criteria>

<output>
After completion, create `.planning/phases/41-verification-production-integration/41-01-SUMMARY.md`
</output>
