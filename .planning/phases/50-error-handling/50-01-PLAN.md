---
phase: 50-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/youtube_analytics/feedback_queries.py
  - tools/youtube_analytics/pattern_synthesizer_v2.py
  - tools/youtube_analytics/retention_scorer.py
  - tools/youtube_analytics/topic_strategy.py
  - tools/youtube_analytics/backfill.py
  - tools/youtube_analytics/benchmarks.py
  - tools/youtube_analytics/analyze.py
  - tools/youtube_analytics/video_report.py
autonomous: true
requirements:
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "No bare except: blocks exist in tools/youtube_analytics/"
    - "No except Exception: pass blocks exist in tools/youtube_analytics/"
    - "All exception handlers use specific exception types"
    - "video_report.py uses proper absolute imports (deferred Phase 48 fix)"
  artifacts:
    - path: "tools/youtube_analytics/feedback_queries.py"
      provides: "5 bare excepts replaced with StatisticsError, ValueError, IndexError, Exception as e"
    - path: "tools/youtube_analytics/pattern_synthesizer_v2.py"
      provides: "2 bare excepts + 2 broad excepts replaced with json.JSONDecodeError, TypeError"
    - path: "tools/youtube_analytics/retention_scorer.py"
      provides: "1 bare except + 3 broad excepts replaced with json.JSONDecodeError, TypeError"
    - path: "tools/youtube_analytics/topic_strategy.py"
      provides: "1 bare except + 1 broad except replaced with AttributeError, TypeError, KeyError"
    - path: "tools/youtube_analytics/backfill.py"
      provides: "3 broad excepts replaced with sqlite3.OperationalError, sqlite3.Error, KeyError, ValueError"
    - path: "tools/youtube_analytics/benchmarks.py"
      provides: "6 broad excepts replaced with sqlite3.Error"
    - path: "tools/youtube_analytics/analyze.py"
      provides: "3 broad excepts replaced with OSError, sqlite3.Error"
    - path: "tools/youtube_analytics/video_report.py"
      provides: "Bare imports fixed to absolute tools.youtube_analytics.* imports"
  key_links:
    - from: "tools/youtube_analytics/feedback_queries.py"
      to: "statistics.StatisticsError"
      via: "import at top of file"
      pattern: "from statistics import StatisticsError"
---

<objective>
Replace all bare `except:` and `except Exception: pass` blocks in youtube_analytics/ with specific exception types.

Purpose: ERR-01 requires zero bare excepts in tools/. The youtube_analytics/ package has the highest concentration (10 bare excepts across 4 files + 15 broad except-pass blocks across 4 more files). Also fixes the deferred video_report.py import issue from Phase 48.

Output: 8 files modified with specific exception types replacing all bare/broad catch blocks.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-error-handling/50-RESEARCH.md

The research file contains exact line numbers and replacement patterns for every bare except and broad except block. Use Pattern 1 (bare except → specific type) and Pattern 2 (except Exception: pass → specific type) from the research.

**Key rules:**
- `from statistics import StatisticsError` for `mean()` on empty list
- `json.JSONDecodeError, TypeError` for `json.loads()` calls
- `sqlite3.OperationalError` for ALTER TABLE (column may already exist)
- `sqlite3.Error` for general DB query failures
- `ValueError, IndexError` for float parsing / list access
- `AttributeError, TypeError, KeyError` for dict/object access
- `except Exception as e:` (with capture) acceptable when wrapping external function calls with truly unknown exception types — add comment explaining why broad is acceptable
- Every `except Exception as e:` must capture `as e` even if `e` is not used yet (Phase 51 will add logging)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bare excepts in feedback_queries.py, pattern_synthesizer_v2.py, retention_scorer.py, topic_strategy.py</name>
  <files>
    tools/youtube_analytics/feedback_queries.py
    tools/youtube_analytics/pattern_synthesizer_v2.py
    tools/youtube_analytics/retention_scorer.py
    tools/youtube_analytics/topic_strategy.py
  </files>
  <action>
Fix all 9 bare `except:` blocks in these 4 files with specific exception types. Also fix 6 `except Exception: pass` blocks in pattern_synthesizer_v2.py, retention_scorer.py, and topic_strategy.py.

**feedback_queries.py (5 bare excepts):**
1. Lines ~267-270: `mean()` on empty list → `except StatisticsError:` (add `from statistics import StatisticsError` at top)
2. Lines ~275-278: Same pattern, second `mean()` call → `except StatisticsError:`
3. Lines ~530: `extract_winning_patterns()` → `except Exception as e:` with comment "# External function — unknown exception types; Phase 51 adds logger"
4. Lines ~604-607: `float(words[i-1]) / 100` → `except (ValueError, IndexError):`
5. Lines ~641-644: `generate_topic_strategy()` → `except Exception as e:` with comment "# External function — graceful degradation; Phase 51 adds logger"

Read the file first to get exact line numbers. Search for `except:` to find all 5 locations.

**pattern_synthesizer_v2.py (2 bare excepts + 2 broad excepts):**
1. Line ~355: `json.loads(examples)` → `except (json.JSONDecodeError, TypeError):`
2. Line ~493: `json.loads(examples)` → `except (json.JSONDecodeError, TypeError):`
3-4. Find `except Exception: pass` or `except Exception:` blocks — narrow to specific types based on what the try block does

Read the file first. Search for `except:` and `except Exception`.

**retention_scorer.py (1 bare except + 3 broad excepts):**
1. Line ~300: `json.loads(lessons)` → `except (json.JSONDecodeError, TypeError):`
2-4. Find `except Exception: pass` blocks — narrow to specific types

**topic_strategy.py (1 bare except + 1 broad except):**
1. Line ~144: dict `.get()` and `.extend()` → `except (AttributeError, TypeError, KeyError):`
2. Find `except Exception: pass` — narrow to specific type

For ALL changes: if the existing `except` already has `as e`, keep it. If not, add `as e` for any broad `except Exception` conversions. Add a comment explaining why broad is acceptable if `Exception` is kept.
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && grep -rn "except:" tools/youtube_analytics/feedback_queries.py tools/youtube_analytics/pattern_synthesizer_v2.py tools/youtube_analytics/retention_scorer.py tools/youtube_analytics/topic_strategy.py | grep -v "except " | wc -l</automated>
  </verify>
  <done>
- Zero bare `except:` in these 4 files (grep confirms)
- Zero `except Exception: pass` without specific type or `as e` capture
- StatisticsError imported in feedback_queries.py
- All json.loads blocks catch json.JSONDecodeError, TypeError
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix broad excepts in backfill.py, benchmarks.py, analyze.py + fix video_report.py imports</name>
  <files>
    tools/youtube_analytics/backfill.py
    tools/youtube_analytics/benchmarks.py
    tools/youtube_analytics/analyze.py
    tools/youtube_analytics/video_report.py
  </files>
  <action>
Fix all `except Exception: pass` blocks in these 3 files with specific exception types. Also fix the deferred import issue in video_report.py.

**backfill.py (3 broad excepts):**
1. ALTER TABLE operations → `except sqlite3.OperationalError:` with comment "# Non-fatal: column may already exist"
2-3. Read file and find remaining `except Exception` blocks. For DB operations → `except sqlite3.Error:`. For mixed operations → `except (sqlite3.Error, KeyError, ValueError) as e:`

**benchmarks.py (6 broad excepts):**
All are DB query enrichment operations. Replace each `except Exception: pass` with `except sqlite3.Error:` and keep `pass` with comment "# Non-blocking: metadata enrichment only"

Read file first — find all 6 `except Exception` locations.

**analyze.py (3 broad excepts):**
Replace with `except (OSError, sqlite3.Error) as e:` or the appropriate specific type based on what the try block does. Read file to determine correct types.

**video_report.py (import fix — deferred from Phase 48):**
Replace bare imports at the top of the file:
- `from metrics import ...` → `from tools.youtube_analytics.metrics import ...`
- `from retention import ...` → `from tools.youtube_analytics.retention import ...`  (or whatever the actual imports are)
- `from ctr import ...` → `from tools.youtube_analytics.ctr import ...` (or similar)

Read file first to find the exact import lines. Use the same `tools.youtube_analytics.*` absolute import pattern established in Phase 48.
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && grep -rn "except Exception" tools/youtube_analytics/backfill.py tools/youtube_analytics/benchmarks.py tools/youtube_analytics/analyze.py | grep -c "pass$" && python -c "import tools.youtube_analytics.video_report" 2>&1 | head -5</automated>
  </verify>
  <done>
- Zero `except Exception: pass` blocks in backfill.py, benchmarks.py, analyze.py
- All exception handlers use sqlite3.Error, sqlite3.OperationalError, OSError, or other specific types
- video_report.py uses absolute `tools.youtube_analytics.*` imports (no bare `from metrics import`)
  </done>
</task>

</tasks>

<verification>
```bash
# ERR-01 gate for youtube_analytics/
cd "G:/History vs Hype"
echo "=== Bare excepts (should be 0) ==="
grep -rn "except:" tools/youtube_analytics/ --include="*.py" | grep -v "except " | wc -l
echo "=== Broad except-pass (should be 0) ==="
grep -rn "except Exception:" tools/youtube_analytics/ --include="*.py" -A 1 | grep "pass$" | wc -l
echo "=== Import check ==="
python -c "from tools.youtube_analytics import video_report; print('OK')"
```
</verification>

<success_criteria>
1. `grep -rn "except:" tools/youtube_analytics/ --include="*.py" | grep -v "except "` returns zero lines
2. `grep -rn "except Exception:" tools/youtube_analytics/ --include="*.py" -A 1 | grep "pass$"` returns zero lines
3. `python -c "from tools.youtube_analytics import video_report"` succeeds without ImportError
4. All bare excepts replaced with specific exception types documented in the research
</success_criteria>

<output>
After completion, create `.planning/phases/50-error-handling/50-01-SUMMARY.md`
</output>
