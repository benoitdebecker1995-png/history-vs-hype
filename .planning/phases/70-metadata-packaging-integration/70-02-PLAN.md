---
phase: 70-metadata-packaging-integration
plan: "02"
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - tools/production/metadata.py
  - tools/production/title_generator.py
  - .claude/commands/publish.md
  - tests/unit/test_metadata_bundle.py
  - tests/unit/test_title_generator.py
autonomous: true
requirements: [META-03]

must_haves:
  truths:
    - "Title table shows a Coherence column (3/3, 2/3, 1/3) for each candidate"
    - "Coherence detail section only appears for mismatched candidates"
    - "Coherence does NOT influence title ranking order"
    - "generate_metadata_draft() accepts topic_type parameter and passes it through"
    - "Output section order is: Titles, Description, Chapters, Tags, Thumbnail Concepts, Coherence Check"
    - "/publish command documents --topic and --thumbs flags"
  artifacts:
    - path: "tools/production/metadata.py"
      provides: "_coherence_check(), updated generate_metadata_draft() with topic_type param and new output order"
      contains: "_coherence_check"
    - path: "tools/production/title_generator.py"
      provides: "format_title_candidates() with optional coherence column"
      contains: "coherence"
    - path: ".claude/commands/publish.md"
      provides: "Documentation for --topic and --thumbs flags"
      contains: "--topic"
    - path: "tests/unit/test_metadata_bundle.py"
      provides: "Tests for META-03 coherence check"
    - path: "tests/unit/test_title_generator.py"
      provides: "Test for format_title_candidates coherence column"
  key_links:
    - from: "tools/production/metadata.py"
      to: "tools/production/title_generator.py"
      via: "format_title_candidates(candidates, thumbnail_concepts=..., desc_first_line=...)"
      pattern: "format_title_candidates.*thumbnail_concepts"
    - from: "tools/production/metadata.py"
      to: "_coherence_check"
      via: "called at end of generate_metadata_draft"
      pattern: "_coherence_check"
---

<objective>
Add metadata coherence check (META-03), wire generate_metadata_draft() with topic_type passthrough and new output order, update format_title_candidates() with optional coherence column, and document new flags in /publish command.

Purpose: Users see at a glance which title candidates are coherent with their thumbnail concepts and description. The metadata bundle output follows the locked section order. The --topic flag is consistent with Phase 67/69 patterns.

Output: _coherence_check() method, updated generate_metadata_draft() signature and output assembly, format_title_candidates() with coherence column, updated publish.md documentation.
</objective>

<execution_context>
@C:/Users/Benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-metadata-packaging-integration/70-CONTEXT.md
@.planning/phases/70-metadata-packaging-integration/70-RESEARCH.md
@.planning/phases/70-metadata-packaging-integration/70-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will exist when this plan runs) -->

From tools/production/metadata.py (after Plan 01):
```python
from tools.title_scorer import CLICKBAIT_PATTERNS, ALLOWED_ACRONYMS

class MetadataGenerator:
    def generate_metadata_draft(self, sections, entities, timings) -> str: ...
    def _generate_description(self, sections, entities, timings, material=None) -> str: ...
    def _generate_chapters(self, timings) -> str: ...
    def _generate_tags(self, entities, sections) -> str: ...
    def _generate_title_variants(self, sections, entities, topic_type=None) -> list: ...
    def _generate_thumbnail_concepts(self, material, entities, topic_type=None) -> str: ...
    def _extract_citations(self, sections) -> List[str]: ...
```

From tools/production/title_generator.py (current signature):
```python
def format_title_candidates(candidates: List[Dict[str, Any]]) -> str:
    # Produces "## Title Candidates (ranked by score)" + markdown table
    # Columns: # | Title | Score | Grade | Pattern
    # Appends [warning] lines for hard_rejects below table
```

From tools/production/title_generator.py:
```python
class TitleMaterialExtractor:
    def extract_from_sections(self, sections) -> dict:
        # Returns: {'entities': [...], 'documents': [...], 'numbers': [...], ...}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coherence check + format_title_candidates coherence column</name>
  <files>tools/production/metadata.py, tools/production/title_generator.py, tests/unit/test_metadata_bundle.py, tests/unit/test_title_generator.py</files>
  <behavior>
    - Test: _coherence_check() with title containing "Spain", thumbnail containing "Spain", desc containing "Spain" returns 3/3 for that candidate
    - Test: _coherence_check() with title containing "Spain", thumbnail containing "Spain", desc NOT containing "Spain" returns 2/3 for that candidate
    - Test: _coherence_check() detail section only present for candidates with count < 3
    - Test: _coherence_check() with no candidates returns empty string
    - Test: _coherence_check() with None primary entity returns warning message, not crash
    - Test: format_title_candidates() with thumbnail_concepts=None produces table WITHOUT Coherence column (backward compat)
    - Test: format_title_candidates() with thumbnail_concepts and desc_first_line produces table WITH Coherence column
    - Test: format_title_candidates() coherence column does NOT change sort order (candidates still sorted by score descending)
  </behavior>
  <action>
1. Add _coherence_check(self, candidates, thumbnail_concepts_text, description) -> str to MetadataGenerator:
   - Use self._primary_entity (set during generate_metadata_draft from TitleMaterialExtractor top entity, filtering out entity_type=='date' and entities < 4 chars)
   - If _primary_entity is None, return "warning: No primary entity detected for coherence check."
   - Parse thumbnail_concepts_text into list of concept strings using regex (split on "**Concept" headers)
   - For each candidate: check primary_entity.lower() in title.lower(), in desc_first_line.lower(), in any concept.lower()
   - Count matches. Badge: 3/3=checkmark, 2/3=warning, 1/3 or 0/3=cross
   - Store coherence result in candidate dict: candidate["coherence"] = "N/3 badge"
   - Build detail section only for candidates where count < 3, naming which pair is misaligned and what entity is missing
   - Return formatted markdown string: "## Coherence Check" header + detail

2. Update format_title_candidates() in title_generator.py:
   - Add optional params: thumbnail_concepts: Optional[List[str]] = None, desc_first_line: Optional[str] = None
   - When both are None (backward compat): produce table exactly as before (# | Title | Score | Grade | Pattern)
   - When provided: add "Coherence" column using candidate.get("coherence", "---")
   - Sort order remains score descending regardless of coherence (annotation only per CONTEXT.md)
   - Warning lines still appended below table as before

3. Add tests to test_metadata_bundle.py for _coherence_check behaviors.
4. Add test to test_title_generator.py for format_title_candidates with coherence column + backward compat.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_metadata_bundle.py tests/unit/test_title_generator.py -x -q</automated>
  </verify>
  <done>_coherence_check() produces correct counts and detail sections. format_title_candidates() shows Coherence column when params provided, omits it when not. Backward compatibility preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generate_metadata_draft() + output order + publish.md update</name>
  <files>tools/production/metadata.py, .claude/commands/publish.md, tests/unit/test_metadata_bundle.py</files>
  <action>
1. Update generate_metadata_draft() signature:
   - Add topic_type: Optional[str] = None parameter
   - At top: compute material once via TitleMaterialExtractor().extract_from_sections(sections)
   - Set self._primary_entity from material: first entity entry that isn't a date and has 4+ chars, else None
   - Pass topic_type through to _generate_title_variants(), _generate_thumbnail_concepts()
   - Pass material to _generate_description()

2. Assemble output in locked section order (per CONTEXT.md):
   - title_candidates = self._generate_title_variants(sections, entities, topic_type)
   - description = self._generate_description(sections, entities, timings, material)
   - chapters = self._generate_chapters(timings)
   - tags = self._generate_tags(entities, sections)
   - thumbnail_concepts = self._generate_thumbnail_concepts(material, entities, topic_type)
   - coherence = self._coherence_check(title_candidates, thumbnail_concepts, description)
   - Re-format title table with coherence column:
     format_title_candidates(title_candidates, thumbnail_concepts=[parsed concepts], desc_first_line=description.splitlines()[0])
   - Final assembly: Titles section + Description section + Chapters + Tags + Thumbnail Concepts + Coherence Check + VidIQ Research Notes placeholder

3. Update .claude/commands/publish.md:
   - Add --topic flag to the Flags table: "Override topic type for thumbnail pattern selection and coherence check (territorial, ideological, political_fact_check)"
   - Add --thumbs flag to the Flags table (if not already present): "Generate thumbnail concepts only"
   - In the metadata generation section, note that thumbnail concepts are now auto-generated with validation badges
   - Remove or update the placeholder language about thumbnail strategy notes

4. Add integration test: generate_metadata_draft() with minimal sections + entities + timings produces output containing all 6 sections in correct order: "Title Candidates", "Description", "Chapters", "Tags", "Thumbnail Concepts", "Coherence Check".
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_metadata_bundle.py -x -q</automated>
  </verify>
  <done>generate_metadata_draft() accepts topic_type, outputs all sections in locked order, coherence column appears in title table, publish.md documents --topic and --thumbs flags</done>
</task>

</tasks>

<verification>
python -m pytest tests/unit/test_metadata_bundle.py tests/unit/test_title_generator.py -x -q
</verification>

<success_criteria>
- Coherence column shows N/3 with badge in title table
- Coherence detail section only for mismatched candidates
- Coherence does not change sort order
- format_title_candidates() backward compatible (no coherence column when called with 1 arg)
- generate_metadata_draft() accepts topic_type, assembles output in locked section order
- TitleMaterialExtractor called once at top, result reused for description + thumbnails + coherence
- publish.md documents --topic and --thumbs flags
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/70-metadata-packaging-integration/70-02-SUMMARY.md`
</output>
