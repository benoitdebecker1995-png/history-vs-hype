---
phase: 49-dead-code-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - tools/youtube_analytics/patterns.py
  - tools/discovery/database.py
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "No public function with zero callers remains in patterns.py"
    - "No private helper with zero callers remains in database.py"
    - "All stale imports and references cleaned after function removal"
    - "Import smoke tests pass for all modified modules"
  artifacts:
    - path: "tools/youtube_analytics/patterns.py"
      provides: "Patterns module with only actively-called functions"
    - path: "tools/discovery/database.py"
      provides: "KeywordDB class with only actively-called methods"
  key_links:
    - from: "tools/youtube_analytics/patterns.py"
      to: ".claude/commands/patterns.md"
      via: "python -m tools.youtube_analytics.patterns CLI"
      pattern: "generate_.*_report"
    - from: "tools/discovery/database.py"
      to: "tools/discovery/__init__.py"
      via: "public API exports"
      pattern: "__all__"
---

<objective>
Audit large modules for unreachable functions and remove any with zero callers across the codebase.

Purpose: Satisfy CLEAN-02 (unused functions identified and removed). Focus on patterns.py (2,070 lines) and database.py (2,927 lines) — the two largest modules with highest refactor history. Extend audit to other large modules at discretion.

Output: Leaner modules with only actively-used code. Commit messages document what was removed.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dead-code-cleanup/49-RESEARCH.md
@.planning/phases/49-dead-code-cleanup/49-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and clean patterns.py</name>
  <files>tools/youtube_analytics/patterns.py</files>
  <action>
    **Context:** patterns.py (2,070 lines) is the backend for `/patterns` command. The `__main__` block calls `generate_all_reports()`, `generate_topic_report()`, `generate_title_report()`, and `generate_monthly_summary()`. Internal helpers are called within patterns.py itself.

    1. Read `tools/youtube_analytics/patterns.py` and list ALL top-level functions (def at module level, not inside a class).

    2. For each function, run a caller-count check:
       ```bash
       grep -rn "function_name" tools/ --include="*.py" | grep -v "def function_name\|#.*function_name"
       ```
       Count results. Functions called only from within patterns.py itself count as having callers (they're internal helpers used by the report generators). Functions in the `__main__` block also count as having callers.

    3. For functions with ZERO callers anywhere (not even in patterns.py itself — truly orphaned), remove them.

    4. After removal, check `tools/youtube_analytics/__init__.py` for any stale exports referencing removed functions. Clean them.

    5. Leave 1-caller functions alone (per user's discretion guidance — flag, don't delete).

    6. Run import smoke test: `python -c "from tools.youtube_analytics.patterns import generate_topic_report; print('OK')"`
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && python -c "from tools.youtube_analytics.patterns import generate_topic_report, generate_title_report, generate_monthly_summary; print('OK')"</automated>
    <manual>Confirm no orphaned functions remain by reviewing the audit results</manual>
  </verify>
  <done>All functions in patterns.py have at least one caller. Any zero-caller functions removed. Imports verified clean.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and clean database.py + other large modules</name>
  <files>tools/discovery/database.py</files>
  <action>
    **Context:** database.py (2,927 lines, 55+ methods in KeywordDB class). Research found all public methods have active callers. Focus on private helpers (`_ensure_*`, `_backup_*`, `_migrate_*` methods) that may be orphaned after schema reached v29.

    1. Read `tools/discovery/database.py` and identify all private methods (those starting with `_`).

    2. For each private method, check if it has callers:
       - Within the class itself (called from other methods)
       - From outside the class (called by other modules)
       Use grep: `grep -rn "_method_name" tools/ --include="*.py"`

    3. Special attention to migration-related methods:
       - `_backup_database()` — called by `_ensure_variant_tables()` during v27 migration
       - `_ensure_variant_tables()` — called by `_ensure_connection()` or `_migrate_to_v*()`
       - `_migrate_to_v*()` methods for versions below current (v29)
       These ARE still needed: fresh databases start at v0 and migrate through all versions. Do NOT delete migration methods.

    4. Remove only methods with truly zero callers (not called by any other method, not part of migration chain).

    5. After removal, verify `tools/discovery/__init__.py` exports are still valid.

    6. **Additional modules to audit** (Claude's discretion based on size):
       - `tools/youtube_analytics/backfill.py` (971 lines) — check for any functions orphaned after _longform JSON deletion in Plan 01
       - `tools/youtube_analytics/analyze.py` (1,429 lines) — quick scan for orphaned helpers
       - Any other module over 500 lines that Claude identifies as having dead code during the audit

    7. For each removal: clean stale imports in other files, docstring mentions, and comments (per user decision).

    8. Run import smoke tests:
       ```bash
       python -c "from tools.discovery.database import KeywordDB; print('OK')"
       python -c "from tools.youtube_analytics.backfill import run_backfill; print('OK')"
       python -c "from tools.youtube_analytics.analyze import run_analysis; print('OK')"
       ```
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && python -c "from tools.discovery.database import KeywordDB; db = KeywordDB(':memory:'); print('OK')" && python -c "from tools.youtube_analytics import patterns; print('OK')"</automated>
    <manual>Confirm audit covered database.py private methods and at least one additional large module</manual>
  </verify>
  <done>All audited modules contain only actively-called functions. Zero-caller private methods removed (if any found). Stale imports cleaned. Smoke tests pass.</done>
</task>

</tasks>

<verification>
1. `python -c "from tools.youtube_analytics.patterns import generate_topic_report; print('OK')"` passes
2. `python -c "from tools.discovery.database import KeywordDB; print('OK')"` passes
3. `python -c "from tools.youtube_analytics.backfill import run_backfill; print('OK')"` passes
4. No function in patterns.py or database.py has zero callers across the entire codebase
5. All `__init__.py` exports reference existing functions only
</verification>

<success_criteria>
- CLEAN-02 satisfied: Unused functions identified and removed from active modules
- patterns.py audit complete — all functions have callers or are removed
- database.py audit complete — all private helpers have callers or are removed
- At least one additional large module audited
- Import smoke tests pass for all modified modules
- Commit messages document which functions were removed and why
</success_criteria>

<output>
After completion, create `.planning/phases/49-dead-code-cleanup/49-02-SUMMARY.md`
</output>
