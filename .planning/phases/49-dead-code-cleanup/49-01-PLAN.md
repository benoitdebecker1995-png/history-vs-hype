---
phase: 49-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/youtube_analytics/_csv_backfill.py
  - tools/youtube_analytics/_competitor_fetch.py
  - tools/youtube_analytics/_backfill_ids.txt
  - tools/youtube_analytics/_longform_all.json
  - tools/youtube_analytics/_longform_enriched.json
  - tools/youtube_analytics/_longform_ids.json
  - tools/youtube_analytics/_longform_metrics.json
  - tools/prompt_evaluation.py
  - .gitignore
  - .claude/skills/notebooklm-prompt-generator.md
  - .claude/skills/script-reviewer.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03
must_haves:
  truths:
    - "The 7 dead files (_csv_backfill.py, _competitor_fetch.py, _backfill_ids.txt, _longform_*.json) no longer exist on disk"
    - "prompt_evaluation.py no longer exists on disk (or is kept with documented rationale)"
    - "git status shows no untracked scratch files in tools/youtube_analytics/"
    - "tools/discovery/backups/ is gitignored and no longer appears as untracked"
    - "No skill files reference deleted modules"
  artifacts:
    - path: ".gitignore"
      provides: "Patterns preventing re-tracking of scratch files and backup directories"
      contains: "tools/youtube_analytics/_longform_"
    - path: ".gitignore"
      provides: "Backup directory exclusion"
      contains: "tools/discovery/backups/"
  key_links:
    - from: ".claude/skills/notebooklm-prompt-generator.md"
      to: "tools/prompt_evaluation.py"
      via: "reference removal"
      pattern: "prompt_evaluation references removed"
    - from: ".claude/skills/script-reviewer.md"
      to: "tools/prompt_evaluation.py"
      via: "reference removal"
      pattern: "prompt_evaluation references removed"
---

<objective>
Delete all confirmed dead files from the codebase and add .gitignore rules to prevent scratch file re-tracking.

Purpose: Satisfy CLEAN-01 (dead file removal) and mark CLEAN-03 (datetime.utcnow already fixed — zero occurrences). Remove prompt_evaluation.py after assessing it. Clean up stale skill references.

Output: Clean working tree with no dead files, proper .gitignore patterns, and updated skill files.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-dead-code-cleanup/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and add .gitignore patterns</name>
  <files>
    tools/youtube_analytics/_csv_backfill.py
    tools/youtube_analytics/_competitor_fetch.py
    tools/youtube_analytics/_backfill_ids.txt
    tools/youtube_analytics/_longform_all.json
    tools/youtube_analytics/_longform_enriched.json
    tools/youtube_analytics/_longform_ids.json
    tools/youtube_analytics/_longform_metrics.json
    .gitignore
  </files>
  <action>
    **CRITICAL:** These 7 files are UNTRACKED (shown as `??` in git status). Use `rm` to delete them, NOT `git rm`.

    1. Delete the 7 dead files from tools/youtube_analytics/:
       ```bash
       rm tools/youtube_analytics/_csv_backfill.py
       rm tools/youtube_analytics/_competitor_fetch.py
       rm tools/youtube_analytics/_backfill_ids.txt
       rm tools/youtube_analytics/_longform_all.json
       rm tools/youtube_analytics/_longform_enriched.json
       rm tools/youtube_analytics/_longform_ids.json
       rm tools/youtube_analytics/_longform_metrics.json
       ```

    2. Verify deletion: `ls tools/youtube_analytics/_*` should return no results.

    3. Add .gitignore patterns (append to existing .gitignore):
       ```
       # Scratch/temporary files in youtube_analytics (backfill artifacts)
       tools/youtube_analytics/_longform_*.json
       tools/youtube_analytics/_backfill_ids.txt
       # Database migration backups (auto-generated, not source)
       tools/discovery/backups/
       ```

    4. Note on backfill.py: The `_longform_enriched.json` and `_longform_metrics.json` files were read by backfill.py's `import_from_json_prefetch()` and `_load_own_channel_ids()` at runtime. Both functions have graceful fallbacks when files are absent. The data is already in the database. No code changes needed — backfill.py handles absence correctly.

    5. Note on CLEAN-03: datetime.utcnow() has zero occurrences in the codebase (confirmed by research). Mark CLEAN-03 as pre-satisfied in the commit message. No code changes needed.
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && ls tools/youtube_analytics/_csv_backfill.py 2>/dev/null; echo "EXIT:$?" && ls tools/youtube_analytics/_longform_*.json 2>/dev/null; echo "EXIT:$?" && grep -q "tools/discovery/backups/" .gitignore && echo "GITIGNORE_OK" || echo "GITIGNORE_MISSING"</automated>
    <manual>Verify git status shows no untracked _* files in tools/youtube_analytics/</manual>
  </verify>
  <done>All 7 dead files deleted from disk. .gitignore has patterns for _longform_*.json, _backfill_ids.txt, and tools/discovery/backups/. git status clean of those untracked entries.</done>
</task>

<task type="auto">
  <name>Task 2: Assess and remove prompt_evaluation.py + clean skill references</name>
  <files>
    tools/prompt_evaluation.py
    .claude/skills/notebooklm-prompt-generator.md
    .claude/skills/script-reviewer.md
  </files>
  <action>
    1. Read `tools/prompt_evaluation.py` to confirm the research assessment:
       - Contains prompt templates (SCRIPT_PROMPTS, FACT_CHECK_PROMPTS, VOICE_CHECK_PROMPTS)
       - Has `evaluate_script_quality()` heuristic scorer
       - Has `run_live_comparison()` calling Anthropic API
       - References deleted file paths (`.claude/SCRIPTWRITING-STYLE-GUIDE.md`)
       - The `channel_voice_check` subcommand cited in skills doesn't exist as an actual CLI flag

    2. Decision context: The module is superseded by:
       - script-writer-v2 agent (19 rules covering voice, style, retention)
       - script_checkers/ module (stumble, scaffolding, repetition, flow, pacing)
       - STYLE-GUIDE.md Part 6 voice pattern library
       Per user decision: if deleted, commit message must describe what the module did and why it was removed.

    3. Delete the file: `rm tools/prompt_evaluation.py`
       (If the file is tracked, use `git rm`. If untracked, use `rm`.)

    4. Clean stale references in skill files:
       - Read `.claude/skills/notebooklm-prompt-generator.md` and find the line referencing `tools/prompt_evaluation.py channel_voice_check`. Remove that reference line or replace with a note that voice checking is handled by script-writer-v2.
       - Read `.claude/skills/script-reviewer.md` and do the same.

    5. Check for any other references: `grep -rn "prompt_evaluation" tools/ .claude/` — clean all hits.

    6. Verify no Python imports break: `python -c "import tools; print('OK')"` (prompt_evaluation.py should not be imported by any active module).
  </action>
  <verify>
    <automated>cd "G:/History vs Hype" && ls tools/prompt_evaluation.py 2>/dev/null; echo "DELETED:$?" && python -c "from tools.youtube_analytics import backfill; print('OK')" && python -c "from tools.discovery import database; print('OK')"</automated>
    <manual>Verify skill files no longer reference prompt_evaluation.py</manual>
  </verify>
  <done>prompt_evaluation.py deleted. Skill files updated to remove stale references. No import errors in active modules. Commit message describes what the module did (prompt templates, heuristic scoring, voice check) and why it was removed (superseded by script-writer-v2, script_checkers/, and STYLE-GUIDE Part 6).</done>
</task>

</tasks>

<verification>
1. `ls tools/youtube_analytics/_*` returns nothing (no dead files remain)
2. `ls tools/prompt_evaluation.py` returns nothing
3. `grep -rn "prompt_evaluation" tools/ .claude/skills/` returns nothing
4. `git status` shows no untracked scratch files in tools/youtube_analytics/
5. `grep "tools/discovery/backups" .gitignore` finds the pattern
6. Python import smoke tests pass for youtube_analytics and discovery modules
</verification>

<success_criteria>
- CLEAN-01 satisfied: All 7 dead files removed, prompt_evaluation.py removed
- CLEAN-03 satisfied: Pre-satisfied (zero datetime.utcnow() occurrences confirmed)
- .gitignore prevents future scratch file tracking
- No stale references to deleted files in skill files or tool imports
</success_criteria>

<output>
After completion, create `.planning/phases/49-dead-code-cleanup/49-01-SUMMARY.md`
</output>
