---
phase: 46-project-dashboard
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - .claude/commands/status.md
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "/status with no arguments shows multi-project dashboard table"
    - "/status [project] retains existing single-project behavior unchanged"
    - "Dashboard mode calls scan_projects() and format_dashboard() from tools/dashboard/project_scanner.py"
    - "Dashboard output shows project name, phase, next action, days since activity for all active projects"
  artifacts:
    - path: ".claude/commands/status.md"
      provides: "Enhanced /status command with dashboard mode"
      contains: "project_scanner"
  key_links:
    - from: ".claude/commands/status.md"
      to: "tools/dashboard/project_scanner.py"
      via: "Python inline code block import"
      pattern: "from tools.dashboard.project_scanner import"
---

<objective>
Update the /status command to show a multi-project dashboard when called with no arguments, while preserving existing single-project behavior when a project name is provided.

Purpose: Makes the dashboard accessible via the existing /status entry point (DASH-01, DASH-02, DASH-03). The user gets a project-manager standup view showing what needs attention NOW.
Output: Updated .claude/commands/status.md with new DASHBOARD MODE section.
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-project-dashboard/46-01-SUMMARY.md
@.claude/commands/status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard mode to /status command</name>
  <files>.claude/commands/status.md</files>
  <action>
Update `.claude/commands/status.md` to add a DASHBOARD MODE section. The key change: when `/status` is called with NO arguments (no project name, no natural language question about a specific project), run the dashboard Python code first, then proceed with existing logic only if a specific project is identified.

**Structure the updated file as follows:**

1. Keep the existing frontmatter (description, model: haiku)

2. After the `## Usage` section, add a new section `## DASHBOARD MODE (No Arguments)` BEFORE the existing `## DETECTION LOGIC` section. This section instructs the agent to:

   a. **Trigger condition:** User runs `/status` with no arguments, or says "show all projects", "project dashboard", "what's in production"

   b. **Action:** Run the following Python code block inline:
   ```python
   import sys
   sys.path.insert(0, '.')
   from tools.dashboard.project_scanner import scan_projects, format_dashboard

   data = scan_projects('.')
   print(format_dashboard(data))
   ```

   c. **Display:** Show the output directly to the user. The formatted dashboard IS the response — no additional processing needed.

   d. **After displaying:** Ask "Which project would you like to work on?" to guide the user to a specific project action.

3. Update the existing `## Usage` section to clarify the two modes:
   ```
   /status                      # Dashboard: all projects overview
   /status [project]            # Single project: detailed status + next action
   ```

4. Update the `## DETECTION LOGIC` → `### Step 1: Find Active Project` section to add a new first check:
   - **Step 0 (new):** If no arguments provided → Run DASHBOARD MODE (above) instead of detection logic
   - Keep all existing steps 1-4 unchanged for when a specific project IS identified

5. Keep ALL existing sections intact (DETECTION LOGIC steps 2-4, SPECIAL CASES, NATURAL LANGUAGE RESPONSES, QUICK STATUS, Reference Files). The dashboard mode is purely additive — it adds a new code path for no-argument invocation without changing any existing behavior.

**Important:** The existing `/status` command is a pure LLM instruction file (no Python). This update adds the FIRST Python code block to it, following the same inline pattern used by `/analyze`, `/prep`, and `/publish` commands. The agent reads the `.md`, sees the code block, and executes it via Bash tool.
  </action>
  <verify>
1. Read `.claude/commands/status.md` and confirm:
   - DASHBOARD MODE section exists with Python code block
   - `from tools.dashboard.project_scanner import scan_projects, format_dashboard` is present
   - Existing DETECTION LOGIC sections are unchanged
   - Usage section shows both modes
2. Run the inline Python code manually:
   `python -c "import sys; sys.path.insert(0, '.'); from tools.dashboard.project_scanner import scan_projects, format_dashboard; data = scan_projects('.'); print(format_dashboard(data))"`
   — Should produce the same dashboard output as Plan 01 verification
  </verify>
  <done>
/status with no arguments triggers dashboard mode showing all projects table. /status [project] retains existing single-project detection and status behavior. The Python inline code block follows the established pattern from other slash commands. No existing functionality is broken.
  </done>
</task>

</tasks>

<verification>
1. `.claude/commands/status.md` contains "DASHBOARD MODE" section
2. `.claude/commands/status.md` contains `from tools.dashboard.project_scanner import`
3. `.claude/commands/status.md` still contains all original sections (DETECTION LOGIC, SPECIAL CASES, NATURAL LANGUAGE RESPONSES, QUICK STATUS)
4. Python code block in status.md executes without errors when run manually
</verification>

<success_criteria>
- /status with no arguments shows multi-project dashboard
- /status [project] behavior is completely unchanged
- Dashboard Python code block follows established slash command pattern
- All existing status.md sections preserved
- No new dependencies introduced
</success_criteria>

<output>
After completion, create `.planning/phases/46-project-dashboard/46-02-SUMMARY.md`
</output>
