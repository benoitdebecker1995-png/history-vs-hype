---
phase: 42.1-translation-pipeline-claude-code-integration
plan: 02
type: execute
wave: 2
depends_on: ["42.1-01"]
files_modified:
  - tools/translation/cross_checker.py
  - tools/translation/legal_annotator.py
  - tools/translation/surprise_detector.py
  - tools/translation/verification.py
  - tools/translation/cli.py
  - .claude/commands/verify.md
  - .claude/commands/translate.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "cross_checker.py contains zero 'import anthropic' references but retains DeepL/googletrans logic"
    - "legal_annotator.py contains zero 'import anthropic' references and provides payload builder + response parser"
    - "surprise_detector.py contains zero 'import anthropic' references and provides payload builder + response parser"
    - "verification.py contains zero 'import anthropic' references; audit mode unchanged, fresh/scholarly modes become payload builders"
    - "cli.py LLM-dependent subcommands (translate, crosscheck, annotate, surprise) show clear error message directing user to /translate command"
    - "cli.py detect and smoketest subcommands work without API key"
    - "verify.md --translation section reflects new no-API-key TranslationVerifier usage"
    - "No file in tools/translation/ imports anthropic or env_loader"
  artifacts:
    - path: "tools/translation/cross_checker.py"
      provides: "CrossCheckDataBuilder with build_comparison_payload() and parse_comparison_response(); DeepL/googletrans backends intact"
      contains: "build_comparison_payload"
    - path: "tools/translation/legal_annotator.py"
      provides: "AnnotationDataBuilder with build_annotation_payload() and parse_annotation_response()"
      contains: "build_annotation_payload"
    - path: "tools/translation/surprise_detector.py"
      provides: "SurpriseDataBuilder with build_surprise_payload() and parse_surprise_response()"
      contains: "build_surprise_payload"
    - path: "tools/translation/verification.py"
      provides: "TranslationVerifier with audit mode intact, scholarly comparison as payload builder"
      contains: "class TranslationVerifier"
    - path: "tools/translation/cli.py"
      provides: "CLI with detect + smoketest working, LLM commands showing redirect message"
      contains: "detect"
  key_links:
    - from: ".claude/commands/translate.md"
      to: "tools/translation/cross_checker.py"
      via: "Bash call for DeepL backend + payload building"
      pattern: "CrossCheckDataBuilder"
    - from: ".claude/commands/translate.md"
      to: "tools/translation/legal_annotator.py"
      via: "Bash call for payload building"
      pattern: "AnnotationDataBuilder"
    - from: ".claude/commands/verify.md"
      to: "tools/translation/verification.py"
      via: "TranslationVerifier instantiation (no API key needed)"
      pattern: "TranslationVerifier"
---

<objective>
Apply the prompt extraction pattern (validated in Plan 01) to the remaining 4 translation modules, update cli.py to redirect LLM commands, and update verify.md for the new TranslationVerifier interface.

Purpose: Complete the refactor — all 5 translation modules become pure data processors, eliminating all Anthropic SDK usage from the translation pipeline.

Output: Refactored cross_checker.py, legal_annotator.py, surprise_detector.py, verification.py, updated cli.py and verify.md
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42.1-translation-pipeline-claude-code-integration/42.1-CONTEXT.md
@.planning/phases/42.1-translation-pipeline-claude-code-integration/42.1-RESEARCH.md
@.planning/phases/42.1-translation-pipeline-claude-code-integration/42.1-01-SUMMARY.md
@tools/translation/cross_checker.py
@tools/translation/legal_annotator.py
@tools/translation/surprise_detector.py
@tools/translation/verification.py
@tools/translation/cli.py
@.claude/commands/verify.md
@.claude/commands/translate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor cross_checker.py, legal_annotator.py, surprise_detector.py, and verification.py</name>
  <files>
    tools/translation/cross_checker.py
    tools/translation/legal_annotator.py
    tools/translation/surprise_detector.py
    tools/translation/verification.py
  </files>
  <action>
Apply the same prompt extraction pattern from Plan 01 (translator.py → TranslationDataBuilder) to each module. Read each file fully before refactoring.

**cross_checker.py:**
1. Remove `import anthropic` and `from env_loader import ...`
2. Remove API client initialization from `__init__`
3. Keep `_translate_with_backend()` INTACT — this calls DeepL/googletrans, NOT Anthropic. This is the independent translation source.
4. Convert `_compare_translations()` (the Anthropic call) into `build_comparison_payload(claude_translation, backend_translation, original_text, clause_id)`:
   - Returns dict: `{'clause_id': ..., 'system_prompt': ..., 'user_prompt': ...}`
   - Keeps the comparison prompt template in Python
5. Add `parse_comparison_response(response_text, clause_id)`:
   - Parses the LLM response into structured comparison result with severity
   - Returns: `{'clause_id': ..., 'severity': ..., 'differences': [...], 'recommendation': ...}`
6. Rename class or keep as CrossChecker — but update docstring to clarify it's now a data processor for comparison payloads + a DeepL/googletrans backend caller
7. The `__init__` should still accept backend configuration (deepl_key, preferred_backend) since those are NOT Anthropic

**legal_annotator.py:**
1. Remove `import anthropic` and `from env_loader import ...`
2. Remove API client initialization
3. Convert `annotate_clause()` into `build_annotation_payload(clause_text, translation, clause_id, document_context)`:
   - Returns dict with system_prompt and user_prompt for legal term annotation
4. Add `parse_annotation_response(response_text, clause_id)`:
   - Parses LLM response into structured annotations
   - Returns: `{'clause_id': ..., 'annotations': [...], 'footnotes': [...]}`
5. Keep all formatting helpers and data structures

**surprise_detector.py:**
1. Remove `import anthropic` and `from env_loader import ...`
2. Remove API client initialization
3. Convert `_analyze_clause()` into `build_surprise_payload(clause_text, translation, narrative_baseline, clause_id)`:
   - Returns dict with system_prompt and user_prompt for surprise detection
4. Add `parse_surprise_response(response_text, clause_id)`:
   - Returns: `{'clause_id': ..., 'severity': ..., 'surprise_elements': [...], 'script_beats': [...]}`
5. Keep severity tier logic and formatting helpers

**verification.py:**
1. Remove `import anthropic` and `from env_loader import ...`
2. Remove API client initialization
3. `_audit_existing_output()` — KEEP UNCHANGED (pure Python regex parsing, no LLM)
4. Convert `_compare_against_scholarly_summary()` into `build_scholarly_comparison_payload(translation_text, scholarly_summary)`:
   - Returns payload dict for Claude Code to execute
5. Convert `_compare_against_claude_knowledge()` into `build_knowledge_comparison_payload(translation_text, document_name)`:
   - Returns payload dict
6. Add corresponding parse methods for each comparison type
7. `TranslationVerifier.__init__()` should take no API-related arguments — just project_dir or file paths
8. Audit mode requires zero changes (already pure Python)

**For all 4 modules:**
- Update module docstrings: "Pure data processor. LLM calls handled by Claude Code via /translate command."
- Ensure no `import anthropic` or `from env_loader` remains
- All existing pure Python helpers (formatting, parsing, data structures) stay unchanged
  </action>
  <verify>
Run for each module:
```
python -c "import sys; sys.path.insert(0, 'tools/translation'); import cross_checker; print('cross_checker OK')"
python -c "import sys; sys.path.insert(0, 'tools/translation'); import legal_annotator; print('legal_annotator OK')"
python -c "import sys; sys.path.insert(0, 'tools/translation'); import surprise_detector; print('surprise_detector OK')"
python -c "import sys; sys.path.insert(0, 'tools/translation'); import verification; print('verification OK')"
```
All must succeed without API key and without env_loader.

Grep: Zero occurrences of `anthropic` or `env_loader` across all 4 files.
  </verify>
  <done>All 4 modules are pure data processors. cross_checker retains DeepL/googletrans backend. verification audit mode unchanged. No Anthropic SDK usage remains in tools/translation/.</done>
</task>

<task type="auto">
  <name>Task 2: Update cli.py with redirect messages + update verify.md + update translate.md with new class names</name>
  <files>
    tools/translation/cli.py
    .claude/commands/verify.md
    .claude/commands/translate.md
  </files>
  <action>
**cli.py update:**
1. Read full cli.py
2. Remove import of `Translator` (now `TranslationDataBuilder`) for LLM-dependent commands
3. Keep imports for: `StructureDetector`, `Formatter`, `run_smoke_test`
4. For LLM-dependent subcommands (translate, crosscheck, annotate, surprise): replace their handler functions with a clear redirect message:
   ```python
   def cmd_translate(args):
       print("ERROR: The 'translate' subcommand has been replaced by the /translate slash command.")
       print("Run: /translate [project] --file [document] --language [language]")
       print("This provides the same functionality through Claude Code's native LLM integration.")
       sys.exit(1)
   ```
   Apply same pattern for crosscheck, annotate, surprise subcommands.
5. Keep `detect` subcommand fully functional (pure Python, calls StructureDetector)
6. Keep `smoketest` subcommand functional (calls updated smoke_test.py from Plan 01)
7. Update module docstring to reflect: detect and smoketest still work standalone; translation commands require /translate slash command
8. Remove any remaining `from env_loader import ...` or `import anthropic` if present

**verify.md update:**
1. Read current verify.md
2. Find the `## TRANSLATION VERIFICATION` section
3. Update the process description:
   - TranslationVerifier no longer requires API key initialization
   - Audit mode works as before (pure Python)
   - Fresh mode and scholarly comparison now require Claude Code (the /verify command itself IS Claude Code)
   - Update the "Run TranslationVerifier" instruction to reflect new no-arg constructor
4. Remove any references to API keys or anthropic SDK in the translation verification section
5. Keep the GREEN/YELLOW/RED verdict system unchanged

**translate.md update:**
1. Read the translate.md created in Plan 01
2. Update class references to match actual refactored names:
   - cross_checker: use actual class/method names from Task 1
   - legal_annotator: use actual class/method names from Task 1
   - surprise_detector: use actual class/method names from Task 1
3. Ensure Step 3 (cross-check) references the correct payload builder method
4. Ensure Step 4 (legal annotation) references the correct payload builder method
5. Ensure Step 5 (surprise detection) references the correct payload builder method
6. Make sure all Python code snippets in the command use correct import paths and class names
  </action>
  <verify>
Run: `python tools/translation/cli.py detect --help` — must show detect subcommand help without errors.
Run: `python tools/translation/cli.py translate` — must show redirect message and exit with code 1.
Grep: Zero occurrences of `anthropic` or `env_loader` in cli.py.
Grep: No stale API key references in verify.md translation section.
Confirm: translate.md references correct class names matching refactored modules.
  </verify>
  <done>cli.py detect/smoketest work standalone, LLM commands redirect to /translate. verify.md reflects new TranslationVerifier interface. translate.md uses correct refactored class names for all 5 modules.</done>
</task>

</tasks>

<verification>
1. Run comprehensive grep across entire `tools/translation/` directory: zero occurrences of `import anthropic`, `from env_loader`, `anthropic.Anthropic`, `load_api_key`
2. `python tools/translation/cli.py detect --file test.txt` works (pure Python)
3. `python tools/translation/cli.py smoketest` works (module health check, no credential check)
4. All 5 refactored modules import successfully without API key
5. `.claude/commands/translate.md` references correct class names for all 5 modules
6. `.claude/commands/verify.md` translation section reflects new no-API-key constructor
7. `tools/translation/env_loader.py` confirmed deleted (Plan 01)
</verification>

<success_criteria>
- Zero Anthropic SDK usage in entire tools/translation/ directory
- Zero env_loader references anywhere in translation pipeline
- All pure Python functionality preserved (structure detection, formatting, audit mode)
- cli.py works for detect/smoketest without API key
- /translate command orchestrates full pipeline through Claude Code
- /verify --translation works with new TranslationVerifier interface
- Clean break: no dual code paths, no fallback to direct API calls
</success_criteria>

<output>
After completion, create `.planning/phases/42.1-translation-pipeline-claude-code-integration/42.1-02-SUMMARY.md`
</output>
