---
phase: 38-structured-choice-architecture
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - .claude/agents/script-writer-v2.md
  - .claude/commands/script.md
autonomous: true

must_haves:
  truths:
    - "User can generate 2-3 opening hook variants before full script when using /script --variants flag"
    - "User can see 2 structural approaches proposed per video"
    - "Past choice patterns surface naturally in /script output before generation"
  artifacts:
    - path: ".claude/agents/script-writer-v2.md"
      provides: "Rule 16 for choice generation behavior"
      contains: "RULE 16"
    - path: ".claude/commands/script.md"
      provides: "--variants flag documentation and choice surfacing workflow"
      contains: "--variants"
  key_links:
    - from: ".claude/agents/script-writer-v2.md"
      to: "tools/youtube-analytics/technique_library.py"
      via: "Rule 16 references log_choice and get_recommendation"
      pattern: "technique_library"
    - from: ".claude/commands/script.md"
      to: ".claude/agents/script-writer-v2.md"
      via: "--variants flag triggers Rule 16 workflow"
      pattern: "--variants"
---

<objective>
Wire variant generation into the script workflow: add Rule 16 (choice generation) to script-writer-v2 agent, add --variants flag to /script command, and surface past choice patterns before script generation.

Purpose: This is the user-facing integration. Plan 01 built the engine; this plan makes it usable through the existing /script workflow.
Output: Rule 16 in agent + --variants flag in /script command
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-structured-choice-architecture/38-RESEARCH.md
@.planning/phases/38-structured-choice-architecture/38-CONTEXT.md
@.planning/phases/38-structured-choice-architecture/38-01-SUMMARY.md
@.claude/agents/script-writer-v2.md
@.claude/commands/script.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rule 16 to script-writer-v2 agent</name>
  <files>.claude/agents/script-writer-v2.md</files>
  <action>
Add Rule 16 between Rule 15 (Retention Playbook) and Rule 17 (Creator Technique Library) in script-writer-v2.md.

**Rule 16: VARIANT GENERATION & CHOICE ARCHITECTURE**

Keep concise with references (per user decision). Structure:

1. **When to activate:** Only when --variants flag is set. Without flag, skip Rule 16 entirely and write script normally.

2. **Hook variant generation (CHO-01):**
   - Generate 2-3 opening hook variants using Part 8 techniques (different technique per variant)
   - Each variant: 100-200 words of actual hook text (not outline)
   - Label: Hook A, Hook B, Hook C
   - Footnote-style attribution: "_Uses [technique_name] (Part 8.1)_" below each hook
   - If recommendation exists from technique_library.py get_recommendation(): display recommended variant first with "(Recommended - [reason])"
   - Present all variants. Wait for user to pick by letter (A/B/C). NEVER auto-skip variants.

3. **Structure variant generation (CHO-02):**
   - After hook selection, generate 2 structural approaches
   - Each approach: 3-5 sentence summary explaining approach FOR THIS SPECIFIC TOPIC (not abstract)
   - Template: "[Approach name]: [Opening] -> [Middle structure] -> [Ending]. [Key benefit]. [Key risk]."
   - Label: Structure 1, Structure 2
   - If recommendation exists: rank recommended first with rationale
   - Wait for user to pick (1/2)

4. **Choice logging (CHO-03):**
   - After each choice, call technique_library.py log_choice() with:
     - choice_type ('opening_hook' or 'structural_approach')
     - project_path (current video project path)
     - topic_type (territorial/ideological/factcheck from research docs)
     - selected_variant, selected_technique, all_variants
     - recommended_technique (what was recommended, if any)
   - Logging is silent — no confirmation message to user

5. **Pre-generation summary:**
   - Before showing variants, call get_choice_summary_for_topic(topic_type)
   - If patterns exist, display: "Past patterns for [topic_type] topics: You chose [technique] [X/Y] times"
   - If no patterns, skip (don't show "no data yet" — invisible friction per user decision)

**Keep Rule 16 under 60 lines.** Reference STYLE-GUIDE.md Part 8 for technique details, don't duplicate.
  </action>
  <verify>
Run: `grep -c "RULE 16" .claude/agents/script-writer-v2.md` — should return 1.
Run: `grep -n "RULE 1[567]" .claude/agents/script-writer-v2.md` — verify Rule 16 is between Rules 15 and 17.
Verify line count: `wc -l .claude/agents/script-writer-v2.md` — should be ~1410-1420 (1356 + ~55-60 for Rule 16).
  </verify>
  <done>Rule 16 exists in script-writer-v2.md between Rules 15 and 17, covers hook variants, structure variants, choice logging, and pre-generation summary</done>
</task>

<task type="auto">
  <name>Task 2: Add --variants flag and choice surfacing to /script command</name>
  <files>.claude/commands/script.md</files>
  <action>
Update .claude/commands/script.md to add --variants flag support and choice pattern surfacing.

**1. Add --variants to flag table:**
Add row: `--variants` | Generate hook and structure variants before full script | `/script --variants 35-gibraltar-treaty-utrecht-2026`

**2. Add --variants workflow section (after WRITE NEW SCRIPT section):**

```markdown
## VARIANT GENERATION (`--variants`)

Generate opening hook and structural approach variants before writing full script.

### Flow
1. Surface past choice patterns for topic type (if any exist)
2. Generate 2-3 opening hook variants (labeled A/B/C)
3. User picks hook by letter
4. Generate 2 structural approach variants (labeled 1/2)
5. User picks structure by number
6. Proceed with full script generation using selected hook + structure

### Choice Logging
Choices are automatically logged to database for pattern recognition.
After 5+ choices, the system recommends preferred options based on your past patterns.

### Review Past Choices
```
python tools/youtube-analytics/technique_library.py --choices
python tools/youtube-analytics/technique_library.py --choices territorial
python tools/youtube-analytics/technique_library.py --choice-stats
```
```

**3. Update PRE-SCRIPT INTELLIGENCE section:**
Add bullet under "Automatic Display": "Past hook and structure choice patterns for this topic type (from variant history)"

**4. Ensure --variants can combine with --new:**
Note in usage section that `--variants` works alongside `--new`: `/script --new --variants [project]`
  </action>
  <verify>
Run: `grep -c "\-\-variants" .claude/commands/script.md` — should return 3+ (flag table, workflow section, usage).
Run: `grep "choice" .claude/commands/script.md` — should show choice logging and review references.
  </verify>
  <done>--variants flag documented in /script command with workflow section, choice review CLI commands listed, pre-script intelligence updated to include choice patterns</done>
</task>

</tasks>

<verification>
1. Rule 16 exists between Rules 15 and 17 in script-writer-v2.md
2. Rule 16 covers: hook variants, structure variants, choice logging, pre-generation summary
3. --variants flag in /script command flag table and usage section
4. Past choice patterns mentioned in PRE-SCRIPT INTELLIGENCE
5. Choice review CLI commands documented
6. No existing rules modified (Rules 1-15, 17 unchanged)
</verification>

<success_criteria>
- Script-writer-v2 agent has Rule 16 with variant generation workflow
- /script command supports --variants flag with documented workflow
- Sequential choice flow: hooks first, then structure (per user decision)
- Footnote-style technique attribution (per user decision)
- Recommendation display: all variants shown, recommended ranked first with rationale (per user decision)
- Choice logging silent — no confirmation messages (per user decision)
- Past patterns surface in pre-script intelligence (per user decision)
</success_criteria>

<output>
After completion, create `.planning/phases/38-structured-choice-architecture/38-02-SUMMARY.md`
</output>
