---
phase: 38-structured-choice-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/youtube-analytics/technique_library.py
autonomous: true

must_haves:
  truths:
    - "User's hook and structure choices are logged to database with project context"
    - "After 5+ logged choices, system recommends preferred option based on past choice patterns"
    - "User can review past choices via CLI (technique_library.py --choices)"
  artifacts:
    - path: "tools/youtube-analytics/technique_library.py"
      provides: "Choice logging CRUD, recommendation engine, --choices CLI"
      contains: "script_choices"
  key_links:
    - from: "tools/youtube-analytics/technique_library.py"
      to: "tools/discovery/keywords.db"
      via: "SQLite schema v29 migration"
      pattern: "script_choices"
---

<objective>
Build the choice storage and recommendation foundation: schema migration to v29 (script_choices table), choice logging CRUD methods, recency-weighted recommendation engine, and --choices CLI flag.

Purpose: Creates the data layer that variant generation (Plan 02) will write to and read from. Without this, choices can't be logged or recommendations made.
Output: Extended technique_library.py with choice methods + schema v29
</objective>

<execution_context>
@C:/Users/benoi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/benoi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-structured-choice-architecture/38-RESEARCH.md
@.planning/phases/38-structured-choice-architecture/38-CONTEXT.md
@tools/youtube-analytics/technique_library.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration v29 + choice logging CRUD</name>
  <files>tools/youtube-analytics/technique_library.py</files>
  <action>
Extend TechniqueLibrary class with schema v29 migration and choice CRUD methods.

**Schema migration (_ensure_schema_v29):**
- Add method chained after _ensure_schema_v28 in __init__
- Create script_choices table:
  ```sql
  CREATE TABLE IF NOT EXISTS script_choices (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      choice_type TEXT NOT NULL,  -- 'opening_hook' or 'structural_approach'
      project_path TEXT NOT NULL,
      topic_type TEXT,  -- 'territorial', 'ideological', 'factcheck', etc.
      selected_variant TEXT NOT NULL,  -- 'Hook A', 'Structure 1', etc.
      selected_technique TEXT,  -- Part 8 technique name if applicable
      rejected_variants TEXT,  -- JSON array: ["Hook B", "Hook C"]
      recommended_technique TEXT,  -- What was recommended (for override tracking)
      choice_date DATE NOT NULL,
      UNIQUE(choice_type, project_path)
  );
  ```
- Create indexes: idx_choice_type_topic (choice_type, topic_type) and idx_choice_date (choice_date DESC)
- Set PRAGMA user_version = 29
- Follow existing pattern from _ensure_schema_v28 (conditional, idempotent)

**CRUD methods (all return error dicts on failure):**

1. `log_choice(choice_type, project_path, topic_type, selected_variant, selected_technique, all_variants, recommended_technique=None)`:
   - Compute rejected_variants = [v for v in all_variants if v != selected_variant]
   - Normalize project_path with Path.resolve()
   - UPSERT: INSERT ... ON CONFLICT(choice_type, project_path) DO UPDATE
   - Store choice_date as ISO format date
   - Return {'logged': True, 'action': 'inserted'|'updated'}

2. `get_choices(choice_type=None, topic_type=None, limit=50)`:
   - Query script_choices with optional filters
   - Order by choice_date DESC
   - Return list of dicts (deserialize rejected_variants from JSON)
   - Return {'choices': [...]} or {'error': msg}

3. `get_choice_stats()`:
   - Total choices, choices by type, choices by topic_type
   - Return {'total': int, 'by_type': {}, 'by_topic': {}}
  </action>
  <verify>
Run: `python -c "from tools.youtube_analytics.technique_library import TechniqueLibrary; lib = TechniqueLibrary(); print(lib.get_choice_stats())"` from repo root (adjust import path as needed). Should return {'total': 0, 'by_type': {}, 'by_topic': {}}.

Also verify schema: `python -c "import sqlite3; conn = sqlite3.connect('tools/discovery/keywords.db'); print(conn.execute('PRAGMA user_version').fetchone()); print(conn.execute('SELECT sql FROM sqlite_master WHERE name=\"script_choices\"').fetchone())"` — should show version 29 and table schema.
  </verify>
  <done>script_choices table exists in keywords.db, schema version is 29, log_choice/get_choices/get_choice_stats methods work without error</done>
</task>

<task type="auto">
  <name>Task 2: Recency-weighted recommendation engine + --choices CLI</name>
  <files>tools/youtube-analytics/technique_library.py</files>
  <action>
Add recommendation engine methods and CLI flag to TechniqueLibrary.

**Recommendation methods:**

1. `get_recommendation(choice_type, topic_type, available_techniques=None, decay_factor=0.9)`:
   - Implement three-tier fallback per user decision:
     a. Topic-specific (>=3 choices for this topic_type): query script_choices WHERE choice_type=? AND topic_type=?, ORDER BY choice_date DESC. Score each selected_technique using exponential decay (weight = decay_factor ** idx, most recent idx=0).
     b. Global (>=5 choices across all topics): same scoring but no topic filter.
     c. Part 8 fallback: if available_techniques provided, return technique with highest creator_count. Otherwise return None.
   - Before recommending, check `_should_recommend(technique, choice_type, topic_type)` — returns False if technique was recommended but overridden 3 consecutive times.
   - Return: `{'recommended': technique_name, 'reason': str, 'confidence': 'HIGH'|'MEDIUM'|'LOW', 'source': 'topic'|'global'|'part8'}` or None

2. `_should_recommend(technique, choice_type, topic_type)`:
   - Query last 3 choices WHERE choice_type=? AND topic_type=? AND recommended_technique=?
   - If all 3 have selected_technique != technique, return False (auto-adjust per user decision)
   - Otherwise return True

3. `get_choice_summary_for_topic(topic_type)`:
   - For surfacing in /script before generation: "You chose visual contrast 4/5 times for territorial topics"
   - Query opening_hook choices for topic_type, count technique frequencies
   - Query structural_approach choices similarly
   - Return {'hook_patterns': [{'technique': str, 'count': int, 'total': int}], 'structure_patterns': [...]} or empty if <1 choice

**CLI extension (in main() function):**

Add `--choices` flag to argparse:
- `--choices` with optional TOPIC_TYPE: `technique_library.py --choices` (all) or `--choices territorial`
- Display table: Date | Project | Topic | Type | Selected | Technique
- If no choices exist, print "No choices logged yet. Use /script --variants to start."

Add `--choice-stats` flag:
- Show total choices, breakdown by type and topic
  </action>
  <verify>
Run: `python tools/youtube-analytics/technique_library.py --choices` — should print "No choices logged yet" message.
Run: `python tools/youtube-analytics/technique_library.py --choice-stats` — should print totals (all 0).

Test recommendation with no data:
`python -c "from tools.youtube_analytics.technique_library import TechniqueLibrary; lib = TechniqueLibrary(); print(lib.get_recommendation('opening_hook', 'territorial'))"` — should return None (insufficient data).
  </verify>
  <done>Recommendation engine returns None gracefully with no data, --choices and --choice-stats CLI flags work, three-tier fallback logic implemented with auto-adjust after 3 overrides</done>
</task>

</tasks>

<verification>
1. Schema version is 29 with script_choices table and indexes
2. log_choice stores selected + rejected variants with UPSERT behavior
3. get_recommendation returns None with empty database (graceful fallback)
4. --choices CLI shows empty state message
5. Existing technique_library.py functionality unchanged (--list, --search, --stats still work)
</verification>

<success_criteria>
- script_choices table created with correct schema (choice_type, project_path, topic_type, selected/rejected variants, recommended_technique, choice_date)
- CRUD methods follow error dict pattern
- Recommendation engine implements exponential decay with three-tier fallback
- Auto-adjust stops recommending after 3 consecutive overrides
- CLI --choices and --choice-stats flags functional
- Schema v29 migration idempotent (safe to run multiple times)
</success_criteria>

<output>
After completion, create `.planning/phases/38-structured-choice-architecture/38-01-SUMMARY.md`
</output>
