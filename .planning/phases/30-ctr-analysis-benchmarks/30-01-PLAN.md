---
phase: 30-ctr-analysis-benchmarks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/youtube-analytics/benchmarks.py
  - tools/discovery/database.py
autonomous: true

must_haves:
  truths:
    - "Verdict correctly identifies winner when CTR difference exceeds 1.5pp with sufficient impressions"
    - "Verdict returns 'insufficient_data' when total impressions below 200"
    - "Verdict returns 'no_clear_winner' when CTR difference is below 0.5pp"
    - "Single-variant videos compare against category average and report above/below"
    - "Category benchmarks aggregate latest CTR per video grouped by topic_type"
    - "Categories with fewer than 2 videos fall back to overall channel average"
    - "Data freshness warning appears when most recent snapshot is older than 60 days"
  artifacts:
    - path: "tools/youtube-analytics/benchmarks.py"
      provides: "CTR analysis engine with verdict calculation and benchmark aggregation"
      contains: "calculate_verdict"
    - path: "tools/discovery/database.py"
      provides: "New query methods for variant CTR summary and channel benchmarks"
      contains: "get_variant_ctr_summary"
  key_links:
    - from: "tools/youtube-analytics/benchmarks.py"
      to: "tools/discovery/database.py"
      via: "KeywordDB import for get_variant_ctr_summary and get_channel_ctr_benchmarks"
      pattern: "from database import KeywordDB"
---

<objective>
Build the CTR analysis engine with heuristic verdict calculation and channel-specific benchmark aggregation.

Purpose: Enable the user to determine winning variants and compare video CTR to channel/category baselines using practical heuristics (not statistical tests) appropriate for a small channel (~10 long-form videos).

Output: `benchmarks.py` module with core functions and 2 new database query methods in `database.py`.
</objective>

<execution_context>
@C:\Users\Benoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Benoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ctr-analysis-benchmarks/30-RESEARCH.md

# Key source files
@tools/youtube-analytics/variants.py
@tools/discovery/database.py
@tools/youtube-analytics/performance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database query methods + verdict engine (TDD)</name>
  <files>tools/discovery/database.py, tools/youtube-analytics/benchmarks.py</files>
  <action>
  **RED phase — write failing tests first:**

  Create test functions (inline in benchmarks.py or a temporary test script) that verify:

  1. **calculate_verdict()** — pure function, no DB needed:
     - Empty variant_data returns `{'status': 'no_data', 'recommendation': 'Record CTR snapshots first'}`
     - Single variant with category_avg and 200+ impressions: returns `status: 'single_variant'` with above/below/near message
     - Single variant with <200 impressions: returns single_variant with low confidence
     - Two variants, delta < 0.5pp: returns `status: 'no_clear_winner'`
     - Two variants, delta 0.5-1.5pp: returns `status: 'edge'`, winner = higher CTR variant letter
     - Two variants, delta 1.5-3.0pp: returns `status: 'winner_found'` with medium confidence at 200-499 impressions
     - Two variants, delta 3.0+pp with 1000+ impressions: returns `status: 'winner_found'` with high confidence
     - Two variants, total impressions <200: returns `status: 'insufficient_data'`

  2. **get_category_benchmarks()** — needs test DB:
     - No snapshots: returns empty overall with 0 video_count
     - Snapshots for 3 videos across 2 categories: returns correct per-category averages using LATEST snapshot per video
     - Category with 1 video: still appears in by_category (caller decides min threshold)
     - Overall average includes all videos regardless of category

  3. **get_variant_ctr_summary()** — needs test DB:
     - No attributed snapshots: returns empty list
     - Snapshots attributed to 2 thumbnail variants: returns grouped avg_ctr, total_impressions, snapshot_count per variant

  4. **compare_variants_for_video()** — orchestrator function:
     - Calls get_variant_ctr_summary then calculate_verdict
     - Falls back to single_variant comparison against category_avg when no attributed variants
     - Includes freshness warning when latest snapshot > 60 days old

  **GREEN phase — implement to pass:**

  In `tools/discovery/database.py`, add two new methods to KeywordDB class:

  **get_variant_ctr_summary(video_id, variant_type='thumbnail'):**
  - Query ctr_snapshots grouped by active_thumbnail_id or active_title_id (based on variant_type)
  - JOIN to thumbnail_variants or title_variants to get variant_letter
  - Return list of dicts: `[{'variant_id': int, 'variant_letter': str, 'avg_ctr': float, 'total_impressions': int, 'snapshot_count': int}]`
  - Filter: only rows where the relevant active_*_id IS NOT NULL
  - Order by avg_ctr DESC

  **get_channel_ctr_benchmarks():**
  - Get latest snapshot per video (subquery: MAX(snapshot_date) per video_id)
  - JOIN to video_performance for topic_type
  - Calculate overall avg_ctr and video_count
  - Calculate per-category avg_ctr and video_count
  - Return `{'overall': {'avg_ctr': float, 'video_count': int, 'date_range': {'earliest': str, 'latest': str}}, 'by_category': {topic_type: {'avg_ctr': float, 'video_count': int}}}`
  - Handle NULL topic_type gracefully (group under 'general')
  - Use statistics.mean() for aggregation

  In `tools/youtube-analytics/benchmarks.py`, implement:

  **calculate_verdict(variant_data, category_avg=None):**
  - Pure function, no DB dependency
  - Implement impression confidence tiers: <200=low, 200-499=medium, 500+=high
  - Implement CTR difference thresholds: <0.5pp=TIE, 0.5-1.5pp=EDGE, 1.5+=WINNER
  - For 3.0+pp difference, use "CLEAR WINNER" in recommendation text
  - Single variant path: compare to category_avg if provided and impressions >= 200
  - Returns dict with keys: status, winner, confidence, recommendation, delta_pp (when applicable)

  **compare_variants_for_video(video_id, variant_type='thumbnail', benchmarks=None):**
  - Orchestrator: opens DB, calls get_variant_ctr_summary, gets benchmarks if not provided
  - Determines category from video_performance topic_type
  - Calls calculate_verdict with category_avg from benchmarks
  - Adds freshness warning if latest snapshot > 60 days old (use datetime comparison)
  - Adds attribution_rate field: "X of Y snapshots have variant attribution"
  - Returns verdict dict + metadata

  **get_benchmarks_report():**
  - Opens DB, calls get_channel_ctr_benchmarks
  - Adds freshness check on date_range
  - Returns formatted benchmark dict with overall + by_category
  - Marks categories with <2 videos as "low_sample" flag

  **REFACTOR phase:** Clean up any duplication between compare_variants_for_video and get_benchmarks_report (shared DB open/close pattern). Use `statistics` module for mean calculations consistently.

  **Thresholds (from research, Claude's discretion):**
  - Impression tiers: 200 (low), 500 (medium), 1000 (high)
  - CTR difference: <0.5pp (no_clear_winner), 0.5-1.5pp (edge), 1.5-3.0pp (winner_found), 3.0+pp (winner_found with "clear winner" text)
  - Freshness warning: 60 days
  - Min category videos for reliable: 2 (show category with n=1 but add low_sample flag)
  </action>
  <verify>
  Run the test functions. All tests must pass:
  - calculate_verdict returns correct status for each threshold boundary
  - get_category_benchmarks returns correct per-category averages from test data
  - get_variant_ctr_summary returns correct groupings from test data
  - compare_variants_for_video returns freshness warning when data is stale
  - Verify with: `python -c "from benchmarks import calculate_verdict; print(calculate_verdict([]))"` returns no_data status
  - Verify DB methods: `python -c "import sys; sys.path.insert(0, 'tools/discovery'); from database import KeywordDB; db = KeywordDB(); print(db.get_channel_ctr_benchmarks()); db.close()"`
  </verify>
  <done>
  - calculate_verdict handles all 8 test scenarios correctly (empty, single, multi at each threshold, insufficient impressions)
  - get_channel_ctr_benchmarks returns overall + by_category with correct aggregation
  - get_variant_ctr_summary returns grouped variant data with avg_ctr and totals
  - compare_variants_for_video orchestrates all three and adds freshness/attribution metadata
  - All functions follow error dict pattern (return {'error': msg} on failure)
  </done>
</task>

</tasks>

<verification>
1. `python -c "from benchmarks import calculate_verdict; v = calculate_verdict([{'variant_letter': 'A', 'avg_ctr': 5.0, 'total_impressions': 300, 'snapshot_count': 2}], category_avg=3.0); print(v['status'], v['recommendation'])"` prints single_variant with above-average message
2. `python -c "from benchmarks import calculate_verdict; v = calculate_verdict([{'variant_letter': 'A', 'avg_ctr': 5.0, 'total_impressions': 300, 'snapshot_count': 2}, {'variant_letter': 'B', 'avg_ctr': 3.0, 'total_impressions': 300, 'snapshot_count': 2}]); print(v['status'], v['winner'])"` prints winner_found A
3. Database methods execute without error on existing keywords.db
</verification>

<success_criteria>
- All verdict threshold boundaries produce correct status labels
- Benchmark aggregation uses latest-snapshot-per-video strategy
- Freshness warning triggers at 60+ days
- All functions importable from benchmarks.py
- Zero new external dependencies (stdlib only)
</success_criteria>

<output>
After completion, create `.planning/phases/30-ctr-analysis-benchmarks/30-01-SUMMARY.md`
</output>
