---
phase: 30-ctr-analysis-benchmarks
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - tools/youtube-analytics/benchmarks.py
  - tools/youtube-analytics/analyze.py
autonomous: true

must_haves:
  truths:
    - "User can run benchmarks.py VIDEO_ID and see variant verdict in terminal"
    - "User can run benchmarks.py --benchmarks and see all category averages in terminal"
    - "User can run benchmarks.py VIDEO_ID --markdown and get a saved markdown file"
    - "/analyze output includes CTR Analysis section after Variant Tracking with verdict and benchmark context"
    - "Both overall channel average AND category-specific average shown in /analyze output"
    - "CTR Analysis section only appears when CTR snapshot data exists (graceful absence)"
  artifacts:
    - path: "tools/youtube-analytics/benchmarks.py"
      provides: "CLI entry point with argparse and terminal/markdown formatters"
      contains: "def main"
    - path: "tools/youtube-analytics/analyze.py"
      provides: "Extended format_analysis_markdown with CTR Analysis subsection"
      contains: "CTR Analysis"
  key_links:
    - from: "tools/youtube-analytics/analyze.py"
      to: "tools/youtube-analytics/benchmarks.py"
      via: "import of format_ctr_analysis and compare_variants_for_video"
      pattern: "from benchmarks import"
    - from: "tools/youtube-analytics/benchmarks.py"
      to: "tools/discovery/database.py"
      via: "KeywordDB for database queries"
      pattern: "from database import KeywordDB"
---

<objective>
Add CLI interface to benchmarks.py and integrate CTR analysis into the existing /analyze workflow.

Purpose: Provide two entry points for CTR analysis — a quick summary appended to /analyze output, and a detailed standalone CLI command — matching the user's locked decision for dual entry points.

Output: Complete benchmarks.py CLI with terminal/markdown output, and extended analyze.py with CTR Analysis section.
</objective>

<execution_context>
@C:\Users\Benoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Benoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ctr-analysis-benchmarks/30-RESEARCH.md
@.planning/phases/30-ctr-analysis-benchmarks/30-01-SUMMARY.md

# Key source files
@tools/youtube-analytics/analyze.py
@tools/youtube-analytics/benchmarks.py
@tools/youtube-analytics/variants.py
@tools/youtube-analytics/performance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: benchmarks.py CLI with terminal and markdown output</name>
  <files>tools/youtube-analytics/benchmarks.py</files>
  <action>
  Add CLI entry point to existing benchmarks.py (which already has core logic from Plan 01).

  **argparse structure** (modeled on variants.py pattern):
  - `python benchmarks.py VIDEO_ID` — analyze single video: show variant verdicts (thumbnail + title) and benchmark comparison
  - `python benchmarks.py --benchmarks` — show all category benchmarks table
  - `python benchmarks.py VIDEO_ID --markdown` — save analysis to markdown file
  - `python benchmarks.py --benchmarks --markdown` — save benchmarks to markdown file

  **Terminal output format for VIDEO_ID mode:**
  ```
  CTR Analysis: VIDEO_ID
  ========================================

  Thumbnail Verdict:
    [WINNER] Variant A (+2.3pp CTR over B)
    Confidence: HIGH (1,200 total impressions)
    Recommendation: Keep A

  Title Verdict:
    [SINGLE VARIANT] Compare to territorial avg: 3.5%
    Your CTR: 4.3% — above category average by +0.8pp

  Benchmark Context:
    Channel avg CTR: 3.27%
    Category (territorial): 3.5% (n=4 videos)

  Data: 5 snapshots, 3 with variant attribution
  Latest snapshot: 2026-02-01 (7 days ago)
  ```

  If freshness warning applies (60+ days), append:
  ```
  WARNING: Latest snapshot is 75 days old — data may be stale
  ```

  **Terminal output format for --benchmarks mode:**
  ```
  Channel CTR Benchmarks
  ========================================

  Overall: 3.27% avg CTR (10 videos)
  Data range: 2025-08-01 to 2026-02-01

  By Category:
    territorial    3.50%  (n=4)
    ideological    5.20%  (n=3)
    colonial       2.10%  (n=2)
    general        1.80%  (n=1) *low sample

  * Categories with fewer than 2 videos have limited reliability
  ```

  **Markdown output** (--markdown flag):
  - For VIDEO_ID mode: save to project folder if found (use find_project_folder pattern from analyze.py), else save to current directory as `CTR-ANALYSIS-{video_id}.md`
  - For --benchmarks mode: save to `channel-data/CTR-BENCHMARKS.md`
  - Markdown format: proper headers, tables, bold verdicts

  **Implementation details:**
  - Add `format_terminal_video(video_id, thumb_verdict, title_verdict, benchmarks)` function
  - Add `format_terminal_benchmarks(benchmarks)` function
  - Add `format_markdown_video(video_id, thumb_verdict, title_verdict, benchmarks)` function
  - Add `format_markdown_benchmarks(benchmarks)` function
  - Add `main()` with argparse and `if __name__ == '__main__': main()`
  - Match sys.path.insert pattern from variants.py for KeywordDB import
  - Include docstring with Usage examples matching variants.py epilog style
  - Handle Shorts exclusion note in help text: "Note: Only record CTR for long-form videos. Shorts are excluded from analysis."
  </action>
  <verify>
  - `python tools/youtube-analytics/benchmarks.py --help` shows usage with both VIDEO_ID and --benchmarks modes
  - `python tools/youtube-analytics/benchmarks.py --benchmarks` runs without error (may show empty data if no snapshots recorded yet)
  - `python tools/youtube-analytics/benchmarks.py FAKE_VIDEO_ID` runs and shows "no data" message gracefully
  </verify>
  <done>
  - CLI runs in both modes (video analysis + channel benchmarks)
  - Terminal output is formatted and readable
  - --markdown flag saves to appropriate path
  - Graceful handling of missing data (no snapshots, no variants, no performance data)
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate CTR analysis into /analyze workflow</name>
  <files>tools/youtube-analytics/analyze.py</files>
  <action>
  Extend analyze.py to include CTR analysis in the /analyze output, following the existing VARIANTS_AVAILABLE pattern.

  **Step 1: Add benchmarks import** (near line 61-66, after VARIANTS_AVAILABLE block):
  ```python
  # Try to import CTR analysis (Phase 30)
  try:
      from benchmarks import compare_variants_for_video, get_benchmarks_report
      BENCHMARKS_AVAILABLE = True
  except ImportError:
      BENCHMARKS_AVAILABLE = False
  ```

  **Step 2: Extend run_analysis()** (after step 7 variant tracking, around line 443):
  Add step 8 that calls CTR analysis if benchmarks are available AND variant_data exists:
  ```python
  # 8. Run CTR analysis if available
  ctr_analysis = None
  if BENCHMARKS_AVAILABLE and variant_data:
      try:
          benchmarks_data = get_benchmarks_report()
          thumb_verdict = compare_variants_for_video(video_id, 'thumbnail', benchmarks_data)
          title_verdict = compare_variants_for_video(video_id, 'title', benchmarks_data)
          ctr_analysis = {
              'thumbnail_verdict': thumb_verdict,
              'title_verdict': title_verdict,
              'benchmarks': benchmarks_data
          }
      except Exception as e:
          errors.append({
              'source': 'ctr_analysis',
              'message': f'CTR analysis failed: {str(e)}'
          })
  ```
  Add `'ctr_analysis': ctr_analysis` to the return dict.

  **Step 3: Extend format_analysis_markdown()** (after existing Variant Tracking section, before Errors section):
  Add a "### CTR Analysis" subsection within the existing "## Variant Tracking" section. This goes AFTER the existing CTR History table and trend line (around line 1043).

  Format:
  ```markdown
  ### CTR Analysis

  **Thumbnail:** [VERDICT_LABEL] recommendation_text
  **Title:** [VERDICT_LABEL] recommendation_text

  **Channel avg CTR:** X.XX%
  **Category (topic_type) avg CTR:** X.XX% (n=N videos)

  _Data: X snapshots, Y with variant attribution | Latest: YYYY-MM-DD_
  ```

  Show both overall AND category average (per locked decision). Only show CTR Analysis subsection when ctr_analysis data exists (graceful absence pattern matching existing sections).

  If freshness warning exists in verdict, add:
  ```markdown
  > **Warning:** Latest snapshot is N days old — data may be stale
  ```

  **Key constraints:**
  - Do NOT restructure existing Variant Tracking section — append CTR Analysis as a subsection within it
  - Maintain the exact same graceful absence pattern: section only appears when data exists
  - Follow existing line-by-line append pattern (lines.append)
  - Keep the Errors section as the last section (CTR Analysis goes before Errors)
  </action>
  <verify>
  - `python tools/youtube-analytics/analyze.py FAKE_ID --markdown` runs without error (benchmarks import may gracefully degrade)
  - Grep analyze.py for "CTR Analysis" — section header exists in format function
  - Grep analyze.py for "BENCHMARKS_AVAILABLE" — import guard exists
  - `python -c "import sys; sys.path.insert(0, 'tools/youtube-analytics'); sys.path.insert(0, 'tools/discovery'); from analyze import run_analysis; print('OK')"` succeeds
  </verify>
  <done>
  - /analyze output includes CTR Analysis subsection when variant data and benchmarks exist
  - Both overall channel average and category-specific average are shown
  - Verdict labels (WINNER/EDGE/TIE/WAIT/NOTE) appear with recommendation text
  - Section gracefully absent when no CTR data exists
  - No changes to existing Variant Tracking tables or CTR History display
  </done>
</task>

</tasks>

<verification>
1. `python tools/youtube-analytics/benchmarks.py --help` shows complete usage info
2. `python tools/youtube-analytics/benchmarks.py --benchmarks` shows channel benchmarks (or clean "no data" message)
3. `python tools/youtube-analytics/analyze.py --help` still works (no import errors)
4. Grep for "BENCHMARKS_AVAILABLE" in analyze.py confirms integration
5. Grep for "CTR Analysis" in analyze.py confirms section formatting
6. Grep for "def main" in benchmarks.py confirms CLI entry point
</verification>

<success_criteria>
- Two entry points work: standalone CLI and /analyze integration
- Terminal and markdown output both render correctly
- Both overall and category averages shown (locked decision honored)
- Graceful degradation when no data exists
- --markdown flag saves to appropriate paths
- No breaking changes to existing /analyze output
</success_criteria>

<output>
After completion, create `.planning/phases/30-ctr-analysis-benchmarks/30-02-SUMMARY.md`
</output>
