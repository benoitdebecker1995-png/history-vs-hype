I will use Google Search to verify each entry's metadata. For each entry, I will construct a search query using the provided filename, known fields, and excerpt. I will then analyze the search results to find authoritative sources like publisher pages or academic databases to resolve missing fields. If a field cannot be resolved with high or medium confidence, it will be marked as 'UNRESOLVED'. I will ensure the `publisher_abbrev` is chosen from the provided list and the `source_url` is included for all resolved entries.
